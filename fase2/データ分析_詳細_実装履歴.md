---
name: データ分析-詳細-実装履歴
description: 旧CLAUDE.mdのfase2ファイル構成表にあった詳細記載(実装経緯・変更履歴・関数説明)の原文保存。2026-07-14のCLAUDE.md省エネ化で移設
metadata:
  type: project
---

# データ分析_詳細_実装履歴（旧CLAUDE.md fase2表の原文）

> 2026-07-14のCLAUDE.md省エネ化で移設した原文。各ファイルの実装経緯・変更履歴つき詳細説明。
> 最新の一次情報は[`データ分析_skill.md`](データ分析_skill.md)(ハブ)と`データ分析_詳細_*.md`を参照。

| ファイル | 状態 | 内容 |
|---|---|---|
| [`fase2/preprocess.py`](preprocess.py) | 実装済み | Stage0〜4-1: DB読み込み→正規化→Tier判定→logLR→統合スコア→判定保留（NumPyベクトル化済み） |
| [`fase2/patterns.py`](patterns.py) | 実装済み(2026-07-08 タスク3でS_据え置きを日次判定版に差し替え・同日タスク4で遷移モデルの層別拡張を追加・2026-07-09に`score_zentaikei_judgment`を追加・2026-07-10に末尾版レイヤー2を追加・同日「機種単位の癖分析」を追加・2026-07-13に導入後カーブを追加) | 移動台検出・幅型パターン(S_全台系/新台/移動台)・深さ型パターン(S_鉄板台/ローテ/据え置き)・αブレンド。S_鉄板台は該当日のみ効果量ベースでスコア付与(検出条件は`teppan_conditions`へ保存)、αは固定0.3(旧ウォークフォワード学習は当日リークのため停止、Stage7で再設計予定)。S_据え置きは`score_sueki_daily`(直近14日窓のlag-1自己相関をEWMA平滑した日次符号付きスコア、旧`score_sueki`の全期間1定数から差し替え。**2026-07-14応急処置**: prediction_accuracy実測で全店無信号と確定したため負側に`NEGATIVE_SCALE`=0.5を乗じて飽和を-1→-0.5に緩和、あわせて`weights.json`の重みを1.0→0.3に減重。据え置き癖の監視役は遷移行列のp_stay−πに委譲)・翌日投影は`predict_sueki_with_blend`。`estimate_transition_matrix`は`pi`(ベース率)も返す。`estimate_transition_matrix_stratified`は前日実測差枚の店舗内上位2割/下位8割で層別した遷移行列を推定し、並べ替え検定(`_stratified_permutation_test`)で層間差の有意性を判定する(タスク4)。`score_zentaikei_judgment`は機種×日×ホールでzスコア・投入率・S_全台系から3値の判定ラベル(全台系/高配分/普段どおり)を算出(同日×同ホール内でbenjamini_hochbergによるFDR補正、今後の実装予定.md 1.8節Phase1)。`calendar_candidates`は17→49候補(毎月X日31を追加、`calendar_test`にも自動適用)に拡張。`tail_digit_group`(台番号末尾0〜9+ゾロ目の11グループ)・`group_calendar_test`(グループ×日付条件のMann-Whitney U相対検定)・`match_rule_test`(日付=台番号下2桁/日付末尾=台番号末尾の一致ルール、Wilcoxon符号順位検定)・`build_group_calendar_conditions`(両者を1つの仮説群としてBH補正+効果量ゲート、`include_match_rules`/`include_constant`フラグでグループ定義違いに対応)・`predict_tail_group_next_day`(該当する有意条件のmax効果量を採用する翌日予測、重複統合込み)を追加(今後の実装予定.md 1.8節「末尾版」)。**機種単位の癖分析(2026-07-10)**: グループ定義を機種に差し替えた`machine_group`(日次有効台数中央値2台未満はNaN、`MACHINE_GROUP_MIN_UNITS`)・`group_constant_test`(看板機種検定。自グループvs同日の他グループ平均の対応ペアWilcoxon符号順位検定)・`predict_machine_group_next_day`を追加。恒常窓(全期間)・直近`RECENT_TEST_WINDOW_DAYS`(=90)日窓の2窓検定並走が確定設計(詳細は[`fase2/データ分析_skill.md`](データ分析_skill.md)「機種版: 看板機種＋機種カレンダー癖の検出」参照)。**導入後カーブ(2026-07-13)**: `detect_introduction_events`(店舗観測日ベースのタイムラインで機種レベルイベント5種=新台/増台/減台/再導入/純移動を判別。増台側は復帰ガード・減台は7日遅延確定・再導入は不在7日ベース、既存`detect_events`は変更なし)・`introduction_curve_test`(カテゴリ5種×経過日数ビン5種=最大25仮説を店舗単位でWilcoxon符号順位検定・両側+BH補正)・`predict_introduction_next_day`を追加。詳細は[`fase2/データ分析_skill.md`](データ分析_skill.md)「導入後カーブ」参照 |
| [`fase2/score.py`](score.py) | 実装済み(2026-07 UIリニューアルで`stage3_scores`拡張・2026-07-08 タスク3で`store_profile`に遷移4列追加・2026-07-09 タスク5で`score_snapshot`追加・同日`machine_judgment_log`追加・2026-07-10に`group_calendar_conditions`追加・同日「機種単位の癖分析」で拡張・2026-07-13に`introduction_events`追加) | S_稼働低さ・合成スコアΣ(wᵢ×Sᵢ)÷Σ(wᵢ)・店舗プロファイル管理。`write_prediction_log`は(ホール名, 予測種別, 使用データ最終日)単位の重複追記ガード付き(fase4のcatchup/リトライで同一予測が二重記録されるのを防ぐ)。`write_stage3_scores`は幅型/深さ型サブスコア6列(S_全台系/S_鉄板台/S_ローテ/S_新台増台/S_移動台/S_据え置き)も台×日粒度で保存(`_ensure_stage3_scores_schema`がPRAGMA table_info方式でマイグレーション。トップページ「熱い台予測」7タブが再計算なしで参照するため)。`update_store_profile`は店舗の癖(据え/上げ/下げ)として遷移_ベース率/p_stay/p_up/ペア数の4列も保存(記録のみ、表示は今後の実装予定)。`write_score_snapshot`はwᵢ学習用の日次スナップショット(店舗×使用データ最終日で1行、サブスコア店舗平均・狙い目度_店舗平均・実効重みJSON)を`score_snapshot`テーブルへappend-only記録(重複ガードは`write_prediction_log`と同型。学習側は蓄積待ちで未実装、記録側のみ先行実装)。`write_machine_judgment_log`は`patterns.score_zentaikei_judgment`の出力(機種×日×ホール粒度)を`machine_judgment_log`テーブルへappend-only記録(重複ガードは`PRIMARY KEY (ホール名, 日付, 機種名)`への`INSERT OR IGNORE`。全履歴バックフィル対応)。`write_group_calendar_conditions`は`patterns.build_group_calendar_conditions`/`introduction_curve_test`の出力(末尾版レイヤー2の検定結果・機種版の看板機種/機種カレンダー癖・導入後カーブ)を`group_calendar_conditions`テーブルへ保存(`teppan_conditions`と同じ「グループ種別単位で全削除→再挿入」方式。**2026-07修正**: 従来はホール名のみでの全削除だったため機種版書き込みが末尾版の既存行を消すバグがあり、`group_types`引数で削除対象を明示する方式に変更。機種版のみ使う`台数中央値`列も追加)。`write_introduction_events`は`patterns.detect_introduction_events`の出力(機種レベルイベント登記簿)を`introduction_events`テーブルへ保存(`teppan_conditions`と同じ店舗単位で全削除→再挿入方式。`app_top.py`の新台/増台タブ分離が読む) |
| [`fase2/app_a.py`](app_a.py) | 実装済み(2026-07-09に機種名比較ビューへ「設定投入率」軸を追加・2026-07-10に統合スコア軸を設定投入率へ置き換え) | 機能A: Streamlit 店内比較・可視化ツール（店舗分析/台番号/機種名の3ビュー）。`render(hole_name)`で店舗固定の本体を公開し、`app.py`の店舗トップページから呼ばれる（旧サイドバーフィルタ・単独起動は2026-07のUI再構成で廃止）。台番号ビューのY軸は「統合スコア」「差枚」の2択のまま。機種名比較ビューのY軸は「設定投入率」「差枚」の2択(機種×日のΣhigh_prob÷n×6を0〜6軸に表示。`patterns.score_zentaikei_judgment`をオンザフライ計算し、zスコアが閾値未満の点はグレー・小マーカー、0.9に基準線、ツールチップにz・期待高設定台数・台数を表示。台番号末尾フィルタとの併用は未対応(ガードメッセージ表示)。統合スコア軸は機種平均high_prob×6と数値的に同一曲線のため2026-07-10に廃止、設定投入率軸へ完全置き換え済み) |
| [`fase2/app_b.py`](app_b.py) | 実装済み(再設計Phase1〜6実装済み・Phase7は将来対応。2026-07 UIリニューアルで表示形式改修、2026-07-13機能B理想形で店舗特徴を再設計) | 店舗特徴(機能B個別店舗詳細) + 狙い目メモ。`render_store_detail(profiles, 店名)`（γ_store・**癖の有効性マトリクス**(`build_habit_matrix`。軸ごとに有効/弱い/無効/検証中/データ不足/対象外を判定、旧サブスコア内訳の縦棒グラフを置換)・**検知期間履歴**(機種名/台番号つき。`machine_judgment_log`/`stage3_scores`/`introduction_events`の実データ参照、ローテのみ未実装案内)・**カレンダーヒートマップ**(店舗内相対評価−1〜1、未来はカレンダー型条件から投影)→店舗トップページ「店舗特徴」、機能Aの「店舗分析」ビュー選択時のみ表示）を公開（旧`render_overview()`(店舗横断比較)は削除、店舗横断のおすすめ表示は`app_top.render_recommend_stores()`に統合。テキストメモは`python app_b.py`）。おすすめ店舗ランキング用の`compute_store_recommend_score`(案cハイブリッド)も公開。詳細は[`fase2/データ分析_skill.md`](データ分析_skill.md)「機能B再設計」Phase6参照 |
| [`fase2/app_top.py`](app_top.py) | 実装済み(機能B再設計Phase4。2026-07 UIリニューアルで再実装。同月08日に古い予測フィルタ・理由文表示を追加。同月13日に新台/増台タブ分離・機能B理想形でおすすめ店舗定義変更とスケール統一) | トップページ: `render_recommend_stores()`(MM/DD(曜)のおすすめ店舗。**2026-07-13再設計**: 「設定予測の的中が期待できる店舗」ランキング(`app_b.compute_store_recommend_score`、案cハイブリッド)の上位3+下位3を色分け表形式、稼働は添え列)と`render_hot_predictions()`(MM/DD(曜)の熱い台予測。店舗ごと/全店舗横断×個別台・機種・ローテ・新台・増台・移動台・据えの7タブ。**2026-07-13再設計**: 生スコアを符号付きパーセンタイル(`signed_percentile`、案b・0固定アンカー)に統一し表示列名を「予測スコア」に統一)を公開し`app.py`のホームページから呼ばれる（単独起動は廃止）。`_load_latest_predictions`は個別台/機種タブ(prediction_log由来)にグローバル最新対象日−2日の鮮度フィルタを適用し、猶予内の古い行は鮮度バッジ+グレーアウト、全店舗猶予落ち時は専用メッセージを表示。個別台タブのみ`teppan_conditions`由来の根拠短文をテーブル下にキャプション表示。**新台/増台タブ分離(2026-07-13)**: 両タブとも`S_新台増台`スコアは共通のまま、`introduction_events`テーブル(`_load_latest_introduction_categories`)から(ホール名,機種名)ごとの最新イベントカテゴリを読み、新台タブ=最新イベントが「新台」・増台タブ=「増台」の機種のみに振り分ける(旧`detect_events`は区別不能だったためこれまで同一データを表示していた) |
| [`fase2/app.py`](app.py) | 実装済み(2026-07 UI再構成・同月モバイルUI改修・同月黒基調ダークテーマ改修・同月UIリニューアル) | 統合エントリポイント。ホームページ(主ページ: 店舗検索selectbox＋おすすめ店舗＋熱い台予測7タブ)⇔店舗トップページ(店舗切替selectbox＋機能A店内比較＋店舗特徴。店舗特徴は店舗分析ビュー時のみ表示)を`st.session_state`で切替（サイドバーは廃止）。`streamlit run app.py`で起動。iPhone Safari前提のカードUI(`layout='centered'`・`ui_theme.inject_css()`・1カラムピルボタン)。黒基調ダークテーマ(ZZZ風・ライムアクセント`#D6FE3E`)、発散配色は プラス=青/マイナス=赤 に統一 |
| [`fase2/ui_theme.py`](ui_theme.py) | 実装済み(2026-07モバイルUI改修・同月黒基調ダークテーマ改修・同月UIリニューアルで色反転) | モバイルファーストUI(黒基調ダークテーマ・ZZZ風カードUI)の共通スタイル・Plotly設定を集約する表示層モジュール。パレット定数(`ACCENT`等・`POS_COLOR`=青/`NEG_COLOR`=赤)・`inject_css()`（角丸カード・ピルボタン・アクセント色`#D6FE3E`・タイトル折返し防止のCSS注入）・`apply_mobile_layout(fig, height, show_colorbar)`（全チャート共通のレイアウト調整。文字色・`colorway`・`gridcolor`・`automargin`・凡例横型・`dragmode=False`）・`wrap_label()`（長いラベルを省略せず改行折り返し）・`style_signed(df, cols)`（表の数値をプラス/マイナスで同配色に着色するStyler、2026-07追加）・`style_stale_rows(styler, stale_mask)`（指定行をグレーアウトするStyler、2026-07-08追加。古い予測の視覚的区別用）を提供。機能(データ・計算ロジック)には関与しない |
| [`fase2/evaluate_predictions.py`](evaluate_predictions.py) | 実装済み(機能B再設計Stage7-2) | `prediction_log`とレプリカの実測差枚を突き合わせ、Spearman相関・Precision@N・リフトを`prediction_accuracy`へ集計。リフトは**2026-07-14応急処置**で比率(÷店舗平均。0近傍で発散)から差枚差ベース(上位N台平均−店舗平均、単位:枚)へ変更。`python evaluate_predictions.py`で実行(翌日以降の実データがレプリカに入ってから意味を持つ) |
| [`fase2/multi_store.py`](multi_store.py) | 実装済み(2026-07再設計) | Stage1b+Stage5+Stage6: 機種別デシルカーブ(bin_curves)学習と**LOSO交差検証ゲート**(旧実装は循環学習だったため、直交化残差が翌観測日のRNG証拠を予測できた場合のみw3>0・γ_storeを保存。不合格時はw3=0=回転数チャンネル無効)・検証(Tier再現性/マクロ整合性)。`python multi_store.py`で全店舗一括実行 |
| [`fase2/data_source.py`](data_source.py) | 実装済み | fase2共通のデータ読み込み層。入力=Tursoレプリカ(`ホールデータ/turso_replica.db`、読み取り専用)・分析成果物の保存先=分析DB(`ホールデータ/analysis.db`)のパスと接続を集約 |
| [`fase2/run_store_profile.py`](run_store_profile.py) | 実装済み(2026-07-08 タスク3で`_run_sueki_predictions`追加・同日タスク4で`_run_transition_predictions`を拡張・2026-07-09 タスク5で`score_snapshot`書き込みを追加・同日`_run_zentaikei_judgment`を追加・2026-07-10に末尾版2関数を追加・同日「機種単位の癖分析」2関数を追加・2026-07-13に導入後カーブ2関数を追加) | 1店舗分のpreprocess→patterns→scoreパイプラインを通しで実行し、分析DBの`stage3_scores`(Stage3出力)と`store_profile`を更新するバッチ(`--hole <店舗名>`で特定店舗のみ)。`fase4/run_daily.py`が全店舗一括(引数なし)で毎日自動実行するほか、新規店舗取込時の即時反映等では引き続き手動実行も可能。`_run_teppan_predictions`直後に`_run_sueki_predictions`を実行し予測種別`'S_据え置き'`を`prediction_log`へ追記。`_run_transition_predictions`は無条件版(`'遷移予測'`)に加え、層間差が有意な店舗のみ前日差枚条件付き版(`'遷移予測_前日差枚'`)を並走記録する(タスク4)。`synthesize`直後に使用データ最終日断面の店舗平均を`sc.write_score_snapshot`で`score_snapshot`へ記録する(タスク5。wᵢ学習用の記録側先行実装)。`write_stage3_scores`直後に`_run_zentaikei_judgment`を実行し、機種×日の全台系/高配分判定(収集済み全履歴分)を`machine_judgment_log`へ記録する(π=`preprocess.load_channel_weights()['prior_high_ratio']`)。続けて`_run_group_calendar_conditions`(末尾版レイヤー2の検定結果を`group_calendar_conditions`へ保存)・`_run_tail_group_predictions`(予測種別`'S_末尾'`を`prediction_log`へ並走記録)を実行(今後の実装予定.md 1.8節「末尾版」)。さらに`_run_machine_group_conditions`(グループ=機種で恒常窓/直近90日窓の2窓検定を`group_calendar_conditions`へグループ種別`'機種'`/`'機種_直近'`で保存)・`_run_machine_group_predictions`(予測種別`'S_機種'`/`'S_機種_直近'`を`prediction_log`へ並走記録)を実行(今後の実装予定.md 1.8節「機種単位の癖分析」)。続けて`_run_introduction_conditions`(`patterns.detect_introduction_events`＋`introduction_curve_test`を実行し、イベント登記簿を`introduction_events`へ・導入後カーブの検定結果をグループ種別`'導入後'`で`group_calendar_conditions`へ保存)・`_run_introduction_predictions`(機種ごとの最新イベントから経過日数を求め、予測種別`'S_導入後'`を`prediction_log`へ並走記録)を実行(データ分析_skill.md「導入後カーブ」)。**注意**: `--hole`指定なしの全店舗一括実行を怠ると特定店舗だけ更新が滞り機能Bの予測日付が他店舗より遅れる事例が発生済み(詳細は[`fase2/データ分析_skill.md`](データ分析_skill.md)「既知の運用リスク」参照) |
| [`fase2/scrape_machine_specs.py`](scrape_machine_specs.py) | 実装済み | chonborista.comから機種別設定差確率表を取得し`raw_specs_scraped.json`に保存 |
| [`fase2/assign_tier.py`](assign_tier.py) | 実装済み | `raw_specs_scraped.json`を正規化・Tier判定して`machine_setting_specs.json`を再構築 |
| [`fase2/ground_truth_entry.py`](ground_truth_entry.py) | 実装済み(2026-07-12) | 正解発表(ground truth)のローカル専用入力フォーム(`streamlit run fase2/ground_truth_entry.py`。`app.py`には統合しない)。日付→ホール→機種名→台番号の順にレプリカ`slot_data`から候補を絞り込み、`ホールデータ/ground_truth.db`の`ground_truth`テーブルへappend-only記録。レプリカ未収集日は近傍±3日にフォールバック、直近1件のみ取消可能、入力済み一覧にレプリカとの突合チェック機能あり。詳細は[`fase2/今後の実装予定.md`](今後の実装予定.md)3節参照 |
