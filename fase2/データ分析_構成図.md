# 構成図 — 要件定義 2「設定推測・パターン分析」

## システム構成図

```mermaid
flowchart TD
    DB[("🗄️ slot_data\n(ホールデータ/turso_replica.db)\nfase1が維持するTurso埋め込みレプリカ\n読み取り専用参照(data_source.py)\n日付/機種名/台番号\n回転数/差枚/BB/RB\nBB確率/RB確率/合成確率")]

    subgraph preprocess["preprocess.py（前処理・キャリブレーション）"]
        Stage0["Stage 0: データ正規化\nTEXT→数値変換\n台×日のユニーク化"]
        Stage1a["Stage 1a: 機種別Tierキャリブレーション\nBB/RB列のTier A/B/C判定(機種×データ提供元で共通・全店再利用)\n列マッピング自己検出(resolve_rate_columns、店舗ごとに実データで検証)\n回転数の機械的バイアス補正"]
        Stage1b["Stage 1b: 店舗別行動成分(実装済み)\n回転数チャンネルの重み\nmulti_store.learn_bin_curvesが全店舗プールで\n機種別デシルカーブを学習→kaiten_bin_curves.json"]
        Stage2["Stage 2: 証拠チャンネルごとのlogLR計算\n①RNG確率(Tier A/B列) → ポアソン対数尤度\n②差枚(出玉) → 正規分布logLR\n  μ_sはspecsの理論機械割(payout)優先/無ければ経験的分位点\n③回転数(行動) → 経験的logLR曲線\n  (kaiten_bin_curves.json未学習時は0.0)"]
        Stage3["Stage 3: 統合スコア化\nLogOdds = β₀ + w₁·logLR₁ + w₂·logLR₂ + w₃·logLR₃\nβ₀=ln(π/(1-π)) 事前確率切片(π=prior_high_ratio、既定0.15)\n→ sigmoid で高設定確率(0〜1)\nw1/w2/w3/prior_high_ratioはstage3_channel_weights.jsonから自動読込"]
        Stage4["Stage 4: 信頼度しきい値・判定保留\n回転数不足台(期待当り数<5回)→ 判定不能除外\nStage4-1: 欠損偏りガード(判定不能率差チェック)"]
        Stage5["Stage 5: 複数店舗精緻化(2026-07再設計)\n階層モデル LogOdds_i = β₀+β₁y₁+β₂y₂+(β₃+γ_store)y₃\nLOSO交差検証(翌観測日y1予測)の合格時のみw3>0\n不合格時はw3=0(回転数チャンネル無効)\nγ_store = out-of-sample評価に基づく店舗固有補正"]
        MachineSpec["machine_setting_specs.json\n機種別理論値差表(153機種)\nTier A機種のp_s→Stage1a\n機械割(payout)→Stage2の全機種で使用"]
    end

    subgraph multistore["multi_store.py（複数店舗精緻化・検証、実装済み）"]
        MSLoad["load_all_stores_scored\nレプリカ内の全店舗をStage0〜2までプールして結合"]
        MSCurve["learn_bin_curves (Stage1b)\n機種別デシルカーブ学習\n→ kaiten_bin_curves.json"]
        MSFit["fit_hierarchical_model (Stage5)\nvalidate_kaiten_channel: LOSO交差検証\n(直交化残差y3⊥→翌観測日y1の予測力を検証)\n合格時のみw3>0 → γ_store・stage3_channel_weights.json"]
        MSStage6["Stage6検証\ncheck_tier_reproducibility(Tier・符号一致)\ncheck_macro_consistency(実測差枚率↔high_prob順位相関)"]
        MSLoad --> MSCurve --> MSFit --> MSStage6
    end

    subgraph patterns["patterns.py（パターン検出）"]
        Breadth["幅(breadth)型検出\n・S_全台系: 同機種/列/島の横断スコア集計\n・S_新台増台: 直近移動平均-基準値\n・S_移動台: 移動後直近平均-同機種店舗平均"]
        Depth["深さ(depth)型検出\n・S_鉄板台: ACF→PDM(→Lomb-Scargle)周期探索\n  + 既知カレンダー検定(49候補×FDR補正、2026-07-10に17→49へ拡張)\n  該当日のみ効果量ベースでスコア付与(2026-07変更)\n  検出条件はteppan_conditionsテーブルへ保存\n・S_ローテ: 集中度指標(エントロピー/ジニ)+並べ替え検定\n・S_据え置き: 直近14日窓lag-1自己相関(EWMA平滑)の\n  日次符号付きスコア(タスク3、旧全期間1定数から差替)"]
        BlendAlpha["長期/短期αブレンド(Section6.3)\n長期版(全履歴) + 短期版(直近Mウィンドウ)\nブレンド = α×短期 + (1-α)×長期\nα = 固定値0.3(2026-07変更。旧ウォークフォワード学習は\n当日リークのため停止、Stage7で再設計)"]
        MoveDetect["移動/撤去/増台 判定ロジック\nK=0日前日比較\n消失台番号 ↔ 新規台番号 → 移動N件\n残り → 撤去/増台"]
        FDR["FDR補正(Benjamini-Hochberg)\n候補49 × 対象台数 の多重比較補正\n+ 効果量ゲート(p値のみ非依拠)"]
        GroupDetect["グループ×日付条件のカレンダー癖検定(2026-07-10追加)\n・末尾版: 台番号末尾0〜9+ゾロ目の11グループ×49候補+一致ルール\n・機種版: グループ=機種(看板機種検定+機種カレンダー癖、恒常/直近90日の2窓)\nMann-Whitney U相対検定+BH補正+効果量ゲート\n→ group_calendar_conditionsへ保存、予測種別S_末尾/S_機種/S_機種_直近を並走記録\n・機種×日の全台系/高配分判定(score_zentaikei_judgment)→machine_judgment_log"]
    end

    subgraph score["score.py（スコア統合・店舗プロファイル）"]
        UpLimit["店舗×日 高設定台数上限キャリブレーション(Step1実装済み)\ncompute_uplimit: 長期/短期分位点blend_scalar\n超過日のみlog_odds/high_probをin-place補正\nStage3出力(Stage4通過後)に適用、S_稼働低さ後"]
        SubScore["サブスコア統合\n狙い目度 = Σ(wᵢ×Sᵢ) ÷ Σ(wᵢ)(符号付き[-1,1]、reliabilities減衰込み)\n不足スコアは0点でなく除外して再正規化"]
        StoreProfile["店舗プロファイル更新\n・パターン別サブスコア値(符号付き)\n・信頼度(履歴日数・欠損偏りフラグ反映)\n・γ_store(複数店舗揃ったら学習)\n・上限キャリブレーション値/上限信頼度\n保存先: ホールデータ/analysis.db"]
        PatternHistory["pattern_history追記(write_pattern_history)\nappend-only、検知期間履歴用\n保存先: ホールデータ/analysis.db"]
        Stage3Scores["stage3_scores保存\n(write_stage3_scores、上限補正後の値)\n台×日のlog_odds/high_prob/is_invalid\n保存先: ホールデータ/analysis.db"]
        TeppanCond["teppan_conditions保存\n(write_teppan_conditions)\n鉄板台の検出条件(周期日数含む)\n保存先: ホールデータ/analysis.db"]
        PredictionLog["prediction_log追記(write_prediction_log)\n予測種別7種: S_鉄板台(predict_next_day_with_blend)\n+ 遷移予測(Stage7-3無条件版、2状態マルコフ)\n+ S_据え置き(タスク3、日次自己相関×偏差)\n+ 遷移予測_前日差枚(タスク4、前日差枚層別・有意店舗のみ並走)\n+ S_末尾(末尾版・2026-07-10)\n+ S_機種/S_機種_直近(機種版・2026-07-10)\nappend-only、リーク防止\n保存先: ホールデータ/analysis.db"]
        Weights["weights.json\n初期重み wᵢ(均等=1)\nコード変更なしで調整可"]
    end

    subgraph evalpred["evaluate_predictions.py（評価専用・実装済み）"]
        EvalRun["evaluate()\nprediction_log × slot_data(実測差枚)をJOIN\nSpearman相関/Precision@N/リフトを集計"]
        PredictionAccuracy["prediction_accuracy UPSERT\n(ホール名, 予測種別)ごとの的中率キャッシュ\n保存先: ホールデータ/analysis.db"]
        EvalRun --> PredictionAccuracy
    end

    subgraph output["output/（出力、app.pyの2ページ構成で統合表示。2026-07UIリニューアルで再編）"]
        AppRecommend["MM/DD(曜)のおすすめ店舗\n(app_top.render_recommend_stores())\n符号付き合成スコアのプラス上位3+マイナス下位3\n表形式・色分け(プラス=青/マイナス=赤)"]
        AppHotPred["MM/DD(曜)の熱い台予測\n(app_top.render_hot_predictions())\n店舗ごと(検索セレクトボックス)/全店舗横断の2段構成\n×個別台・機種・ローテ・新台・増台・移動台・据えの7タブ\n(新台/増台は同一データ。区別ロジックは今後の実装予定.md)"]
        FuncA["機能A: 店内比較・可視化\n(Streamlit、app_a.render(hole_name)・店舗固定)\nビュー: 店舗分析 / 台番号 / 機種名\nY軸: 統合スコア・差枚のみ\n日付絞り込み(末尾/曜日/単日)\n台番号末尾絞り込み(複数選択・色分け)\n日付末尾→台番号末尾 自動同期\n横軸YYYY/MM/DD(1週間時は曜日付き)\n※旧サイドバーフィルタは廃止"]
        FuncB_Detail["店舗特徴(機能B-個別店舗詳細)\n(app_b.render_store_detail(profiles, 店名))\n機能Aで「店舗分析」ビュー選択時のみ表示\nサブスコア内訳(縦棒グラフのみ)+γ_store\n検知期間履歴(パターン別st.tabsで分割)\n当月カレンダーヒートマップ2層(第1週が上)"]
        FuncB_Simple["機能B-簡潔: 狙い目メモ\n次回来店前の要約\n結論+最低限の根拠のみ\n(python app_b.py)"]
        AppEntry["app.py(streamlit run app.py)\nホームページ(主ページ): 店舗検索selectbox+AppRecommend+AppHotPred\n店舗トップページ: 店舗切替selectbox+FuncA+FuncB_Detail\n(st.session_stateで切替、サイドバー廃止)"]
        AppEntry -.->|ホームページ| AppRecommend
        AppEntry -.->|ホームページ| AppHotPred
        AppEntry -.->|店舗トップページ| FuncA
        AppEntry -.->|店舗トップページ| FuncB_Detail
    end

    %% メインフロー
    DB --> Stage0
    MachineSpec --> Stage1a
    MachineSpec --> Stage2
    Stage0 --> Stage1a
    Stage1a --> Stage1b
    Stage1b --> Stage2
    Stage2 --> Stage3
    Stage3 --> Stage4
    Stage4 -->|"LogOdds/高設定確率\n(台×日の中間スコア)"| Breadth
    Stage4 -->|"LogOdds/高設定確率"| Depth
    Stage4 --> Stage5
    Stage5 -->|"γ_store更新"| StoreProfile

    Breadth --> MoveDetect
    MoveDetect --> Breadth
    Depth --> FDR
    FDR --> Depth
    Stage4 -->|"high_prob(上限補正後)"| GroupDetect
    GroupDetect -->|"S_末尾/S_機種/S_機種_直近"| PredictionLog

    Breadth --> BlendAlpha
    Depth --> BlendAlpha

    BlendAlpha --> SubScore
    SubScore --> Weights
    Weights --> SubScore
    SubScore --> UpLimit
    UpLimit --> StoreProfile
    StoreProfile --> PatternHistory
    StoreProfile --> FuncB_Detail
    StoreProfile --> FuncB_Simple
    PatternHistory -->|"検知期間履歴・カレンダーヒートマップ"| FuncB_Detail

    Depth -->|"score_teppandaiのdetails_out"| TeppanCond
    TeppanCond -->|"翌日の該当/非該当判定"| PredictionLog
    UpLimit -->|"上限補正後のlog_odds/high_prob"| PredictionLog
    PredictionLog -->|"個別台/機種タブ(S_鉄板台の最新行)"| AppHotPred
    PredictionLog -.->|"対象日の実測差枚とJOIN"| EvalRun
    PredictionAccuracy -->|"的中率(個別台/機種タブ、他5タブは検証中固定)"| AppHotPred
    StoreProfile -->|"符号付き合成スコア"| AppRecommend
    StoreProfile -->|"パターン別信頼度(7タブの店舗ゲート)"| AppHotPred

    Stage4 -->|"run_store_profile.py実行時(上限補正後)"| Stage3Scores
    Stage3Scores -->|"初期表示の高速読み込み\n(未作成時はオンザフライ計算)"| FuncA
    Stage3Scores -->|"熱い台(最新日の上位high_prob)"| FuncB_Simple
    Stage3Scores -->|"店舗ごとの最新日スナップショット(ローテ/新台/増台/移動台/据え)"| AppHotPred
    Stage3 --> FuncA
    SubScore --> FuncA

    %% multi_store.py 連携(複数店舗データをプールしてStage1b/5/6を実行)
    Stage0 -.->|"全店舗分をプール"| MSLoad
    MSCurve -.->|"kaiten_bin_curves.json"| Stage1b
    MSFit -.->|"stage3_channel_weights.json"| Stage3
    MSFit -.->|"γ_store"| StoreProfile
```

> **店舗×日 高設定台数上限キャリブレーション(Step1実装済み)**: Stage4出力(score.pyが店舗プロファイルに書き込む直前、S_稼働低さ計算後)に、店舗全体のE[high_prob]/Nが動的上限を超えた場合にソフトシュリンクする後処理(`score.compute_uplimit`)を挿入済み。Stage5(γ_store)の正則化として位置づけ。Kalman filterによる上限自体の逐次推定(Step2)はmulti_store.py側のKalman基盤構築後に着手予定(未着手)。詳細は[`データ分析_skill.md`](データ分析_skill.md)「長期/短期αブレンド」直後・今後の項目は[`今後の実装予定.md`](今後の実装予定.md)参照。
>
> **機能B再設計(Phase1〜5実装済み・Phase6は将来項目)**: `SubScore`(狙い目度)は符号付き[-1,1]に拡張済み、`pattern_history`(append-only)・`prediction_log`/`prediction_accuracy`(Stage7-0〜7-2、S_鉄板台の翌日予測基盤)・`AppRecommend`+`AppHotPred`(トップページ、2026-07UIリニューアルで`AppTop`から分割・7タブ化)を実装済み。S_ローテ・S_据え置きの翌日予測拡張(Stage7-5)・ウォークフォワードα学習への置き換え(Stage7-4)・新台/増台タブの区別ロジックは将来項目、詳細は[`今後の実装予定.md`](今後の実装予定.md)参照。

---

## 処理フロー補足

| フェーズ | 処理 | 担当モジュール |
|---|---|---|
| ① 正規化 | TEXT→数値変換・台×日ユニーク化 | `preprocess.Stage0` |
| ② Tier判定 | BB/RB列の信頼性分類(A/B/C)・列マッピング自己検出(店舗ごとの実列検証)・回転数バイアス補正 | `preprocess.Stage1a` |
| ③ logLR計算 | 3チャンネル(RNG/差枚/回転数)ごとに対数尤度比を算出 | `preprocess.Stage2` |
| ④ 統合スコア | weighted LogOdds → sigmoid → 高設定確率 | `preprocess.Stage3` |
| ⑤ 判定保留 | 回転数不足台の除外・欠損偏りガード | `preprocess.Stage4` |
| ⑥ パターン検出 | 幅型(全台系/新台増台/移動台)・深さ型(鉄板台/ローテ/据え置き) | `patterns.py` |
| ⑦ αブレンド | 長期版・短期版サブスコアを学習的重みαで合成 | `patterns.blend` |
| ⑧ スコア統合 | Σ(wᵢ×Sᵢ)÷Σ(wᵢ)、不足スコアは除外 | `score.py` |
| ⑨ 店舗プロファイル更新 | サブスコア・信頼度・γ_storeを再計算して分析DB(`ホールデータ/analysis.db`)へ保存 | `score.py` |
| ⑩ 出力 | 機能A(可視化)/店舗特徴(個別店舗詳細)/簡潔メモ。app.pyのホームページ(店舗検索+おすすめ店舗+熱い台予測7タブ)⇔店舗トップページ(機能A+店舗特徴、店舗分析ビュー時のみ表示)の2ページ構成で統合表示 | `output/` |
| ①〜⑨一括実行(1店舗分) | ①〜⑨を通しで実行し`stage3_scores`と`store_profile`を作成・更新するバッチ(`--hole <店舗名>`で特定店舗のみ) | `run_store_profile.py` |
| Stage1b/5/6(複数店舗) | 全店舗データをプールしてbin_curves・γ_store・βを学習、Tier再現性/マクロ整合性を検証 | `multi_store.py` |

---

## 台同一性・移動検出ロジック

```
店舗×機種名で D-1 と D を比較:
  消失台番号 = D-1に存在しDに存在しない台番号
  新規台番号 = D-1に存在せずDに存在する台番号
  N = min(len(消失), len(新規))
  → N件を「移動」(S_移動台の対象) として扱う
  → 消失の残り = 撤去
  → 新規の残り = 増台(S_新台増台の対象)
```

- 号機(実機固有ID)は**取得不可・確定**。同一性キー = (機種名, 台番号)
- 台番号変化で系列リセット(鉄板台・据え置きの自己相関履歴も含む)
- 同型入替の検知は**対象外(既知の制約)**

---

## サブスコア一覧

| サブスコア | 系統 | 入力の実体 | データ不足時 | αブレンド |
|---|---|---|---|---|
| S_全台系 | 幅 | Stage3スコアの当日横断集計 | 除外 | ◯ |
| S_鉄板台 | 深さ | Stage3スコアの縦断履歴(台単位) | 除外 | ◯ |
| S_ローテ | 深さ | Stage3スコアの縦断履歴(機種/列単位) | 除外 | ◯ |
| S_新台増台 | 幅 | Stage3スコアの移動平均(増台後) | 除外 | — |
| S_移動台 | 幅 | Stage3スコアの移動平均(移動後) | 除外 | — |
| S_据え置き | 深さ | Stage3スコアの自己相関 | 除外 | ◯ |
| S_稼働低さ | 店舗全体 | 店舗全体の回転数集計(Section7) | 除外 | — |

---

## DBスキーマ(入力・fase1から引き継ぎ)

入力は`ホールデータ/turso_replica.db`(fase1が維持するTurso埋め込みレプリカ)。パス・接続は`data_source.py`に集約され、sqlite3の読み取り専用モードで参照する(fase2からTursoへの直接接続はしない)。

### slot_data テーブル(読み取り専用)

| カラム | 型 | 備考 |
|---|---|---|
| 日付 | TEXT | YYYY-MM-DD |
| ホール名 | TEXT | URLスラッグ |
| 機種名 | TEXT | |
| 台番号 | INTEGER | |
| 回転数 | INTEGER | |
| 差枚 | INTEGER | 符号付き |
| BB / RB / ART | INTEGER | ART列は多くの店舗で全行NULLだが、店舗によってはBB相当のデータがART列に入る(resolve_rate_columnsが吸収) |
| BB確率 / RB確率 / ART確率 / 合成確率 | REAL | 1/xxx → 実数変換済み |

### 派生データ(fase2で新規追加)

| テーブル / ファイル | 内容 |
|---|---|
| `stage3_scores` テーブル | 台×日ごとのLogOdds・高設定確率・判定不能フラグ+幅型/深さ型サブスコア6列(S_全台系/S_鉄板台/S_ローテ/S_新台増台/S_移動台/S_据え置き、2026-07UIリニューアルで追加)。保存先は分析DB(`ホールデータ/analysis.db`)。`run_store_profile.py`実行時に`score.write_stage3_scores`が店舗単位で全削除→再挿入(2026-07: 店舗×日 高設定台数上限キャリブレーション(`score.compute_uplimit`)による補正後の値を保存するため、書き込み位置をパイプライン後段(S_稼働低さ計算後)に移動)。サブスコア6列はトップページ「熱い台予測」7タブが再計算なしで最新日スナップショットを参照するための追加 |
| `store_profile` テーブル | 店舗×パターンのサブスコア・信頼度・γ_store・上限キャリブレーション値・上限信頼度・更新日時。保存先は分析DB(`ホールデータ/analysis.db`)。`run_store_profile.py`で作成・更新(未実行の店舗は行が無く機能Bに表示されない)。「上限キャリブレーション値」「上限信頼度」は2026-07にStep1実装済み(`gamma_store`と同様、店舗単位のスカラー値を全パターン行に複製) |
| `teppan_conditions` テーブル | S_鉄板台の検出条件(経路/条件/効果量/周期経路は周期日数)の台単位メタデータ(2026-07追加)。保存先は分析DB。「明日は該当日か」の判断・トップページ表示・翌日予測に使う |
| `pattern_history` テーブル(機能B再設計Phase1) | `store_profile`と異なりappend-only(DELETEしない)で検知期間履歴を蓄積。`app_b.py`の検知期間履歴・カレンダーヒートマップ2層目が参照する |
| `prediction_log` テーブル(機能B再設計Stage7-1) | 翌日予測結果をappend-onlyで記録(リーク防止: 使用データ最終日は実データから取得)。予測種別は現在7種: `'S_鉄板台'`(検出済みの台のみ)・`'遷移予測'`(Stage7-3、判定可能な全台の無条件版)・`'S_据え置き'`(2026-07-08 タスク3、r̄_t計算可能な全台)・`'遷移予測_前日差枚'`(2026-07-08 タスク4、層間差が有意な店舗のみ`'遷移予測'`と並走記録する条件付き版)・`'S_末尾'`(末尾版・2026-07-10)・`'S_機種'`/`'S_機種_直近'`(機種版・2026-07-10)。`app_top.render_hot_predictions()`の個別台/機種タブは`'S_鉄板台'`のみ最新行を表示(他6種はトップページ非表示) |
| `machine_judgment_log` テーブル(2026-07-09) | 機種×日×ホール粒度の全台系/高配分判定ログ(`patterns.score_zentaikei_judgment`の出力をappend-only記録。`PRIMARY KEY (ホール名, 日付, 機種名)`への`INSERT OR IGNORE`) |
| `score_snapshot` テーブル(2026-07-09) | wᵢ学習用の日次スナップショット(店舗×使用データ最終日で1行、各サブスコアの当日断面店舗平均・狙い目度_店舗平均・実効重みJSON)。append-only+重複ガード。学習側は未実装(記録側のみ先行) |
| `group_calendar_conditions` テーブル(2026-07-10) | 末尾版・機種版のカレンダー癖検定結果(ホール名/グループ種別/グループキー/日付条件/p値/効果量/該当日数/BH有意フラグ/台数中央値)。グループ種別単位で全削除→再挿入 |
| `prediction_accuracy` テーブル(機能B再設計Stage7-2) | `evaluate_predictions.py`が`prediction_log`と実測差枚を突き合わせて集計する的中率キャッシュ(Spearman相関・Precision@N・リフト)。サンプル数`MIN_SAMPLES`未満は検証中扱い |
| `uplimit_config.json`(店舗上限モデルStep1) | 分位点・安全マージン・絶対上限・業界一般値フォールバック・短期ウィンドウ日数・最低必要日数・特異日除外リストを外出し |
| `machine_setting_specs.json` | 機種別理論値差表。全機種にBB/RB/機械割(payout)を格納。BB/RBはTier A機種のみp_sとしてStage1aで使用、機械割(payout)は全機種でStage2差枚チャンネルのμ_sとして使用(店舗単位の一律補正は入れない、理由は`データ分析_skill.md`参照)。`scrape_machine_specs.py`(chonborista.comから収集)→`assign_tier.py`(Tier判定・正規化)で生成 |
| `raw_specs_scraped.json` | `scrape_machine_specs.py`が取得した生データ(machine_setting_specs.json生成前の中間ファイル) |
| `weights.json` | サブスコアの重み wᵢ(コード変更なしで調整可) |
| `kaiten_bin_curves.json` | Stage1b(`multi_store.learn_bin_curves`)が全店舗データから学習した機種別デシルカーブ。`preprocess.compute_all_logLR`が自動読込 |
| `stage3_channel_weights.json` | Stage5(`multi_store.fit_hierarchical_model`)が学習したチャンネル重み{w1, w2, w3}+直交化パラメータ{orth_a, orth_b}+事前確率{prior_high_ratio}(手動設定値、既定0.15。β₀=ln(π/(1-π))としてStage3に入る。Stage5の保存時も既存値を保全)。既定はw3=0.0(回転数チャンネル無効)で、LOSO交差検証合格時のみ正のw3を保存。`preprocess.compute_log_odds`/`compute_all_logLR`が自動読込 |
