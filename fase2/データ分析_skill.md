---
name: データ分析-skill
description: データ分析フェーズ全体のスキルまとめ（preprocess.py / patterns.py / score.py）— 設定推測アルゴリズム(Stage0〜6)・パターン検出・スコア統合・店舗プロファイル管理
metadata:
  type: project
---

# データ分析スキル — 総合リファレンス

> 要件定義「2. 設定推測・パターン分析」・設計メモv0.6・データ分析_構成図.md を統合したドキュメント（2026-06-28）

---

## システム概要

`slot_data`(fase1で収集済み。入力は`ホールデータ/turso_replica.db`＝fase1が維持するTurso埋め込みレプリカを読み取り専用参照)を入力として、個別台の高設定スコアを算出し、店舗パターンを検出・スコアリングして3系統の出力を生成する。分析成果物(`stage3_scores`/`store_profile`)は`ホールデータ/analysis.db`に保存する(パス・接続は`data_source.py`に集約)。

```
data_source.py（レプリカ/分析DBのパス・接続の共通層）
  └── preprocess.py（正規化・キャリブレーション・LogOddsスコア生成 / Stage0〜6）
        ├── patterns.py（パターン検出・αブレンド）
        │     └── score.py（サブスコア統合・stage3_scores/店舗プロファイル管理）
        └── output/（機能A可視化 / 機能B-詳細 / 機能B-簡潔）
```

---

## 目的と位置づけ

**最終ゴール**: 「設定配分パターンが明瞭(読みやすい)かつ稼働(抽選人数)が低い」店舗・エリアを発掘する。

個別台の設定推測(Stage3のLogOdds)は目的そのものではなく、店舗単位パターン検出への**中間的なシグナル**として使う位置づけ。

---

## preprocess.py

### データの実態(確認済み制約)

| 制約 | 内容 |
|---|---|
| ART列は店舗によって使われ方が異なる | 多くの店舗ではART列は全行NULL(AT/CZ機の当りもBB/RB列に一括格納)だが、店舗によっては他店でBB/BB確率に入るデータがART/ART確率列に入っている(データ提供元のページ仕様が店舗ごとに異なるため。2026-07、エスパス日拓新宿歌舞伎町店で確認)。Stage1aの`resolve_rate_columns`が店舗ごとに実データで検証し自動吸収する |
| AT機の理論値不一致 | BB/RB確率が公開設定差表と一致しない機種あり(例: ヴァルヴレイヴ2のRBは逆相関) |
| 号機(実機固有ID)は取得不可 | 台同一性キー = (機種名, 台番号) |
| 欠損行の形式 | 識別子は実値・計測値NULL・判定不能フラグ列あり |

### Stage 0: データ正規化

- TEXT→数値変換(カンマ除去・±符号対応)
- 日付・台番号・機種名でユニーク化
- 1行 = 1台×1日のサンプル単位

### Stage 1a: 機種別Tierキャリブレーション(機種×データ提供元で共通)

BB/RB/ART列を3段階に分類し、信頼性に応じた使い方を決める。

| Tier | 条件 | 使い方 |
|---|---|---|
| A | 観測確率が公開設定差表と一致 | 理論値 $p_s$ をそのままベイズ尤度に使用 |
| B | 理論値未対応だが差枚率との相関 \|r\|>0.5 | 絶対確率では使わず、同機種内でパーセンタイル化して相対スコア化 |
| C | 理論値とも差枚率とも無関係 | 切り捨て、他指標で代替 |

**実証済み判定例**(回転数≥1500サンプル):

| 機種 | BB↔差枚率 | RB↔差枚率 | 判定 |
|---|---|---|---|
| マイジャグラーV | — | — | Tier A(BB=BIG, RB=REGそのまま) |
| 東京喰種 | r=0.96 | r=0.41 | Tier B、BB+RB合成で使用 |
| ヴァルヴレイヴ2 | r=0.95 | **r=-0.06(逆相関)** | BB=Tier B、**RB=Tier C(切り捨て)** |
| 戦国乙女4 | r=0.82 | r=0.89 | Tier B、合成確率(相関0.905)で使用 |

**店舗ごとの列マッピング自己検出**(`resolve_rate_columns`、2026-07追加):
- specsのTierラベル(BB/RB)は機種の性質を表すが、**どの物理列(BB確率/RB確率/ART確率)がそのチャンネルの実データを持つかは店舗ごとに異なりうる**(データ提供元サイトのページ仕様が店舗ごとに異なるため)
- `calibrate_all`は機種ごとに、BB確率/RB確率/ART確率の3列それぞれと差枚率の相関を**その店舗の実データで**計算し、最も強く相関する列(|r|>0.5、サンプル数30以上)をTier Bチャンネルの参照列として採用する(`column_map`)。同一列を'BB'・'RB'で重複採用はしない
- specsのラベルに対応する列(BB確率など)がこの店舗でたまたま欠損していても、実データが別列(ART確率など)にあれば自動的にそちらを参照する。どの列も相関が閾値未満ならそのチャンネルは寄与0として扱う(判定不能の代入はしない)
- `compute_all_logLR`にはこの`column_map`を渡し、Tier B分岐で決め打ちの列名の代わりに使用する

**152機種への一括Tier判定**(`fase2/assign_tier.py`、2026-07時点):
- ノーマルタイプ(BIG/REG方式、AT/CZ要素なし)→ 相関計算せず無条件でTier A(AT/CZによる理論値ズレ要因が無いため。マイジャグラーVと同じ扱い)
- それ以外(AT/CZ機)→ 上記と同じ相関ロジック(|r|>0.5)を全店舗データで実行しB/Cを自動判定
- 判定結果: BB Tier(A=11 / B=132 / C=9)、RB Tier(同上)
- 生データは`fase2/raw_specs_scraped.json`(chonborista.comのスクレイピング結果)に保持。列名→BB/RB/payoutの対応は各機種の`source_columns`で確認可能

**「ノーマルタイプ」判定はBIG/REG列名だけでは不十分(2026-07判明・修正済み)**:
- `is_pure_bonus`(BIG/REG列のみの確率表)だけでTier Aを確定させていたところ、沖ドキ!シリーズ・
  ドッチ・ディスクアップULTRAREMIX等、BIG/REG列名の確率表を公開しているにも関わらず実際は
  AT/スマスロ機である機種が誤って無条件Tier A(理論値をそのまま使用)になっていた
- 実害: これらの機種の観測BB/RB確率が理論値と大きくズレ、logLR_rngが異常値(最大+86)に
  達し、sigmoid飽和(high_prob=1.0固定)を起こしてS_全台系等の下流サブスコアを歪めていた
  (全店舗で104件の統計的外れ値のうち95件がこれらの機種に集中)
- 修正: `scrape_machine_specs.py`がchonborista.comの「機種概要」テーブルの「仕様」欄
  (例: `ノーマル`/`ノーマルタイプ`/`AT機`/`AT機(スマスロ)`)を`machine_type`として追加取得し、
  `assign_tier.py`の`is_confirmed_normal_type`が「仕様」欄が`ノーマル`/`Aタイプ`で始まる
  場合のみTier A確定を許可するようにした(それ以外は列名が一致していても相関ベースの
  Tier B/C判定にフォールバックする)
- 修正後もキングハナハナ-30・マイジャグラーVの一部の行に残った異常(9件)は、Tier判定とは
  別の原因(サイト側の個別データ表記ミス)であり、上記Stage4の`mark_rng_anomaly`で対応する

**回転数の機械的バイアス補正**(AT中除外問題):
- AT機はAT中の回転数がデータに含まれないため、当りが多い台ほど記録上の回転数が実態より少なくなる
- 同一データ提供元なら店舗を跨いで再現する → 機種ごとに補正式を1回校正すれば全店舗で再利用可能

### Stage 1b: 店舗別行動成分(複数店舗データ前提、`multi_store.py`で実装済み)

- 繁盛度・客層効果による回転数チャンネルの判別力の強さは店舗ごとに異なる
- 単店データのみでは学習不可。Stage 5の階層モデルで複数店舗データから自動学習

**実装(2026-07、`multi_store.learn_bin_curves`)**:
- チャンネル③の入力`kaiten_zscore`(台内=同一ホール名×機種名×台番号の回転数zスコア、`preprocess.compute_kaiten_zscore`。履歴5日未満はNaN)を全店舗データでプールし、機種ごとにデシル分けして「そのデシルの平均logLR_rng(RNGベースの疑似正解y1) − 機種全体平均」を実測カーブとして学習する
- 標本数200件未満の機種はカーブを持たない(＝該当機種はStage2でlogLR_kaiten=0.0のまま)
- 学習結果は`kaiten_bin_curves.json`に保存され、Stage1a(Tier判定)と同じ「1回学習・全店舗再利用」方式で`preprocess.compute_all_logLR`が自動読み込みする(単店実行の`app_a.py`/`app_b.py`は無改修で恩恵を受ける)

**⚠ 循環学習対策(2026-07再設計)**: カーブの教師がy1(logLR_rng)であるため、素のカーブ値をStage3で`w1×y1`に加算するとRNG証拠の**二重計上**になる。このため現在は:
- **w3の既定値は0.0(チャンネル無効)**。Stage5のLOSO交差検証(`validate_kaiten_channel`)に合格した場合のみ正のw3が保存される
- 有効化時もカーブ値そのままではなく、**直交化残差**(カーブ値 − (orth_a + orth_b×y1)、y1で説明できる成分を除いた「回転数だけが持つ独立成分」)をチャンネル③として使う(`compute_logLR_kaiten_column`のorth_a/orth_b引数)

### Stage 2: 証拠チャンネルごとのlogLR計算

3チャンネルを**個別に**計算する。

| チャンネル | 内容 | 計算方法 |
|---|---|---|
| ① RNG確率 | Tier A/Bと判定された列 | ポアソン近似: $\ln L(s) = k\ln(np_s) - np_s$ |
| ② 差枚(出玉) | 分散補正済み | 正規近似 **per-game スケール**: $\text{logLR} = \frac{\bar{x} \cdot \mu_s - \mu_s^2/2}{\sigma^2}$（$\bar{x}=\text{差枚}/n$）、σは機種別校正 |
| ③ 回転数(行動) | Stage1a/1b補正済み | 台内zスコアをデシル分けし、実測logLR曲線(非線形・非対称)の**直交化残差**を使用。**既定は無効(w3=0.0)**で、Stage5のLOSO交差検証合格時のみ有効化 |

**チャンネル②のμ_s推定(2026-07更新)**:
- `machine_setting_specs.json` に理論機械割(payout)がある機種(2026-07時点で153機種)は、上位半分の設定の平均payoutから理論期待差枚/G($\mu_s = (\overline{\text{payout}_{\text{高設定}}} - 1) \times 3$、3=標準ベット枚数)を**そのまま**使用する
- 無い機種は従来通り実測差枚率の75パーセンタイルを経験的に使用(30サンプル以上必要)
- 店舗単位の一律補正(κ)は**入れていない**。実データ検証の結果、店舗全体の実測平均差枚が理論値より低いのは「低設定運用が多い」という設定配分の反映であり、個々の台が高設定だった場合の期待値のズレではないため。一律補正をかけると高設定検出力そのものを削ぐことが判明した(詳細: `preprocess.compute_all_logLR`のコメント参照)

回転数の**2つの役割**を区別する:
- 役割①: サンプルサイズ(回転数が多いほどチャンネル①の尤度がシャープになる) → チャンネル①の n に内包
- 役割②: 独立証拠(客が示唆を見て回すという行動の証拠) → チャンネル③として独立集計

> セクション7の稼働推定(店全体の回転数集計)とは目的が異なる点に注意。同じ「回転数」カラムから異なる意味の指標を導出している。

### Stage 3: 統合スコア化

$$\text{LogOdds}(\text{高設定}) = \beta_0 + w_1 \cdot \text{logLR}_1 + w_2 \cdot \text{logLR}_2 + w_3 \cdot \text{logLR}_3$$

- 出力: `sigmoid(LogOdds)` → 高設定確率(0〜1)。または LogOdds そのもの
- 設定3欠番など機種仕様の差にも自然に対応(1〜6の個別事後分布不要)
- 単店段階では $w_3$ は実測相関の強さをそのまま使用。最終的な重みはStage5で学習
- **実装(2026-07)**: `preprocess.compute_log_odds`はw1/w2/w3を明示指定しない場合、`stage3_channel_weights.json`(Stage5が学習・保存)を自動読み込みする。未学習時は既定値(w1=1.0, w2=0.5, w3=1.0)にフォールバックするため、既存の呼び出し側(`app_a.py`/`app_b.py`)は無改修のまま学習結果を反映できる

**事前確率切片β₀(2026-07-07導入)**:

- $\beta_0 = \ln(\pi/(1-\pi))$。$\pi$=「証拠ゼロの台日が高設定である確率」(店舗の高設定投入率の事前推定)で、`stage3_channel_weights.json`の`prior_high_ratio`(既定0.15=`preprocess.DEFAULT_PRIOR_HIGH_RATIO`、業界感覚に基づく暫定値)から読み込む
- 旧実装はβ₀なし=暗黙にπ=0.5で、証拠ゼロの行がhigh_prob=0.5になり、店舗集計E[高設定台数]/Nが0.5近辺の狭帯域に集中する原因だった(上限キャリブレーション過剰発動(発動日割合3.8%〜82.7%)の根本原因)
- β₀は全行一律のため**店舗内の台のランキング(log_oddsの順序)は不変**。変わるのはhigh_probの絶対水準(確率としてのキャリブレーション)で、店舗横断の集計(E[高設定台数]/N・上限モデル・S_全台系の絶対値)が意味を持つようになる
- あわせて`patterns._compute_event_scores`(S_新台増台/S_移動台)の基準値フォールバック0.5を廃止(基準値が計算不能な店舗×機種は「検出不可」として除外。β₀導入後は0.5が「中立」を意味しないため)
- `multi_store._save_channel_weights`はStage5の学習結果を保存する際、学習対象外の`prior_high_ratio`を既存ファイルから保全する(学習再実行で消えない)
- 将来: πの店舗別学習は上限モデルStep2(Kalman)と統合して検討([`今後の実装予定.md`](今後の実装予定.md)2.1節)

**チャンネル間スケール差の注意点（実装メモ）**

差枚チャンネル②は per-game 正規化済みだが、設定間の識別力は RNG チャンネル①より本質的に高い（実測で 5〜10 倍程度）。そのため暫定的に $w_2 = 0.5$ に抑えている。Stage5 の重み学習後に適切な値に更新すること。

また、チャンネル①は O(n_daily) — 1日の回転数に比例してわずかに増加 — なのに対し、チャンネル②は per-game 正規化で O(1) となる非対称性が残る。将来的に完全統一するなら①も n で割る選択肢がある。

> **このStage3の出力(LogOdds/高設定確率)が、patterns.pyのパターン検出・サブスコアへの入力データになる**

### Stage 4: 信頼度しきい値・判定保留

- 判定不能条件: 各指標の期待発生回数が目安5回未満(= 回転数が少なすぎる台日)
- 判定不能日は**低設定・中間値などの代入を行わず単純除外**する(虚構の値を作らない)

**RNGチャンネル異常値検出(`mark_rng_anomaly`、2026-07追加)**

データ提供元(ana-slo.com)側の表記ミスにより、Tier A機種のBB/RB確率が設定6(理論上の
最良ケース)を大幅に超える行がまれに存在する(実例: マルハン新宿東宝ビル店キングハナハナ-30
774番、回転数8922でBB=666=観測確率0.075。理論値は設定6でも0.0043で17倍の乖離)。

- 設定6理論確率を仮定した片側Poisson検定(`scipy.stats.poisson.sf`)で、観測回数が
  統計的に説明不能なほど過剰(p < `RNG_ANOMALY_ALPHA`=1e-6)な行を検出し、Stage4の
  判定不能と同じ「代入せず単純除外」方針で`is_invalid`に統合する
- alpha=1e-6の根拠: 全店舗のTier A行×チャンネル検定数(約10万件)に対するBonferroni
  補正後の有意水準(≈5×10⁻⁷)に近く、実データで確認したところ閾値をこの前後で動かしても
  除外される行の集合が変わらない(境界にp値の空白地帯が存在する)
- **理論値へのクリップ(Winsorize)は行わない**: 代入は虚構の値を作ることになり、
  判定不能の扱い方針(低設定・中間値を代入しない)と矛盾するため
- 発見の経緯: `assign_tier.py`が「BIG/REG列のみの確率表→無条件Tier A」としていたが、
  沖ドキ!シリーズ・ドッチ・ディスクアップULTRAREMIX等はBIG/REG列名の確率表を持つ
  AT/スマスロ機であり、これらはTier誤判定(後述)が根本原因だった。Tier判定を修正した後も
  キングハナハナ-30・マイジャグラーVの一部の行にサイト側の表記ミスとみられる異常が残り、
  この検定で対応する

**Stage 4-1: 欠損偏りガード**

判定不能が低稼働日に偏って発生するリスクに対応するガード機構。

```python
# 各検定の2群(候補日群/非候補日群)それぞれで算出
判定不能率 = 判定不能日数 / 全カレンダー日数

# 両群の判定不能率の差が閾値(仮: 10〜15pt)を超える場合
if abs(率_候補群 - 率_非候補群) > 閾値:
    → 「欠損偏りあり」フラグを立て、信頼度を下げる
    # 大幅超過の場合は検定自体をスキップ → 「検出不可」扱い
```

閾値の具体値は実データ運用後に調整(保留事項)。

### Stage 5: 複数店舗データでの精緻化(`multi_store.fit_hierarchical_model`、2026-07再設計)

階層モデルで店舗をランダム効果として扱い、重みを自動学習する。

$$\text{LogOdds}_i = \beta_0 + \beta_1 y_{1i} + \beta_2 y_{2i} + (\beta_3 + \gamma_{\text{store}(i)}) y_{3i}$$

- $\gamma_{\text{store}}$: 店舗固有補正項。繁盛度・客層・イベント文化の強さを代理表現
- $\gamma_{\text{store}}$ 自体がstage3スコアの精度向上と店舗プロファイル(セクション9)の特徴量として再利用される

**⚠ 旧実装の欠陥(2026-07判明・再設計)**: 旧実装は「y1を教師に学習したカーブ(y3)をy1に回帰」しており、デシル平均は二乗誤差最小の予測子なので**傾きが構成上ほぼ1になる循環学習**だった(実測β3≈0.985はこの理論値通りで、何も学習していなかった)。γ_storeもプールカーブのフィット残差=ノイズ主体だった。

**現実装(LOSO交差検証+直交化+合格ゲート、`validate_kaiten_channel`)**:
1. 店舗sを除いた残り店舗でbin_curvesと直交化パラメータを学習し、sにout-of-sample適用
2. y3をy1で直交化した残差 $y_3^{\perp}$ について、**同一台内で当日の$y_3^{\perp}$が翌観測日のy1を予測するか**を相関で検証(同日y1との相関は「出ている台に客が座り続ける」逆因果で汚染されるため、設定の据え置き傾向を介して翌日に伝わる成分を教師にする)
3. 合格条件: プール相関>0 かつ p<0.05 かつ 相関が正の店舗が70%以上 かつ 2店舗以上で評価可能
4. **合格時のみ** w3=プール回帰傾き(0〜1クリップ)、γ_store=店舗別傾き−プール傾き。**不合格時は w3=0.0(チャンネル無効)・γ_store=0.0**
5. 本番適用用のカーブ・直交化係数(orth_a/orth_b)は全店舗プールで学習して保存(検証はLOSO、適用はプールという分担。新規店舗にも適用するため)

**スケーラビリティ**:
- Stage1a(Tier判定・機械バイアス補正)は機種×データ提供元で1回校正 → 全店舗で再利用
- 新規店舗追加で個別学習が必要なのは $\gamma_{\text{store}}$ のみ

- 学習結果は`stage3_channel_weights.json`(`w1`/`w2`/`w3`/`orth_a`/`orth_b`)・`store_profile.gamma_store`(店舗ごと、分析DB`ホールデータ/analysis.db`)に保存される。`python multi_store.py`で全店舗一括実行(検証レポートも表示される)

### Stage 6: 検証戦略(教師ラベルなし。`multi_store.py`で実装済み)

1. **店舗間Tier再現性チェック**(`check_tier_reproducibility`): 同一機種が複数店舗にあれば、`judge_tier`の再判定結果とkaiten_zscore↔logLR_rngの店舗内相関の符号が店舗を跨いで一致するか確認(各店舗30行未満は対象外)
2. **マクロ整合性チェック**(`check_macro_consistency`): 各店の全日・全機種の差枚合計÷回転数合計(実測差枚率)と、店舗平均high_prob(事後スコア集計値)のスピアマン順位相関を確認(3店舗以上必要)。2026-07時点の実データでは相関0.7(整合)
3. **店舗群分割クロスバリデーション**: 学習用店舗群でキャリブレーション・重みを決め、検証用店舗群で予測力(差枚との相関)を確認(未実装。店舗数が増えた段階での追加検討事項)

---

## patterns.py

### 台同一性・移動/撤去/増台の判定ロジック

```python
# 店舗×機種名で D-1 と D を比較 (K=0日前日比較)
消失台番号 = {D-1に存在} - {Dに存在}
新規台番号 = {Dに存在} - {D-1に存在}
N = min(len(消失台番号), len(新規台番号))
移動 = N件  # S_移動台の対象
撤去 = 消失台番号[N:]
増台 = 新規台番号[N:]  # S_新台増台の対象
```

- 号機なし前提のため「どの台がどこへ移動したか」の1対1対応は不可。件数ベースでまとめて移動扱い
- 入替作業が複数日に分散した場合、撤去と増台が誤分離される可能性あり(現状許容、K拡張で緩和可)

### パターン検出: 幅(breadth)型

| パターン | データ | 検出時期 |
|---|---|---|
| 全台系 | Stage3スコアの当日横断集計(同機種/列/島) | 1日分でも検出可 |
| 新台/増台 | 増台後・直近移動平均 - 店舗内同機種全体平均 | 増台直後から自動フェード |
| 移動台 | 移動後・直近移動平均 - 店舗内同機種全体平均 | 移動判定ロジックのトリガー後 |

```python
# 新台増台・移動台の共通構造
短期トレンド = Stage3スコアの直近N件移動平均
基準値 = 設置/移動以降の累積平均 or 同機種店舗全体平均(Stage3スコア基準)
raw = 短期トレンド - 基準値
S_新台増台 or S_移動台 = np.clip(raw / 0.5, -1.0, 1.0)  # 符号付き(2026-07変更)
```

**符号付き化(機能B再設計Phase2.1、2026-07実装済み)**: 旧実装は`max(0, raw)`でクリップし
「強さ」のみを表現していたが、`max(0, ...)`を外して`np.clip(raw/0.5, -1, 1)`に変更。新台・移動台
がむしろ基準値以下に沈んでいる(＝弱い)ケースを負値で表現できるようにした。

### 機種強さ軸・全台系/高配分の日次判定(`score_zentaikei_judgment`、2026-07-09設計合意・Phase1実装済み)

`S_全台系`(平均×(1−std/0.5))は「揃い」を要求するため、機種内半分だけ高い日(1/2全台系相当)はstdが
大きくスコアが逆に下がり、全台系と鉄板台の谷間に落ちる問題があった(今後の実装予定.md 1.8節ギャップ2)。
機種×日×ホール粒度でzスコア・投入率・S_全台系の3指標を役割分担させて解決する。

```python
z = (Σhigh_prob - n×π) / √(n×π×(1-π))   # 異常検定(投入量が期待から何σ上振れか)
投入率 = Σhigh_prob ÷ n                  # 規模の推定(全台系≈1.0、1/2≈0.5)
S_全台系 = 既存score_zentaiki            # 揃いの確認(全台か部分かの形状判別)
```

- n=機種内有効台数(is_invalid除外)、π=`preprocess.load_channel_weights()['prior_high_ratio']`(Stage3のβ₀と同じ値を使う。ハードコードしない)
- Σhigh_probは較正済み事後確率の和＝期待高設定台数として意味を持つ(Stage3のβ₀導入の帰結)
- 判定ラベル(3値、`JUDGMENT_LABEL_*`定数): `z`が`JUDGMENT_Z_THRESHOLD`(=2.0)以上**かつ**同日×同ホール内の
  全機種で`benjamini_hochberg`によるFDR補正を通過した場合のみ「異常」とし、`S_全台系`が
  `JUDGMENT_ZENTAIKEI_S_THRESHOLD`(=0.5)以上なら**全台系**、未満(またはNaN=機種内有効台数<2で算出不能)
  なら**高配分**とする。異常でない場合は**普段どおり**(S_全台系は見ない)
- 投入率は生の比率(0〜1)で返す(表示側の×6換算は呼び出し元(機能A)の責務。3節の正解発表ラベルの
  投入率=k/nと直接比較できる粒度で保存するため)
- 閾値は固定値でPhase1開始→`machine_judgment_log`(下記データモデル参照)への蓄積後、実測(将来は3節の
  正解発表ラベル)と突き合わせてPhase2で調整する(wᵢ学習=1.6節と同じ「記録側先行」パターン)

### 末尾版: 台番号末尾グループのカレンダー構造検出(`build_group_calendar_conditions`、2026-07-10設計・実装済み)

機種強さ軸と同じ3役分担の機械を、グループ定義を「機種」から「台番号末尾(店舗内全台横断)」に
差し替えて適用する(今後の実装予定.md 1.8節「末尾版」)。発端は「マルハン新宿東宝・エスパス歌舞伎町は
6のつく日に台番号末尾6が高配分」というユーザードメイン知識で、フェーズ1(検証スクリプト)で実データ
答え合わせを行ってから実装した。

**フェーズ1検証で判明した重要な事実**: 当初設計のStouffer統合z(`z=(Σp−nπ)/√(nπ(1−π))`を
`Σz÷√k`で統合)は、店舗全体の平均high_probがπより系統的に高い(sigmoid飽和の右裾。中央値は
π付近だが平均は台単位で0.24〜0.26まで押し上がる)ため、実データで11グループ×日付条件の
**全198仮説が有意化して閾値として破綻**した。既存`calendar_test`(鉄板台のカレンダー検定)と
同じ「候補日 vs 対照日」の相対検定に変更することで店舗オフセットが自動的に相殺されると判明し
(3案を実データ比較し、A案=Mann-Whitney Uが偽仮説(「末尾6×金曜」等の部分一致漏れ込み)を
効果量ゲートで唯一抑制できたため採用)、以下は変更後の確定仕様。

```python
グループ = tail_digit_group(台番号)   # 末尾0〜9 + ゾロ目(全桁同一: 11,22,…,99,111,…)の11区分
投入率(グループ×日) = Σhigh_prob ÷ n  # is_invalid除外

# レイヤー2本体: グループ×日付条件ごとに「候補日 vs 対照日」のMann-Whitney U片側検定(greater)
group_calendar_test(df, hole_name, group_series)
  → 全49候補(calendar_candidates)×11グループ = 539仮説の(該当日数,対照日数,p_raw,効果量)

# 一致ルール2本: 固定グループでは表現できない動的な対応(日によって「一致する台」が変わる)を
# 日ごとの「一致 vs 非一致」投入率差の対応ペアに対するWilcoxon符号順位検定(片側greater)で検定
match_rule_test(df, hole_name, rule='下2桁一致')  # 日付の日(1〜31)==台番号下2桁(unit%100)
match_rule_test(df, hole_name, rule='末尾一致')    # 日付の日の末尾(day%10)==台番号末尾(unit%10)

# 統合エントリポイント: 上記541仮説(539+2)を1つの仮説群としてBH補正+効果量ゲート(rbc>=0.3)
build_group_calendar_conditions(df, hole_name, group_series)
```

- グループ定義は関数注入方式(`group_series`を外部から渡す)。「グループ=機種」等への差し替え
  (今後の実装予定.md 1.8節「機種単位の癖分析」)でも検出ロジックは共通で再利用できる
- 最低該当日数: 候補日・対照日ともに5日未満の組み合わせは検定対象外(品質ガード兼・仮説数の
  自動削減。毎月X日条件は月1回のため5ヶ月分の蓄積が必要な点は許容済み)
- `calendar_candidates`は本節の実装で17候補→**49候補**(曜日7+末尾10+ゾロ目1+毎月X日31)へ
  拡張済み。既存`calendar_test`(鉄板台検定)も同じ関数を共用するため自動的に49候補で動作する
  (鉄板台の検出結果が従来と変わり得る点はユーザー許容済み)
- **重複条件の統合(max効果量)は保存時に行わない**: 「毎月6日⊂末尾6⊂一致ルール」のように条件が
  入れ子になるが、`group_calendar_conditions`テーブルには全条件をそのまま保存し、
  重複統合は予測時(下記`predict_tail_group_next_day`)にのみ適用する(検出は緩く保存・
  使用側でゲートの方針)
- **実データ検証結果(2026-07-10、マルハン新宿東宝・エスパス歌舞伎町)**: 本命「末尾6×6の日」が
  両店舗で541仮説中1位(rbc 0.98〜1.0)で検出。副産物として**ゾロ目台×ゾロ目の日(11日・22日)**
  (rbc 0.76〜0.78、ユーザー確認済み「末尾6と同等の鉄板」)、一致ルール2本も両店舗で有意
  (p<0.0001)を確認

**翌日予測(`predict_tail_group_next_day`、並走記録のみ)**: 台番号が属する末尾グループの
固定グループ×日付条件、および一致ルール(unit依存の動的判定)の両方について、翌日が該当する
有意条件を集め、**max(効果量)を採用**(重複統合。加算しない=二重計上回避)。`run_store_profile.py`
の`_run_tail_group_predictions`が予測種別`'S_末尾'`で`prediction_log`へappend-only記録する
(既存`write_prediction_log`の重複ガードを流用)。S_鉄板台等と異なり長期/短期のブレンドは行わない
(検出窓は全期間で開始する設計のため、使用alpha=0.0固定・長期スコア=ブレンド値として記録)。
**合成スコア(狙い目度)へは混ぜない**。`prediction_accuracy`で的中実績を確認してから合流を判断する
(遷移予測と同じ運用)。

### 機種版: 看板機種＋機種カレンダー癖の検出(`machine_group`/`group_constant_test`、2026-07-10設計・実装済み)

末尾版と同じ検出器(`group_calendar_test`/`build_group_calendar_conditions`)を、グループ定義
「機種」で再利用する(今後の実装予定.md 1.8節「機種単位の癖分析」)。対象は看板機種(恒常的に
強い機種)＋機種カレンダー癖(特定日にこの機種が強い)の2つ。新台/増台/再導入/移動台の
「導入後の扱い」は別軸の検討として今後の実装予定.mdに設計メモのみ残し、未実装。

```python
グループ = machine_group(df)  # 機種名そのまま(接頭辞なし)。日次有効台数の中央値が
                              # MACHINE_GROUP_MIN_UNITS(=2)未満の機種はNaN(検定対象外)

# 看板機種検定: 「自グループの投入率 - 同日の他グループ平均投入率」の対応ペアに対する
# Wilcoxon符号順位検定(片側greater)。日付条件='恒常'固定の1行を返す
group_constant_test(df, hole_name, group_series)

# 統合エントリポイント: group_calendar_test(機種カレンダー癖)+group_constant_test(看板機種)を
# 1つの仮説群としてBH補正(一致ルールは機種に意味がないため含めない)
build_group_calendar_conditions(
    df, hole_name, group_series, group_type='機種',
    include_match_rules=False, include_constant=True,
)
```

- **看板機種検定は末尾版の検定方式(Mann-Whitney U「候補日vs対照日」)をそのまま使わない**:
  「恒常的に強いか」は候補日/対照日の区分がなく毎日が該当するため、一致ルール
  (`match_rule_test`)と同じ「対応ペアのWilcoxon符号順位検定」を転用した。店舗全体の平均投入率が
  定数オフセットとして乗る問題(末尾版フェーズ1のStouffer破綻と同型)を、「自グループ vs
  同日の他グループ平均」の対応ペア化で回避する(2026-07ユーザー合意: 案A)
- **2窓検定並走(2026-07ユーザー合意: 案A)**: 恒常窓(全期間)と直近`RECENT_TEST_WINDOW_DAYS`
  (=90)日窓の2本を独立にBH補正して`group_calendar_conditions`へ保存する
  (グループ種別`'機種'`=恒常窓・`'機種_直近'`=直近窓)。個別台側の鉄板台等が採用する
  「検定は全期間1本+予測時に長期/短期αブレンド」とは異なる方式で、機種の癖は個別台より
  非定常(推し機種の月替わり)なリスクが高いと判断し、条件自体が消えたことを表現できる
  2窓検定を選んだ(実証後に個別台側へ展開するかは別途判断。今後の実装予定.md参照)。
  直近90日窓では毎月X日条件がほぼ検定不能(該当日数<5)になるが、これは想定内の挙動
  (既存の最低5日/5日ゲートがそのまま吸収する)
- **n≥2ゲート(2026-07ユーザー合意)**: 検定対象は日次有効台数の中央値2台以上の機種のみ
  (`MACHINE_GROUP_MIN_UNITS`)。2〜3台構成のローテは機種集計の恩恵が薄いため対象外とし、
  1.2節の台粒度ローテに任せる。「検出は緩く保存・使用側でゲート」の方針どおり、機種台数中央値は
  `group_calendar_conditions.台数中央値`列に保存し、表示/予測側の追加ゲート(暫定n≥3等)は
  この値を見て別途判断する(末尾版にはこの列は不要なためNULLのまま)
- **実データ検証結果(2026-07-10、マルハン新宿東宝・bigディッパー東中野)**: マルハン
  (検定対象97機種、全期間窓4850仮説中9件有意・直近90日窓4250仮説中3件有意。看板機種として
  北斗の拳/沖ドキ!ゴージャス/いざ番長等8機種+末尾8のカレンダー癖1件を検出)、bigディッパー
  (検定対象36機種、全期間窓1800仮説中1件有意=「いざ！番長」の看板機種)。効果量(rbc)は
  0.3〜0.6の範囲に収まり、末尾版フェーズ1で見られたStouffer破綻(全仮説有意化)は再現しなかった
- **DELETEスコープの修正(2026-07)**: `write_group_calendar_conditions`は従来「ホール名のみ」で
  当該ホールの全グループ種別を無条件に削除していたため、機種版を書き込むと末尾版の既存行が
  巻き込まれて消えるバグがあった。呼び出し側が`group_types`引数で削除対象を明示する方式に変更
  (省略時はresult_dfのグループ種別列から推定するが、result_dfが空になり得る呼び出しでは
  推定不能になるため明示が必須)

**翌日予測(`predict_machine_group_next_day`、並走記録のみ)**: `predict_tail_group_next_day`の
機種版(一致ルールに相当する動的グループがないためその分岐は持たない)。機種が該当する有意条件
('恒常'固定行は常に該当、カレンダー条件は翌日該当時のみ)を集め、max(効果量)を採用(重複統合)。
`run_store_profile.py`の`_run_machine_group_predictions`が恒常窓・直近窓それぞれ独立に
予測種別`'S_機種'`/`'S_機種_直近'`で`prediction_log`へappend-only記録する(機種→台展開、
台単位で書くのは`evaluate_predictions.py`が実測差枚と台単位で突合するため)。長期/短期の
ブレンドは行わない(使用alpha=0.0固定)。**合成スコア(狙い目度)へは混ぜない**。

**今後の課題(今後の実装予定.md 1.8節「機種単位の癖分析」に集約)**: 新台/増台/再導入/移動台の
「導入後の扱い」(店舗単位で経過日数ビン別に検定する枠組み)は未実装。新台/増台/再導入の判別
(店舗初出日・不在期間からの再出現)、および「直近の扱い」を看板機種と同じ2窓検定方式にするかの
設計は2026-07にユーザーと合意済みだが実装は別セッション。

### パターン検出: 深さ(depth)型

深さ型は**履歴日数が増えるほど精度が上がる**。データ不足時は「検出不可」扱い(低スコアは出さない)。

#### 鉄板台の検出フロー(2経路)

**メイン経路: 未知周期の発見**

```
Step1: Stage3スコア系列を欠損込みの日次グリッドで保持
Step2: ACF(pairwise-complete版、欠損ペアを除外)で lag 1〜60日をスクリーニング
Step3: PDM(Phase Dispersion Minimization)で波形非依存の再確認
       (ACFは線形相関が前提のため矩形的な鉄板台パターンへの感度が弱い)
Step4: 判定不能率が高い台(欠損が多い場合) → Lomb-Scargle periodogramに切り替え
       (等間隔サンプリングの前提が崩れているため)
```

**別経路: 既知カレンダー条件の検定**

```
候補49カテゴリ(`patterns.calendar_candidates`。2026-07-10「末尾版」フェーズ2で17→49候補へ拡張):
  曜日:    月/火/水/木/金/土/日 (7候補)
  日付末尾: 0〜9 (10候補)
  ゾロ目:  該当/非該当 (1候補)
  毎月X日: 1〜31日 (31候補、末尾版レイヤー2との共用のため追加)

各候補で:
  1. 一方向(one-sided)の並べ替え検定 or Mann-Whitney U検定
  2. p値を Benjamini-Hochberg FDR補正 (候補49 × 対象台数)
  3. 補正後有意 かつ 効果量(中央値差/rank-biserial相関)が閾値以上 → 候補として残す
  4. Stage4-1の欠損偏りガードを必ず通過させる
```

> 候補拡張(17→49)に伴い鉄板台のカレンダー検出結果が従来と変わり得る点、仮説数増による
> BH検出力の微減はユーザー許容済み(今後の実装予定.md 1.8節「末尾版」参照)。

> **rank-biserial相関の符号に注意(2026-07バグ修正)**: `calendar_test`(patterns.py)は
> 一方向検定`alternative='greater'`(候補日が対照日より高いかを検定)のp値と、
> 効果量`rank-biserial`相関を組み合わせて有意判定するが、`rbc = 1.0 - 2.0*u_stat/(n1*n2)`
> という符号のまま実装すると、p値が検定通り有意でも「候補日が高い」ケースほど`rbc`が
> **負**になり、正の閾値`effect_size>=0.3`のゲートで常に弾かれてしまう。修正版は
> `rbc = 2.0*u_stat/(n1*n2) - 1.0`(候補日が対照日より高いほど正になる)。人工的な
> 強い曜日パターンで検証したところ、修正前はp=1.2e-17でも有意判定されなかったが
> 修正後は正しく有意判定された。修正前は全店舗でカレンダー有意が1件も検出されず、
> S_鉄板台が理論上の最大値1.0(メイン経路・カレンダー経路の両方一致)に到達できていなかった。

**スコアの付与方法(2026-07仕様変更: 該当日のみ・効果量ベース→同年再設計で符号付き化)**:

旧実装は検出台の**全日**に定数(両経路一致=1.0/片方=0.6)を付与していたが、「特定条件の日に入る」という鉄板台の性質と逆に、非該当日の狙い目度を押し上げて該当日のコントラストを消していたため、まず該当日のみ効果量ベースでスコア付与する方式に変更し、続く機能B再設計(Phase2.2、2026-07実装済み)で非該当日を「弱さ」として符号付き表現するよう再拡張した。

```
カレンダー経路: 有意候補(例: 末尾_7)に合致する日 → 効果量(rank-biserial、0〜1クリップ)を正のスコアに
               合致しない日(この機種にカレンダー経路の検出が1つ以上ある場合のみ)
               → -NEGATIVE_SCALE × 検出済み効果量の平均(負、NEGATIVE_SCALE=0.5暫定値)
周期経路:      確認済み周期の位相ビン(5ビン、pdm_confirmと同一)ごとの平均high_probを求め、
               全体平均を上回るビン(該当ビン) → (ビン平均−全体平均)/0.5 を正のスコアに
               それ以外のビン(非該当ビン) → -NEGATIVE_SCALE × 該当ビン効果量の平均(負)
               (位相は観測順インデックス基準。営業日が飛ぶと暦日とズレる既知の近似)
2経路統合:     符号付きnoisy-or(`_combine_signed`)。正の値同士は通常のnoisy-or、負の値同士は
               `-(1-(1-|a|)(1-|b|))`、符号が異なる場合は単純平均(両経路が同じ方向を指すほど
               絶対値が大きくなる。旧「両方一致→1.0」の連続版を符号付きに拡張したもの)
検出自体がない台: 全日NaN(synthesizeで除外・再正規化。判定不能を負値にすると新規台が
               不当に低評価になるため、Stage4-1と同じ方針で「検出不可」のまま維持する)
```

検出条件(経路/条件/効果量/周期経路は周期日数)は`score_teppandai(details_out=...)`で台単位に取り出せ、`run_store_profile.py`が分析DBの`teppan_conditions`テーブルに保存する(「明日は該当日か」の判断材料。トップページ表示・翌日予測(下記)で使用)。

#### S_鉄板台の翌日予測(Stage7-0〜7-2、機能B再設計Phase3、2026-07実装済み)

翌日フォーキャストが可能なのは**S_鉄板台**(カレンダー経路: 翌日の曜日・日付末尾は既知。周期経路: 位相を1つ進めれば翌日の位相ビンを予測できる)と**S_据え置き**(下記「据え置きの日次判定と翌日予測」参照)。S_全台系・S_新台増台・S_移動台は定義上「当日」の記述であり翌日への外挿ロジックを持たないため対象外。S_ローテは「検出済みパターンが翌日も継続する」という前提の定式化が未確定のため対象外(将来項目、`fase2/今後の実装予定.md`参照)。

```python
def predict_next_day(hp, lags, cal_conditions, next_date) -> float | None:
    """検出済み条件のみから次の観測日のスコアを予測。リーク禁止(実測値は使わない)"""
```

- `hp`(観測順history、is_invalidはNaN化済み)は`build_observed_history(df, hole_name, machine_name, unit)`で`score_teppandai`と同じ切り出しを再現して構築する(検出時と翌日投影で位相基準を揃えるため、`_phase_bin_effects`を両者から共有し二重実装を避ける)
- 長期版(全履歴hp)・短期版(直近`SHORT_WINDOW_DEFAULT`日窓のhp)を両方`predict_next_day`し、`FIXED_ALPHA`で`predict_next_day_with_blend`がブレンドする(生の長期/短期予測値も保存し、将来のウォークフォワードα学習に備える)
- `prediction_log`テーブル(analysis.db、**append-only**)に、`run_store_profile.py`実行のたびに検出済みの台ごとの予測を1行追記する。**リーク防止**: `使用データ最終日`は必ずその予測計算に使ったdfの実際の最大日付を取得して記録する(ハードコードやその場の`today()`は使わない)
- `evaluate_predictions.py`(新規、`python evaluate_predictions.py`で実行): `prediction_log`とレプリカ(`turso_replica.db`)の実測差枚を(対象日, ホール名, 機種名, 台番号)でJOINし、(ホール名, 予測種別)ごとにSpearman順位相関(最低サンプル数`MIN_SAMPLES=30`未満は`NULL`=検証中)・Precision@N(予測上位`TOP_N=3`台と実測上位N台の重複率)・リフト(予測上位N台平均実測差枚÷店舗全体平均実測差枚)を`prediction_accuracy`テーブルへ集計・UPSERTする。実測値は複製せず評価時にJOINで参照する(予測計算コード`patterns.py`には実測差枚を一切渡さない)

#### 遷移モデル(据え置き/上げ/下げ)による全台翌日予測(Stage7-3、2026-07-07実装)

設定運用は「据え置き」だけでなく「上げ」「下げ」を含むため、翌日予測の事前分布を店舗単位の2状態(高/低)マルコフ遷移として持つ。S_鉄板台の翌日予測(検出済みの台のみ)と異なり、**使用データ最終日に判定可能だった全台**が対象(全台カバレッジ)。

```
p_stay = P(高_t | 高_{t-1})   … 据え置き率(1 - p_stay が下げ率)
p_up   = P(高_t | 低_{t-1})   … 上げ率
翌日予測 = 当日high_prob × p_stay + (1 - 当日high_prob) × p_up
```

- **推定(v1: 無条件版)**: 真の設定ラベルは観測不能のため、連続した暦日ペア(同一ホール×機種×台番号、両日とも判定可能)の事後確率high_probのソフトカウントで推定(`patterns.estimate_transition_matrix`。暦日差1日でないペアは除外、最低`TRANSITION_MIN_PAIRS`=50ペア)。事後確率のノイズにより持続性を過小評価する既知のバイアスあり
- 長期(全履歴)・短期(直近`SHORT_WINDOW_DEFAULT`日)の両方で遷移行列を推定し、S_鉄板台予測と同じ規約(`FIXED_ALPHA`ブレンド・短期不可=α実質0)で`patterns.predict_transition_with_blend`がブレンドする
- `run_store_profile.py`の`_run_transition_predictions`が上限キャリブレーション補正後のhigh_probを入力に、予測種別`'遷移予測'`で`prediction_log`へ追記(append-only・リーク禁止はS_鉄板台予測と同一規約)。`evaluate_predictions.py`は(ホール名, 予測種別)で汎用集計するため無改修で精度計測に乗る
- `app_top.py`の翌日予測ランキングは`予測種別='S_鉄板台'`のみ表示(レンジ・意味の異なる2種の混在回避。遷移予測の表示は精度確認後に検討、[`今後の実装予定.md`](今後の実装予定.md)1.4節5項)

**実装時の実データ検証で判明した重要な事実(2026-07-07)**: 全6店舗で遷移行列がほぼ縮退(p_stay≈p_up≈店舗平均high_prob。例: yasuda7でp_stay=0.275/p_up=0.273)しており、**これらの店舗は日次でほぼ無記憶**である。

- 前日high_prob↔当日high_probの自己相関(連続暦日ペア、n=1.5万〜12.7万/店): pearson 0.007〜0.055
- 当日high_prob→**翌日**実測差枚のSpearman(全履歴): -0.021〜+0.005(ほぼゼロ。エスパス日拓新宿歌舞伎町店は-0.021, p=2.6e-14で有意な**負**=「出た台は翌日出ない」傾向)
- 同日Spearman 0.77〜0.84との対比: 同日相関はチャンネル②が当日差枚から計算されることによる自己相関が大半で、翌日への持ち越し情報はほぼ含まれない
- 含意: (a)無条件の持続性ベース予測はこれらの店舗ではほぼ機能しない(v1の予測値は台間でほぼ定数になる)、(b)翌日予測の信号は「特定日・周期」(S_鉄板台の方向)と条件付き遷移(前日差枚→下げ、カレンダー→上げ、ローテ連動)に求める必要がある(拡張候補は[`今後の実装予定.md`](今後の実装予定.md)1.5節)、(c)この縮退自体が誠実な出力であり、評価ハーネス上で条件付き拡張との比較基準(ヌルモデル)として機能する

**店舗の癖(据え/上げ/下げ)の`store_profile`保存(2026-07-08 タスク3追記(c)。記録のみ、表示UIは今後の実装予定)**:

- `estimate_transition_matrix`の戻り値に`pi`(ベース率π＝ペア集合のp_prev平均。ソフトカウントと同じ集合で定義)を追加した
- `run_store_profile.run_for_hole`が長期版行列(`matrix_long`)を`update_store_profile`へ渡し、`store_profile`に`遷移_ベース率`/`遷移_p_stay`/`遷移_p_up`/`遷移_ペア数`の4列を追加保存する(gamma_store等と同じ「店舗スカラーを全パターン行に複製」方式)。ペア数不足(`matrix_long`がNone)の店舗は4列ともNULL
- 下げ率(=1−p_stay)・引き継ぎ度λ(=p_stay−p_up)は導出値のため保存しない(表示側で計算する想定)。設計メモ: 店舗横断比較はλを使う(p_stay生値はベース率πに引きずられ店舗間比較不可。λはπの影響が打ち消される共通スケール[-1,1]で、+1寄り=据え置き型・0付近=毎日リセット型・負=逆張り型)。店舗個別表示は据え癖p_stay/上げ癖p_up/下げ率1−p_stayの3値に「πとの差」を添える設計。ソフトカウントの平滑化バイアスにより推定ノイズが大きい店ほどλが0側に縮むため、僅差の順位比較は過信しないこと

#### 遷移モデルの前日差枚条件付き拡張＋並走ログ基盤(Stage7-3拡張、2026-07-08 タスク4実装)

無条件版(v1)は上記の実データ検証で「全店舗が日次でほぼ無記憶」と判明し予測力がほぼ期待できないため、「出た台は翌日出ない」逆信号(エスパス日拓新宿歌舞伎町店で当日high_prob→翌日実測差枚のSpearman -0.021、p=2.6e-14)を層別遷移行列で拾う条件付き拡張を追加した。

```python
def estimate_transition_matrix_stratified(df, hole_name) -> dict | None:
    """前日(t-1)の実測差枚が店舗内上位2割か否かで層別した遷移行列(v2: 条件付き版)"""
```

- **ペア構築の共通化**: 無条件版・層別版とも暦日差1日ペアの構築ロジックが同一のため`_build_transition_pairs`に抽出し二重実装を避けた(p_prev/p_curr/前日の日付/前日の実測差枚を持つDataFrameを返す)。p_stay/p_up/piの推定式(ソフトカウント)も`_fit_transition_from_pairs`に共通化
- **層の定義**: ペア(t-1→t)の前日(t-1)の実測差枚が、その店舗×その日(t-1)の上位`STRAT_QUANTILE`(暫定0.8)分位点以上か否かの2層(`stratify_threshold_by_date`で店舗×日の分位点を算出。閾値が店舗内の日次分位点のため店舗規模に自動適応。ロジスティック変調は不採用と決定済み)。差枚列(`slot_data`由来、preprocessでは加工されず素通りするため`scored`にも残る)が無い/全欠損の店舗は層別不可(None)
- **層ごとの最低ペア数**: `TRANSITION_MIN_PAIRS`(=50、無条件版と共通)未満の層があれば層別不可(None)
- **店舗別有効化(並べ替え検定)**: ソフトカウント由来のp_stay/p_upは通常の比率検定(z検定等)の前提(独立二項分布)に従わないため、層ラベルをシャッフルした帰無分布と観測差を比較する並べ替え検定(`_stratified_permutation_test`、`STRAT_PERMUTATION_ITERS`=1000回、有意水準`STRAT_SIGNIFICANCE_ALPHA`=0.05)を採用。統計量は`max(|Δp_stay|, |Δp_up|)`(p_stay・p_upのどちらか一方でも層間に有意差があれば層別に意味があるとみなす保守的な基準)。有意でない店舗は無条件版のみ(w3のLOSOゲート・1.6節wᵢ店舗別ゲートと同じ「信号なし店舗にノイズを持ち込まない」思想)
- **並走記録**: `run_store_profile._run_transition_predictions`が無条件版(予測種別`'遷移予測'`、全店舗・従来どおり)に加え、有意な店舗のみ条件付き版(予測種別`'遷移予測_前日差枚'`)を同じ実行・同じ台集合で追加記録する(DELETE/UPDATEなしの並走ログ)。条件付き版の予測は、その台の当日(使用データ最終日)の実測差枚が店舗内上位2割かで長期/短期それぞれの上位層・下位層行列を選び`predict_transition_with_blend`でブレンドする(条件に使う差枚は使用データ最終日の確定値のみのためリークではない)。層別短期行列が推定不可(ペア数不足)なら`predict_transition_with_blend`のブレンド規約どおり長期のみ(α実質0)
- **採否ゲート**: `evaluate_predictions.py`は(ホール名, 予測種別)ごと集計のため無改修で両種別を対比較できる(同一日・同一台集合でSpearman/Precision@Nを比較し、上回った場合のみ将来の表示採用を検討する運用)。`app_top.py`の翌日予測ランキングは`予測種別='S_鉄板台'`のみ表示のため新種別の混入なし
- **実データ検証(2026-07-08、9店舗)**: 層別が有意だったのはyasuda7(p=0.009)・グランパ中野(p=0.021)・マルハン新宿東宝ビル店(p=0.018)の3店舗。動機となったエスパス日拓新宿歌舞伎町店自体はこの層別検定ではp=0.134で非有意(層間のp_stay差は0.002程度と小さい)。**この並走ログの仕組みは以後の全拡張(カレンダー条件・ローテ連動・1.6/1.7節)の評価基盤として再利用する**ため、個々の店舗でゲート落ちしても投資は無駄にならない

#### ローテーション検出

鉄板台と同じ検定骨格(一方向検定+FDR補正+効果量ゲート+欠損偏りガード)を再利用。

```
Step1: 窓内(機種/列単位)で各台の高設定シェアの集中度指標を算出
       (エントロピー or ジニ係数)
Step2: 「ランダム配分」を帰無仮説とした並べ替え検定
       → 1台集中=鉄板台、全台均等=全台系、数台分散かつ有意=ローテーション
Step3: p値FDR補正 + 効果量閾値ゲート
Step4: Stage4-1の欠損偏りガード
```

#### 据え置きの日次判定と翌日予測(2026-07-08 タスク3実装)

旧`score_sueki`は台単位で全期間1定数(0〜1)を返しており、「日単位の該当/非該当」を表現できなかった。`patterns.score_sueki_daily`に差し替え、S_鉄板台と同じ符号規約([-1,1]・負=非該当日)の日次スコアにした(**案A「滑走窓自己相関」**。案B(CUSUM等の変化点検出)は段階投入の方針どおり今回は見送り)。

```python
def sueki_daily_r(hp: pd.Series) -> np.ndarray:
    """台ごとの直近K=14日窓のlag-1自己相関r_tを日次計算し、EWMA(span=7)で平滑化したr̄_tを返す"""
```

- 台ごとに直近`SUEKI_WINDOW`=14日窓でlag-1自己相関r_t を日次計算する。窓内の有効ペア(両日とも判定可能)が`SUEKI_MIN_PAIRS`=8未満の日はNaN(履歴不足)。10ではなく8にしたのは、14日窓(最大13ペア)にそのまま「10ペア」を適用すると窓の約8割充足が必要でNaN率が過大になるため(実測: 全8店舗・台×日約50万行で足切り8→NaN率20.5%/足切り10→30.7%)。1窓の相関のブレは後段のEWMA平滑がならす前提
- 窓内サンプルが少なくr_t自体がばらつくため`SUEKI_EWMA_SPAN`=7でEWMA平滑化する(`pandas.Series.ewm(span=7, min_periods=1).mean()`。先頭の連続NaN区間はNaNのまま、一度有効値が出た後の欠損日は直前の平滑値を保持する挙動)
- ベクトル化実装: 台ごとのpython/scipyループでpearsonrを都度呼ぶと約50万行規模で数十分オーダーの実行時間になる(実測: 50万回のscipy.stats.pearsonr呼び出しで約1650秒)。`pandas.Series.rolling(window=13, min_periods=8).corr()`はpairwise-complete(NaNペアはmin_periods判定から除外)なローリング相関をベクトル化して計算できるため、これを採用している
- **符号変換(`score_sueki_daily`)**: 平滑化後のr̄_tが`SUEKI_DAILY_THRESHOLD`(暫定0.2)以上の日を該当日(正スコア=+r̄_t)、それ未満の日を「切断」として非該当日とする。負スコアは`-min(1, (SUEKI_DAILY_THRESHOLD - r̄_t) / SUEKI_DAILY_THRESHOLD)`(S_鉄板台の`NEGATIVE_SCALE`と同じ「負側は控えめ」の思想を独自の式で表現)
- **既知の挙動(2026-07-08確認・現状維持と決定)**: 上記の負側の式はr̄_t=0で-1に飽和するため、全店舗が日次ほぼ無記憶(自己相関≈0、上記遷移モデルの検証参照)という実態のもとでは大半の台×日が-1近傍になり、店舗平均`s_sueki`は大きく負(例: yasuda7で約-0.73)・合成スコア(狙い目度)も下方向に引っ張られる。「負側は控えめ」の思想より強めに効いているが、`prediction_accuracy`の成績を見てから調整する方針(案B)をユーザーと合意済み(調整候補: 負側に`NEGATIVE_SCALE`=0.5を乗じて緩和)
- `compute_short_term_score`のS_据え置き分岐も同じ`score_sueki_daily`に差し替え済み(短期窓版)

**翌日予測(`run_store_profile._run_sueki_predictions`、`_run_teppan_predictions`の直後に実行)**:

```
翌日投影 = r̄_t ×(当日high_probの台基準からの偏差)
台基準 = その台のSUEKI_WINDOW(14)日窓内の平均high_prob
```

- 対象: 使用データ最終日にr̄_t(長期版)が計算できた(NaNでない)全台(検出条件に依存しないカバレッジという点はS_鉄板台と異なり遷移モデルに近い)
- 長期版(全履歴で計算したr̄_t)・短期版(直近`SHORT_WINDOW_DEFAULT`日窓で計算したr̄_t)を`patterns.predict_sueki_with_blend`が`FIXED_ALPHA`でブレンドする(短期不可=α実質0。S_鉄板台・遷移モデルと同じブレンド規約)
- `prediction_log`に予測種別`'S_据え置き'`で追記(append-only・リーク禁止は他の予測種別と同一規約)。`詳細`列に{r値, 切断フラグ, 当日偏差, 窓幅}を保存し、将来のwᵢ学習/GBDT融合(`今後の実装予定.md`1.6/1.7節)の特徴量として再利用できるようにしている
- `evaluate_predictions.py`は(ホール名, 予測種別)ごとの汎用集計のため無改修で`'S_据え置き'`も精度計測に乗る。`app_top.py`の翌日予測ランキング(個別台/機種タブ)は引き続き`予測種別='S_鉄板台'`のみ表示のため、新種別が混入することはない

**トップページ「据え」タブへの影響**: `S_据え置き`が「全期間1定数」→「当日断面の符号付き値」に変わったことに伴い、`app_top._SUEKI_SCORE_GATE`を0.5→0.0に変更した(該当日は必ず`SUEKI_DAILY_THRESHOLD`以上になる仕様のため、0超のフィルタで実質「最新日が据え置き該当日の台」を抽出する)。`app_b.py`は元々`range_y=[-1,1]`に対応済みのため無改修。

**店舗の「癖」指標の移管**: `store_profile`の`s_sueki`行スコア(=S_据え置き列の店舗平均)は、意味が「癖の強さ(0〜1定数の平均)」から「直近該当日の多寡(符号付き日次値の平均)」に変わった。店舗の据え置き癖を見る指標としての役割は、下記「遷移モデル」の`estimate_transition_matrix`が返す`pi`(ベース率)・`p_stay`・`p_up`(2状態マルコフ連鎖ではlag-1自己相関 r ≈ p_stay − p_up の関係にあり、癖とベース率を分離して保持できる上位互換)に譲る。

### 長期/短期αブレンド(Section6.3)

S_全台系・S_鉄板台・S_ローテ・S_据え置きの4種について、同一の検出ロジックを2系統で並行算出してブレンドする。

```python
# 同一の検出ロジックで入力範囲だけ変える
長期版サブスコア = 全履歴データを使った算出
短期版サブスコア = 直近Mウィンドウ(直近M日 or M件)のみを使った算出

# αは店舗・サブスコア種別ごとに個別学習
ブレンド済み = α × 短期版 + (1-α) × 長期版
```

**αの決め方(2026-07仕様変更: 固定値)**: 現在は固定α=0.3(`patterns.FIXED_ALPHA`)。

旧実装のウォークフォワード学習(`walk_forward_alpha`)は以下のリークがあり停止した:
- ターゲット(当日の店舗平均high_prob)に対し、特徴量のS_全台系等は**その当日のhigh_probから計算**されており、同日情報のリークで実質自己回帰だった(αが「未来の予測に効く比率」を表さない)
- `compute_short_term_score`は末尾M日窓を1回計算して貼るだけで各時点tの短期版になっておらず、α推定の実効サンプルが末尾のみだった

真のウォークフォワード(特徴=t時点までで計算、ターゲット=**t+1の実測差枚**)はStage7(予測精度の自己検証ループ)として機能B再設計とあわせて再実装する。`walk_forward_alpha`関数はその際の再利用に備えて残置(現在未使用)。

- 短期版でサンプル不足の場合はα=0扱い(長期版のみにフォールバック、`blend`の仕様)

> S_新台増台・S_移動台はこの構造を既に内包しているため対象外。S_稼働低さは店舗全体集計のため対象外。

### 店舗×日 高設定台数上限キャリブレーション(Step1実装済み・候補C)

> 2026-07にStep1(候補C)を実装。Step2(候補D・Kalman filter)は未着手、詳細は[`今後の実装予定.md`](今後の実装予定.md)参照。

「高設定割合が5割を超えることは稀」という構造的制約を店舗ごとにデータから動的推定し、Stage3出力の店舗全体集計が超過した場合にソフトに下方補正(shrinkage)する後処理。長期/短期αブレンド(直上)と同一の検出ロジック構造を応用している(`patterns.blend_scalar`。既存`blend()`は台×日の行単位Series専用のため、店舗×日の分位点集計値1本をブレンドする用途向けに新設)。

**候補比較**(データ増加での精度向上 / 月次変化への追従 / 実装コスト):

| 候補 | 概要 | 精度UP | 追従 | コスト |
|---|---|:---:|:---:|:---:|
| A. スライディングウィンドウ分位点 | 直近Mヶ月の分位点 | × | ○ | 低 |
| B. EWMA分位点 | 指数減衰で古いデータを減衰 | △ | △ | 低 |
| **C. 長期/短期αブレンド流用(Step1実装済み)** | 上記のブレンド構造をそのまま再利用 | ○ | △ | 低 |
| **D. Kalman filter(最終形・未着手)** | 真の上限を潜在状態として逐次推定 | ○ | ○ | 高 |

採用方針: 候補Dを最終形とし、単店運用中でも検証可能な候補Cを先行実装。multi_store.pyのKalman基盤構築後に候補Dへ移行。

**店舗全体での集計である点に注意**(2026-07の検討時に機種・島単位の集計と誤解した経緯があるため明記):

```python
E[高設定台数] = Σ high_prob(台)   # 機種・島単位ではなく店舗全体で集計
N = 稼働台数                      # その日の店舗全体の稼働台数
上限 = min(絶対上限0.5, α×短期分位点 + (1-α)×長期分位点 + 安全マージン)
if E[高設定台数] / N > 上限:
    offset = 二分探索でE[]/Nが上限相当に収まるようLogOddsに一律適用
```

1つの機種に投入が集中しても、店舗全体の投入率が上限内であれば発動しない(機種単位の集中投入とは無関係に、店舗全体の合計だけを見る設計)。

**実装内容(`score.py`)**:
- `compute_daily_uplimit_ratio(df, hole_name)`: 店舗×日でΣhigh_prob/稼働台数(is_invalid除外後)を集計
- `compute_uplimit(df_scored, hole_name, config)`: 長期分位点(全履歴)・短期分位点(直近M日)を`uplimit_config.json`の設定値で算出し、`patterns.blend_scalar`(α=`patterns.FIXED_ALPHA`固定。α自体の学習はStage7評価ハーネス転用後に行う方針、[`今後の実装予定.md`](今後の実装予定.md)2.2節参照)でブレンド。超過日はin-placeで`log_odds`/`high_prob`を補正
- `uplimit_config.json`: 分位点・安全マージン・絶対上限・業界一般値フォールバック(0.4)・短期ウィンドウ日数・最低必要日数・特異日除外リストを外出し(`weights.json`と同じ方針)
- データ不足(最低必要日数未満)時は業界一般値フォールバック(0.4)を使用し、offset補正はスキップ

**実データ検証結果(2026-07-06、実データ5店舗で`run_store_profile.py`実行)**:

- 発動日割合は店舗ごとに大きく異なる(3.8%〜82.7%)。特にyasuda7(153/185日=82.7%)・パーラースーパーセブン新高円寺店(114/185日=61.6%)は非常に高頻度。これは長期/短期分位点ブレンド値(0.55前後)が絶対上限0.5を上回り、`min(0.5, ...)`により実質的に絶対上限0.5固定になっているため(店舗ごとの動的な分位点が機能していない)。原因はStage3出力(high_prob)の日次平均が0.5近辺に集中している(preprocessのlog_oddsベースラインの偏り。w3=0時代に見られた狭帯域問題と同型の現象が、w3有効化後も一部店舗で残っている)
- yasuda7で台ごとの統合スコア(high_prob)コントラスト(上位10%-下位10%ギャップ)・差枚との相関を確認: コントラストは悪化なし(0.369→0.372)、Spearman相関もほぼ同水準(0.839→0.836)で90パーセンタイルキャップ試験適用時の検証結果の傾向と整合
- **要フォローアップ**: 上記の高発動率は`uplimit_config.json`の安全マージン(0.05)・絶対上限(0.5)が現在のStage3出力分布に対して厳しすぎる可能性を示唆する(発動日の割合が極端に多い場合はマージンを見直す必要がある)。詳細な対応候補は[`今後の実装予定.md`](今後の実装予定.md)2.3節参照。値の調整は運用しながら判断する暫定パラメータ

**発見した前提条件の課題(実装優先度に影響、2026-07当時)**: 回転数チャンネル(w3・`logLR_kaiten`)は`preprocess.compute_all_logLR`で常に0.0固定(Stage5学習待ちのスタブ、本ドキュメントのStage3節では「単店段階ではw3は実測相関の強さをそのまま使用」としているが未対応)。この結果、E[high_prob]/Nが常に0.5近辺の狭い帯域に収束し、上限モデルが判定対象とする「店舗全体の高設定投入率」を意味のある解像度で表現できていない可能性が高い。

**前提条件の解消(2026-07、`multi_store.py`実装により完了)**: 上記のw3有効化(Stage1b/5)を実装済み。ただし`stage3_channel_weights.json`は現時点でw3=0.0のまま(LOSOゲート未合格、または未再学習)であり、上記「実データ検証結果」で見られる狭帯域の残存はこれが一因の可能性がある。

**狭帯域問題の解消(2026-07-07、Stage3の事前確率切片β₀導入により完了)**: 上記の高発動率の根本原因は「β₀なし=暗黙にπ=0.5」のmiscalibrationだった(Stage3節「事前確率切片β₀」参照)。π=0.15導入後に全店舗(6店舗)で再実行した検証結果:

- high_prob日次平均: 0.46〜0.50 → 0.24〜0.28(中央値0.12〜0.17)に低下し、狭帯域が解消
- 上限キャリブレーション発動: yasuda7 153/185日(82.7%)→0/186日、パーラースーパーセブン新高円寺店 114/185日(61.6%)→1/186日、他店舗も0〜4日に減少(動的分位点が絶対上限0.5の下で正常に機能)
- 順位の健全性: β₀は全行一律のため店舗内順位は不変。同日実測差枚とのSpearman相関はyasuda7で0.839と導入前と同値、全店舗0.77〜0.84で維持。上位10%-下位10%ギャップは0.69〜0.77に拡大(sigmoidの飽和領域から抜けたことでコントラストはむしろ向上)

---

## score.py

### 店舗×日 高設定台数上限キャリブレーション(Step1実装済み・候補C)

Stage3が台ごとに独立してsigmoid(LogOdds)を出力するため、店舗内で合計すると理論上ありえない水準まで高設定確率の期待値が積み上がることがある。`run_store_profile.py`の`run_for_hole()`内、S_稼働低さの計算後・`synthesize()`呼び出しの前で`sc.compute_uplimit(scored, hole_name)`を呼び、超過日の`log_odds`/`high_prob`をin-placeで補正してから`write_stage3_scores`を書き込む(補正後の値を機能A/Bに反映するため、この呼び出しはcompute_uplimitの後に移動している)。

- ハードキャップではなく、超過度合いに応じて連続的に縮小するソフトな正則化(ベイズ的shrinkage)。二分探索(`_solve_uplimit_offset`)で`mean(sigmoid(log_odds-offset))`が上限相当に収まるoffsetを求め、全台一律に適用する
- Stage5の階層モデル(β₀切片)に対する正則化として位置づけ。γ_storeの自由な学習は妨げない
- `patterns.py`のサブスコア(S_全台系・S_鉄板台等)は本処理より前の時点で確定済みのため影響を受けない(検出用信号とStage3出力キャリブレーションは役割が別)
- 候補比較・実データ検証結果は上記patterns.pyセクション「長期/短期αブレンド」直後を参照
- `store_profile`テーブルに`上限キャリブレーション値`・`上限信頼度`カラムを追加(`gamma_store`と同様、店舗単位のスカラー値を(ホール名,パターン)の全行に複製。既存DBへは`ALTER TABLE`でマイグレーション)
- 特異日(周年・グランドオープン等)除外は案A(`uplimit_config.json`の「特異日除外リスト」に日付文字列を手動追加。識別方法自体は引き続き未設計、初期値は空リスト)

### サブスコア一覧

| サブスコア | 系統 | データ不足時 | αブレンド |
|---|---|---|---|
| S_全台系 | 幅 | 除外 | ◯ |
| S_鉄板台 | 深さ | 履歴不足 or 欠損偏りガード抵触 → 除外 | ◯ |
| S_ローテ | 深さ | 同上 → 除外 | ◯ |
| S_新台増台 | 幅 | 該当なし → 除外 | — |
| S_移動台 | 幅 | 該当なし → 除外 | — |
| S_据え置き | 深さ | 履歴不足 → 除外 | ◯ |
| S_稼働低さ | 店舗全体 | 不明 → 除外 | — |

### 合成スコア式

```python
狙い目度 = Σ(wᵢ × Sᵢ) / Σ(wᵢ)  # 存在するサブスコアのみで再正規化
```

- データ不足のサブスコアは「0点」ではなく「計算から除外」(新規店がデータ不足だけで低評価にならないようにする)
- 初期重みは均等(wᵢ=1)。全台系×稼働低さを重め(wᵢ=1.5)にする案もあるが、実データで調整前提
- 重みは `weights.json` に外出し → コード変更なしで調整可能

### 稼働(混雑度)の推定(S_稼働低さ)

```python
# 手入力なし・スクレイピング済みの回転数から完全自動推定
推定稼働 = その店・その日の回転数集計(合計または平均)
基準値 = その店の過去の同条件(同曜日・イベント有無等)の平均
S_稼働低さ = 基準値に対する「低さ」を正規化(逆数方向)
```

> 個別台の役割②(設定の証拠としての回転数)とは目的が異なる。同じ生カラムから別の意味の指標を導出している。

### 店舗プロファイル

機能Bの「データ蓄積のみで精度向上」を実現する派生データ層。振り返り分析ごとに再計算される。

| 項目 | 内容 |
|---|---|
| 店舗ID | - |
| パターン種別 | 全台系/鉄板台/ローテ/新台増台/移動台/据え置き/稼働低さ |
| 現在のサブスコア値 | Sᵢ |
| 信頼度 | 履歴日数・サンプル数・欠損偏りフラグを反映(仮) |
| γ_store | Stage5-1で学習される店舗固有補正(複数店舗揃うまで空欄可) |
| 上限キャリブレーション値 | 店舗×日 高設定台数上限キャリブレーション(Step1)の推定値(2026-07追加、詳細は上記score.pyセクション参照) |
| 上限信頼度 | 同上の信頼度(データ量ベース) |
| 最終更新日時 | - |

- 信頼度は「学習結果」ではなく、データ量に基づく統計的信頼性の代理指標
- 出力時、内訳と信頼度を必ず併記(データが薄い新規穴場店を不当に隠さないため)

### 機種×日 全台系/高配分の判定ログ(`write_machine_judgment_log`、2026-07-09設計合意・Phase1実装済み)

`patterns.score_zentaikei_judgment`の出力(機種×日×ホール粒度)を`machine_judgment_log`テーブルへ
append-only記録する(今後の実装予定.md 1.8節)。`run_store_profile.run_for_hole`が`write_stage3_scores`
直後(上限キャリブレーション補正後のhigh_probを使うため。機能Aの表示値と揃える)に呼ぶ。

- `prediction_log`/`score_snapshot`(直近1日のみ記録)と異なり、**呼び出し側は収集済み全履歴分を
  まとめて渡す**(全台系イベントは月数回と稀なため記録を早く始める価値があり、当日完結の記述統計
  でリークの心配がないため過去分の再計算も問題ない)
- 重複ガードは事前の存在チェックではなく、`PRIMARY KEY (ホール名, 日付, 機種名)`に対する
  `INSERT OR IGNORE`で行単位に行う(初回=全履歴バックフィル、以降=新規日のみ実質追記となり、
  fase4の日次再実行でも二重記録されない。`prediction_log`のバッチ単位事前チェック方式とは異なる設計)

### 末尾版レイヤー2検定結果の保存(`write_group_calendar_conditions`、2026-07-10「末尾版」フェーズ2実装済み)

`patterns.build_group_calendar_conditions`の出力(台番号末尾グループ×日付条件のMann-Whitney U検定+
一致ルール2本、詳細は上記patterns.pyセクション参照)を`group_calendar_conditions`テーブルへ保存する。
`run_store_profile.run_for_hole`が`write_stage3_scores`直後(`write_machine_judgment_log`と同じ位置。
上限キャリブレーション補正後のhigh_probを使うため)に呼ぶ。

- `teppan_conditions`と同じ「店舗単位で全削除→再挿入」方式(収集済み全履歴を毎回再検定するため、
  `machine_judgment_log`のようなappend-only履歴蓄積は不要)
- 有意でない組み合わせも含め全条件を保存する(検出は緩く保存・使用側でゲートの方針。重複統合
  (max効果量)は保存時ではなく予測時に適用する)
- 続けて`patterns.predict_tail_group_next_day`で翌観測日の予測を計算し、予測種別`'S_末尾'`で
  `prediction_log`へ並走記録する(`_run_tail_group_predictions`。既存`write_prediction_log`の
  重複ガードをそのまま流用)

---

## 出力3系統

| 出力 | 用途 | 詳細度 |
|---|---|---|
| **トップページ**(2026-07追加・同月UIリニューアルで再編) | 当日・翌日の狙い目を最初の1画面で把握する | MM/DD(曜)のおすすめ店舗(符号付き合成スコアのプラス上位3+マイナス下位3、表形式・色分け)・MM/DD(曜)の熱い台予測(店舗ごと/全店舗横断、個別台・機種・ローテ・新台・増台・移動台・据えの7タブ) |
| **機能A: 店内比較・可視化ツール** | 来店前/来店中に1店舗内の台を手動で見比べる | 生データ寄りの簡易指標。手動フィルタ(期間/機種/台番号/推定設定レンジ/列/島など) |
| **機能B-詳細: 店舗特徴(個別店舗詳細)**(2026-07UIリニューアルで店舗横断比較render_overviewは削除) | 蓄積データからのパターン検出・店舗プロファイルをじっくり見る | サブスコア内訳(符号付き・縦棒グラフ)・信頼度・γ_store・検知期間履歴(パターン別タブ)・当月カレンダーヒートマップを全て表示 |
| **機能B-簡潔: 狙い目メモ** | 次回「家を出る前」に見る簡潔な要約 | 結論+最低限の根拠のみ |

**統合エントリポイント(`app.py`、2026-07追加・同月UI再構成、同月UIリニューアルで再々編)**: `streamlit run app.py`で起動し、2ページ構成を`st.session_state['selected_hole']`で切り替える(サイドバーは使わない。旧サイドバー選択メニュー・サイドバーフィルタ・表示設定は廃止)。

- **ホームページ(主ページ・起動直後に表示)**: ①店舗検索(`st.selectbox`。入力で絞り込み、選択で店舗トップページへ遷移。旧テキスト入力+ボタン列挙は廃止) ②MM/DD(曜)のおすすめ店舗(`app_top.render_recommend_stores()`) ③MM/DD(曜)の熱い台予測(`app_top.render_hot_predictions()`。店舗ごと/全店舗横断の7タブ構成。旧当日・翌日ランキング`render()`・機能B店舗横断比較`render_overview()`は廃止)
- **店舗トップページ(店舗ごと)**: 「← ホームに戻る」ボタン+店舗切替`st.selectbox`＋店舗名タイトル(折返し防止CSS適用)の下に、①機能A店内比較(`app_a.render(hole_name)`: 店舗分析/台番号/機種名の3ビュー) ②店舗特徴(`app_b.render_store_detail(profiles, 店名)`。**機能Aで「店舗分析」ビュー選択時のみ表示**: サブスコア内訳(縦棒グラフのみ)・γ_store・検知期間履歴(パターン別タブ)・当月カレンダーヒートマップ)

`st.set_page_config`は`app.py`側で1回のみ実行し、`app_top.py`/`app_a.py`/`app_b.py`はモジュールトップレベルにst呼び出しを持たない関数(`render_recommend_stores()`+`render_hot_predictions()`/`render(hole_name)`/`render_store_detail()`)として画面本体を公開する(fase3の`streamlit_app.py`が`app`モジュールだけをreloadする前提を維持)。**単体起動(`streamlit run app_a.py`等)は廃止**(Streamlitエントリポイントは`app.py`のみ。機能B-簡潔のテキストメモ`python app_b.py`は維持)。

**モバイルファーストUI(`ui_theme.py`、2026-07追加・同月に黒基調ダークテーマへ改修)**: iPhone Safari前提のカードUIの表示層共通モジュール。当初は薄グレー地+白カードのHoYoLAB風だったが、同月中にZZZ(ゼンレスゾーンゼロ)風の黒基調(ページ`#0D0E11`・カード`#1A1C21`)+ライムイエローアクセント`#D6FE3E`へ改修済み。パレット定数`ACCENT/PAGE_BG/CARD_BG/CARD_BORDER/TEXT/TEXT_SUB/GRID/ZERO_LINE/CATEGORICAL(折れ線用8色)/DIVERGING(発散スケール)/SEQ_BLUE(逐次・信頼度用)/SEQ_WARM(カレンダー暖色)`を集約し、app_a/app_top/app_bはこれらを参照する(色の直書き禁止)。**DIVERGINGは2026-07に2回改修**: 当初は中間点をダークグレー(`#383835`)にしてRdBuの白発光を回避していたが、ユーザー指定により「マイナス=青・プラス=赤・中間=白」の慣例的なRdBu風配色に変更。**同月のUIリニューアルで両端を再度入れ替え、「プラス=青(`#2878E0`)・マイナス=赤(`#F5334F`)・中間=白」に統一**(`[[0,NEG_COLOR],[0.5,'#FFFFFF'],[1,POS_COLOR]]`、`ui_theme.POS_COLOR`/`NEG_COLOR`定数を新設しサイト全体の色統一を明示)。この結果、スコア0付近のセルが白っぽくなる(実データはほとんどのセルが0付近に集中するため多用される)ことは許容した上での選択。表形式の数値着色にも同じ配色を使う`ui_theme.style_signed(df, cols)`(Styler、font-color着色)を新設し、トップページのおすすめ店舗テーブル・熱い台予測7タブで使用する。**白中間点により白文字(`ui.TEXT`)が読めなくなる箇所は個別に濃色文字へ切替が必要**(`app_b.py`のパターン別内訳カレンダー`textfont=dict(color=ui.CARD_BG)`が実例。`ui.CARD_BG`は`#383835`→`#F5334F`/`#2878E0`の全域で3:1以上のコントラストを確保できるため採用)。`inject_css()`(`app.py`が起動時に1回呼ぶ。`layout='centered'`・`max-width:680px`中央寄せ・角丸カード・ピルボタン(primaryはライム地+黒文字)・input系16px以上のCSSを注入)と`apply_mobile_layout(fig, height=None, show_colorbar=False)`(全チャート共通のPlotlyレイアウト調整。文字色`TEXT`指定・`colorway=CATEGORICAL`・軸に`gridcolor/zerolinecolor/automargin=True`(ラベル切れ防止)・`hoverlabel`のダークスタイル統一・凡例横型下配置・`dragmode=False`)、`wrap_label(s, width=14)`(長いラベルを`…`省略でなくwidth文字ごとに`<br>`改行して全文表示。旧`short_label`は廃止・置換済み)を提供する。全チャート(app_top×2・app_a×5・app_b×4の計11箇所)は`ui.apply_mobile_layout(fig, height=…)` → `st.plotly_chart(fig, use_container_width=True, config=ui.PLOTLY_CONFIG)`の順で呼び出す規約に統一。機能(データ・計算ロジック)には一切関与しない表示層のみの変更。テーマ色(`primaryColor`等)は`fase2/.streamlit/config.toml`(ローカル実行用)とリポジトリルート`.streamlit/config.toml`(Streamlit Community Cloud用。cwdがリポジトリルートなためこちらが効く)の2ファイルに同一内容を維持する(相互コピー同期。詳細は[`fase3/配信公開_skill.md`](../fase3/配信公開_skill.md)参照)。

**機能B再設計(2026-07議論・Phase1〜5実装済み、Phase6は将来項目)**:

- **Phase1: `pattern_history`テーブル**(下記データモデル参照)— `store_profile`が最新1行のみ保持するのに対し、`run_store_profile.py`実行のたびに追記のみで積み上げる検知期間履歴用テーブル
- **Phase2: 符号付きスコア拡張** — 幅型(上記patterns.py参照)・深さ型(S_鉄板台、上記参照)の符号付き化と、`score.synthesize()`/`app_b.py`の信頼度重み減衰(有効重み=weights×reliabilities)。`狙い目度`のレンジは`[0,1]`から`[-1,1]`に変更。`app_b.py`の表示レンジ(`range_x=[-1,1]`)・色スケール(発散配色`ui.DIVERGING`、中央0。2026-07ダーク化改修前は`RdBu`。同月中に再改修し中間点を白へ変更、詳細は上記「モバイルファーストUI」参照)も追随済み
- **Phase3: Stage7-0〜7-2**(上記patterns.py「S_鉄板台の翌日予測」参照) — `predict_next_day`・`prediction_log`・`evaluate_predictions.py`
- **Phase4: トップページ**(`app_top.py`、2026-07UIリニューアルで再実装) — `render_recommend_stores()`(MM/DD(曜)のおすすめ店舗。`ab.synthesize_scores`の合成スコアのプラス上位3+マイナス下位3を表形式・色分けで表示)と`render_hot_predictions()`(MM/DD(曜)の熱い台予測。店舗ごと/全店舗横断の2段構成×個別台・機種・ローテ・新台・増台・移動台・据えの7タブ)に分割。個別台/機種タブは`prediction_log`(S_鉄板台)の最新行+`prediction_accuracy`の的中率をそのまま表示(その場での再計算はしない)。ローテ/新台/増台/移動台/据えタブは`stage3_scores`の最新日スナップショット(`_load_latest_snapshot`、Stage B拡張の6列)+`store_profile`の店舗単位信頼度(`_load_store_reliabilities`)を暫定閾値でフィルタして表示(的中率は集計が無いため常に「検証中」固定。据えタブのみ「予測スコア」として生スコアを表示)。**新台/増台タブは同じ`S_新台増台`データを表示**(`detect_events`が機種の店舗初出と既存機種の台数追加を区別していないため。区別ロジックは今後の実装予定.md参照)
- **Phase5: 店舗プロファイル拡張**(`app_b.py`、`render_store_detail()`。2026-07UIリニューアルで表示形式を改修) — サブスコア詳細は縦棒グラフのみ(横棒+表は廃止)。検知期間履歴(`pattern_history`から「スコア>0」の連続区間を軽量な後処理で検出。統計検定の再実行はしない近似表示)はパターンごとに`st.tabs`で分割表示。当月カレンダーヒートマップ(統合スコア日次平均`ui.SEQ_WARM`(旧`YlOrRd`)+パターン別内訳`ui.DIVERGING`(旧`RdBu`。中間点が白のため日数字は`ui.TEXT`でなく`ui.CARD_BG`の濃色文字を使用)。「月」「表示項目(統合スコア日次平均+パターン別)」の2 selectboxで選択1枚のみ描画、**第1週が上になるようy軸を反転**(`update_yaxes(autorange='reversed')`))。カレンダーヒートマップは機能Aには追加せず機能Bのみに実装(機能Aは生データ寄りの手動比較ツールという役割を維持するため)。店舗横断比較`render_overview()`(店舗ランキング・サブスコア比較・全サブスコアヒートマップ)は2026-07UIリニューアルで削除(店舗横断のおすすめ表示はPhase4の`render_recommend_stores()`に統合)
- **Phase6(将来項目)**: Stage7-4(ウォークフォワードα学習への置き換え)・Stage7-5(S_ローテ・S_据え置きの非該当日判定・翌日予測拡張)・新台/増台タブの区別ロジック。詳細は[`今後の実装予定.md`](今後の実装予定.md)参照
- **2026-07-08追加: 古い予測の除外/明示**(`app_top._load_latest_predictions`) — `run_store_profile.py`の実行間隔が空いた店舗(fase4異常停止時など)の古い予測が最新予測と混在しないよう、個別台/機種タブ(`prediction_log`由来の2タブのみ。ローテ/新台/増台/移動台/据えは`stage3_scores`由来で対象外)に鮮度フィルタを追加。グローバル最新対象日(ロード済み行の対象日最大値)から`_PREDICTION_STALE_GRACE_DAYS`(暫定2日)より古い行は除外。猶予2日以内でも最新対象日と不一致の行は「鮮度」列に「N日前」を表示し`ui_theme.style_stale_rows`でグレーアウト(対象日列を読まなくても気づける形)。店舗の予測が全て猶予落ちした場合(または全店舗横断タブで全店舗が猶予落ちした場合)は「該当なし」でなく「予測が古いため非表示(最終対象日: X)」と表示する(`app_top._all_stale`)
- **2026-07-08追加: 個別台タブの理由文表示**(`app_top._reason_text`) — 実装済みだが未接続だった関数を`_render_prediction_tabs`に接続。個別台タブのみ、`prediction_log`の`詳細`列(JSON: カレンダー条件/周期日数)からテーブル下に`st.caption`で根拠短文を行ごとに表示(モバイル幅のためテーブル列には追加しない)。据えタブ等への拡張・ローテタブ対応は未着手([`今後の実装予定.md`](今後の実装予定.md)1.4節参照)

**store_profile/stage3_scoresの前提(`run_store_profile.py`、2026-07追加・同月改修)**: 機能B(詳細/簡潔とも)は分析DB(`ホールデータ/analysis.db`)の`store_profile`テーブルを読むだけで、自らpreprocess→patterns→scoreパイプラインを実行しない。新規に取り込んだ店舗はこのテーブルが未作成のため機能Bに表示されない。`python run_store_profile.py`(全店舗一括)または`--hole <店舗名>`(特定店舗)でパイプラインを実行し、`stage3_scores`(Stage3出力の台×日スコア。`score.write_stage3_scores`が店舗単位で全削除→再挿入)と`store_profile`を作成・更新する。2026-07に[`fase4/`](../fase4/)(日次自動実行)を実装し、`fase4/run_daily.py`が全店舗一括を毎日自動実行するようになった。新規店舗取込時の即時反映等では引き続き手動実行も可能。

> **既知の運用リスク(2026-07-07発覚、fase4導入で解消見込み)**: fase1(`メイン.py`)がTursoレプリカを最新化しても、fase2側で`run_store_profile.py`を実行し忘れると`stage3_scores`/`store_profile`は古いままになり、特定の店舗だけ機能Bの予測日付が他店舗より大幅に遅れる(実例: マルハン新宿東宝ビル店のみ`stage3_scores`が2026-06-03で停止、他店舗は2026-07-04まで更新済みという乖離が発生していた)。原因は特定できていないが、レプリカ側(生データ)は全店舗同じ日付まで取得できていたことから、fase2側の再計算バッチが一部店舗だけ漏れた/失敗したまま気づかれずにいたと考えられる。**予防策**: `fase4/run_daily.py`が`--hole`を付けずに`run_store_profile.py`(全店舗一括)を毎日自動実行するため、実行し忘れ自体は解消される見込み。手動運用時は引き続き全店舗一括での定期実行を推奨。

機能A の実装済みビュー（`app_a.py`。`app.py`の店舗トップページから`render(hole_name)`で呼ばれる）:

| ビュー | 内容 |
|---|---|
| 店舗分析 | 月間累計差枚棒グラフ / 任意月または全期間の累計差枚推移折れ線（cumsum）。折れ線は0ライン強調・プラス/マイナス背景色付き |
| 台番号 | 日付絞り込み → 台番号末尾絞り込み(0〜9・ぞろ目、複数選択) → 機種名でサジェスト → 台番号 multiselect → 台番号×機種名の連続期間でセグメント化して色分け描画。グラフ点クリックでその日の素データをテーブル表示 |
| 機種名 | 日付絞り込み → 台番号末尾絞り込み(0〜9・ぞろ目、複数選択) → **末尾未選択時**: 機種名 multiselect → 機種ごとに色分け描画 / **末尾選択時**: 機種名選択スキップ・末尾ごとに色分けして同一グラフに描画 |

共通機能:
- **店舗選択**: `app.py`のホームページ(店舗検索)で選択済みの店舗に固定(店舗名タイトルは`app.py`側が表示。旧サイドバーの店舗selectbox・フィルタ(期間/機種名/台番号/high_probスライダー)は2026-07のUI再構成で廃止)
- 台番号・機種名ビューのデータ範囲プリセット: 1週間 / 1か月 / 3か月 / 全期間（デフォルト1週間）
- **横軸**: 日付固定・`YYYY/MM/DD` 形式。データ範囲「1週間」選択時は曜日も表示 (`2026/02/01(日)`)。確率列は分数表示(1/N)
- **Y軸(台番号ビュー)**: 統合スコア・差枚の2択。**Y軸(機種名ビュー)**: 設定投入率・差枚の2択
  (2026-07-10「末尾版」フェーズ4で統合スコア軸を廃止・設定投入率軸へ置き換え。理由: 機種×日の
  統合スコア平均=機種平均high_prob×6は設定投入率軸(Σhigh_prob÷n×6)と数値的に同一曲線で、
  z ゲート・基準線・ツールチップ付きの設定投入率軸が上位互換のため。台番号ビューは個別台の
  生値のためこの重複がなく統合スコア軸を維持)。全グラフで日本語単位表示（1万以上は「万」単位）
- **統合スコア (0〜6、台番号ビューのみ)**: Stage3 の `high_prob × 6`。選択時は固定レンジ 0〜6 で表示。`stage3_scores` テーブル未作成の場合は `preprocess.py` でオンザフライ計算
- **日付絞り込み**(台番号・機種名ビュー内インライン、折りたたみ式):
  - 日付末尾: 0〜9・ぞろ目(11/22) から単一選択
  - 曜日: 月〜日から単一選択
  - 単日指定: 日付を複数選択（別枠）
  - 末尾 AND 曜日、単日指定は OR で結合
- **台番号末尾絞り込み**(台番号・機種名ビュー): 0〜9・ぞろ目を複数選択可。複数選択時は末尾ごとに色分けて同一グラフに描画
- **日付末尾→台番号末尾 自動同期**: 日付末尾を変更すると台番号末尾に同じ値が自動セット。台番号末尾を手動変更した場合は上書きしない
- 台同一性: (台番号, 機種名) の連続期間をセグメントとして扱い、機種変更を移動台/入替として自動分離
- **データキャッシュ**: `@st.cache_data` で初回のみ計算、2回目以降は即座。スクレイピング後は機能Aセクション先頭の「キャッシュをクリア」ボタンで新データを反映

---

## データモデル(fase2新規追加分)

**データの流れ(2026-07改修)**: fase2の入力は`ホールデータ/turso_replica.db`(fase1が維持するTurso埋め込みレプリカ。sqlite3で読み取り専用参照)、分析成果物(`stage3_scores`/`store_profile`)の保存先は`ホールデータ/analysis.db`(ローカル専用・再計算で再生成可能)。パスと接続は`data_source.py`(`REPLICA_DB_PATH`/`ANALYSIS_DB_PATH`/`connect_replica`/`connect_analysis`/`list_holes`)に集約。旧構成の店舗別DB(`ホールデータ/{店舗名}.db`)はTurso移行前のアーカイブでありfase2は参照しない。

### `stage3_scores` テーブル（analysis.db、`score.write_stage3_scores`が更新）

| カラム | 型 | 内容 |
|---|---|---|
| 日付 | TEXT | YYYY-MM-DD |
| ホール名 | TEXT | URLスラッグ |
| 機種名 | TEXT | |
| 台番号 | INTEGER | |
| log_odds | REAL | Stage3統合LogOdds |
| high_prob | REAL | sigmoid(log_odds) → 高設定確率 |
| is_invalid | INTEGER | 1=判定不能(回転数不足) |
| S_全台系 | REAL | 幅型サブスコア(2026-07 UIリニューアルで追加。台×日粒度でNULL許容) |
| S_鉄板台 | REAL | 深さ型サブスコア(同上) |
| S_ローテ | REAL | 深さ型サブスコア(同上) |
| S_新台増台 | REAL | 幅型サブスコア(同上) |
| S_移動台 | REAL | 幅型サブスコア(同上) |
| S_据え置き | REAL | 深さ型サブスコア(同上) |

上記6列は`run_store_profile.py`の`scored`(`compute_uplimit`実行後、`write_stage3_scores`呼び出し時点)が
既に保持している値をそのまま保存したもので、算出ロジックの変更ではない。トップページの
「MM/DD(曜)の熱い台予測」7タブが、台×日単位の再計算なしで最新日のスナップショットを
SELECTするだけで済むようにするための追加(`score._ensure_stage3_scores_schema`が
`PRAGMA table_info`方式でALTER TABLEするマイグレーション。既存行はNULLのまま)。

**注意(2026-07-08判明)**: この`_ensure_stage3_scores_schema`は**ローカル**`analysis.db`のみを
マイグレーションする。分析用Turso(クラウド側)のテーブルは追従しないため、`stage3_scores`等
analysis.dbの6テーブルに列を追加・変更した場合は、`fase3/upload_analysis.py`を**次回に限り
`--full`で実行する**運用ルールを忘れないこと(差分upsertのままだと列数不一致でクラウド反映が
失敗し続ける)。手順は[`fase3/配信公開_skill.md`](../fase3/配信公開_skill.md)「アルゴリズム変更時の
`--full`実行手順」参照。

### `store_profile` テーブル（analysis.db、`score.update_store_profile`が更新）

| カラム | 型 | 内容 |
|---|---|---|
| ホール名 | TEXT | |
| パターン | TEXT | s_all / s_teppan / s_rote / s_shintai / s_idoudai / s_sueki / s_kadou |
| スコア | REAL | [-1,1](2026-07符号付き化。それ以前は0〜1) |
| 信頼度 | REAL | 0〜1(履歴日数ベース) |
| gamma_store | REAL | Stage5-1で学習。未学習時はNULL |
| 上限キャリブレーション値 | REAL | 店舗×日 高設定台数上限キャリブレーション(Step1)の推定値(2026-07追加。gamma_storeと同様、店舗単位のスカラー値を全パターン行に複製) |
| 上限信頼度 | REAL | 同上の信頼度 |
| 遷移_ベース率 | REAL | `estimate_transition_matrix`の`pi`(長期版。2026-07-08 タスク3追記(c)で追加。gamma_storeと同様、店舗単位のスカラー値を全パターン行に複製。ペア数不足の店舗はNULL) |
| 遷移_p_stay | REAL | 同上の`p_stay`(長期版) |
| 遷移_p_up | REAL | 同上の`p_up`(長期版) |
| 遷移_ペア数 | INTEGER | 同上の`n_pairs`(長期版) |
| 更新日時 | TEXT | |

**注意**: 2026-07-08 タスク3で上記4列(遷移_*)を追加した。`_ensure_stage3_scores_schema`と同様、`_ensure_store_profile_schema`も**ローカル**analysis.dbのみをマイグレーションするため、`fase3/upload_analysis.py`を次回に限り`--full`で実行すること(上記「分析用Turso列不整合の教訓」と同じ注意)。

### `teppan_conditions` テーブル（analysis.db、`score.write_teppan_conditions`が更新。2026-07追加）

S_鉄板台がどの条件で有意だったかの台単位メタデータ。`run_store_profile.py`実行時に店舗単位で全削除→再挿入。「明日は該当日か」の判断・機能B再設計のトップページ表示(既知カレンダー条件+未知周期の候補表示)に使う。

| カラム | 型 | 内容 |
|---|---|---|
| ホール名 | TEXT | |
| 機種名 | TEXT | |
| 台番号 | INTEGER | |
| 経路 | TEXT | 'カレンダー' / '周期(ACF+PDM)' / '周期(Lomb-Scargle)' |
| 条件 | TEXT | '末尾_7'・'曜日_金'・'周期13日(観測順)' など |
| 効果量 | REAL | カレンダー=rank-biserial、周期=1−θ または最大日次スコア |
| 更新日時 | TEXT | |

### `pattern_history` テーブル（analysis.db、`score.write_pattern_history`が更新。機能B再設計Phase1・2026-07追加）

`store_profile`が最新1行のみ保持するのに対し、`run_store_profile.py`実行のたびに**追記のみ**(DELETEしない)で積み上げる検知期間履歴用テーブル。

| カラム | 型 | 内容 |
|---|---|---|
| ホール名 | TEXT | |
| パターン | TEXT | s_all / s_teppan / s_rote / s_shintai / s_idoudai / s_sueki / s_kadou |
| スコア | REAL | 実行時点のサブスコア値(符号付き拡張後) |
| 信頼度 | REAL | 実行時点の信頼度 |
| 実行日時 | TEXT | |

表示側(`app_b.py`の`_detect_pattern_periods`)は「スコア>0が連続している区間」を軽量な後処理でまとめて検出期間として可視化する(統計検定の再実行はしない近似表示、しきい値は暫定0.0)。過去に記録済みの行は書き換えない(append-only)。2026-07に[`fase4/`](../fase4/)(日次自動実行)を実装し`run_store_profile.py`は毎日自動実行されるようになったため、日付方向の粒度は日次で揃う。

### `prediction_log` テーブル（analysis.db、`score.write_prediction_log`が更新。機能B再設計Stage7-1・2026-07追加）

S_鉄板台の翌日予測結果を**追記のみ**(DELETEしない)で記録する。`pattern_history`(過去向き・現在値の記録)とは目的が逆(未来向き・予測とその答え合わせ)であり別テーブル。

| カラム | 型 | 内容 |
|---|---|---|
| 予測ID | INTEGER | PRIMARY KEY AUTOINCREMENT |
| 実行日時 | TEXT | |
| 使用データ最終日 | TEXT | リーク検証用。この日以前のデータのみで計算した証拠 |
| 対象日 | TEXT | 使用データ最終日の翌営業日 |
| ホール名 | TEXT | |
| 機種名 | TEXT | |
| 台番号 | INTEGER | |
| 予測種別 | TEXT | 文字列化(2026-07時点で7種: 'S_鉄板台'/'遷移予測'/'S_据え置き'/'遷移予測_前日差枚'/'S_末尾'/'S_機種'/'S_機種_直近') |
| 長期スコア | REAL | 長期版hpでの予測値(α学習用の生コンポーネント) |
| 短期スコア | REAL | 短期版hpでの予測値 |
| ブレンド値 | REAL | 実際に使ったFIXED_ALPHAでの予測値 |
| 使用alpha | REAL | |
| 詳細 | TEXT | JSON({"周期日数": [...], "カレンダー条件": [...]}) |

**重複追記ガード(2026-07・fase4導入の随伴改修)**: `write_prediction_log`はINSERT前に
(ホール名, 予測種別, 使用データ最終日)の組が既存かを照会し、**該当する組のrowsを丸ごと
追記前にスキップ**する(粒度は台単位ではなくバッチ単位。1回の`run_store_profile.py`実行=
1店舗×1予測種別×1データ最終日のバッチのため十分)。データが進んでいない店舗で
`run_store_profile.py`を再実行しても、fase4のcatchup実行や手動再実行で同じ予測が
二重記録されない。DELETE/UPDATEは行わずINSERT対象を絞るだけなのでappend-only方針は維持される。

### `prediction_accuracy` テーブル（analysis.db、`evaluate_predictions.evaluate`が更新。機能B再設計Stage7-2・2026-07追加）

`prediction_log`と実測差枚の突合結果を(ホール名, 予測種別)ごとに集計・キャッシュする。機能Bはこれを読むだけ。

| カラム | 型 | 内容 |
|---|---|---|
| ホール名 | TEXT | PRIMARY KEY(ホール名, 予測種別) |
| 予測種別 | TEXT | |
| サンプル数 | INTEGER | |
| spearman相関 | REAL | サンプル数<`MIN_SAMPLES`(30)ならNULL(検証中) |
| precision_at_n | REAL | 予測上位N台(`TOP_N`=3)と実測上位N台の重複率平均 |
| リフト | REAL | 予測上位N台平均実測差枚÷店舗全体平均実測差枚 |
| 集計期間開始 / 集計期間終了 | TEXT | |
| 更新日時 | TEXT | |

### `score_snapshot` テーブル（analysis.db、`score.write_score_snapshot`が更新。2026-07-09 タスク5「記録側先行実装」）

wᵢ(サブスコア重み)学習(今後の実装予定.md 1.6節)の教師データを**日次スナップショットとしてappend-only蓄積**する。学習側(Spearman直接最大化・店舗別ゲート・均等重みとの並走対比較)は蓄積待ちの別タスクで未実装。粒度は**店舗×使用データ最終日で1行**、値は「使用データ最終日の日次断面」の店舗平均(全期間平均ではない。教師=翌日の店舗実測差枚率に対応する説明変数にするため)。

| カラム | 型 | 内容 |
|---|---|---|
| 実行日時 | TEXT | |
| 使用データ最終日 | TEXT | PRIMARY KEY(ホール名, 使用データ最終日) |
| ホール名 | TEXT | |
| S_全台系 〜 S_稼働低さ | REAL | `SUB_SCORES`7列。使用データ最終日断面の店舗平均(NaN可) |
| 狙い目度_店舗平均 | REAL | 同断面の合成スコア店舗平均 |
| 有効重みJSON | TEXT | JSON({サブスコア列: weights×reliabilitiesの実効値}）。合成前のサブスコア別の値と実効重みを両方残すのが本質(狙い目度だけでは重みを変えた再現学習ができない) |

`run_store_profile.run_for_hole`が`synthesize`直後に、その回の`使用データ最終日`断面のみを抽出して呼ぶ。**重複ガード**は`write_prediction_log`と同型((ホール名, 使用データ最終日)が既存ならスキップ、DELETE/UPDATEなし)。タスク3で`S_据え置き`が符号付き日次値に変わった後の値であることに注意(店舗の癖としての解釈は`store_profile`の遷移列(`遷移_ベース率`/`遷移_p_stay`/`遷移_p_up`)を参照)。

`fase3/upload_analysis.py`の`TABLES`は固定6テーブルのリスト方式(`stage3_scores`/`prediction_log`/`store_profile`/`teppan_conditions`/`pattern_history`/`prediction_accuracy`)のため、`score_snapshot`は自動的にアップロード対象外(ローカル学習専用データのためこれでよい。列不整合の心配もない)。

### `machine_judgment_log` テーブル（analysis.db、`score.write_machine_judgment_log`が更新。2026-07-09設計合意「機種強さ軸・全台系/高配分の日次判定」Phase1）

`patterns.score_zentaikei_judgment`の出力を機種×日×ホール粒度でappend-only蓄積する(今後の実装予定.md 1.8節)。

| カラム | 型 | 内容 |
|---|---|---|
| 実行日時 | TEXT | |
| ホール名 | TEXT | PRIMARY KEY(ホール名, 日付, 機種名) |
| 日付 | TEXT | |
| 機種名 | TEXT | |
| 台数 | INTEGER | 機種内有効台数(is_invalid除外、high_prob非NULL) |
| 期待高設定台数 | REAL | Σhigh_prob |
| zスコア | REAL | (Σhigh_prob − n×π) / √(n×π×(1−π)) |
| p値 | REAL | 片側検定(上振れのみ)のp値。同日×同ホール内でbenjamini_hochberg補正の入力に使用 |
| 投入率 | REAL | Σhigh_prob ÷ n(生の比率0〜1。表示側の×6は機能A側の責務) |
| S_全台系 | REAL | 該当日の`score_zentaiki`値(揃いの確認) |
| 判定ラベル | TEXT | 全台系 / 高配分 / 普段どおり(3値) |

重複ガードは`PRIMARY KEY (ホール名, 日付, 機種名)`への`INSERT OR IGNORE`(行単位。他の予測ログ系テーブルとは異なる設計、詳細は上記score.pyセクション参照)。`fase3/upload_analysis.py`の`TABLES`固定6テーブルには含まれないため、`score_snapshot`と同様アップロード対象外(ローカル専用データ)。

### `group_calendar_conditions` テーブル（analysis.db、`score.write_group_calendar_conditions`が更新。2026-07-10「末尾版」フェーズ2実装済み・2026-07「機種単位の癖分析」で拡張）

`patterns.build_group_calendar_conditions`の出力を保存する。グループ種別により
台番号末尾グループ(Mann-Whitney U検定+一致ルール2本)・機種グループ(Mann-Whitney U検定+
看板機種のWilcoxon符号順位検定、恒常窓/直近窓の2窓)の両方を収容する
(今後の実装予定.md 1.8節「末尾版」「機種単位の癖分析」)。

| カラム | 型 | 内容 |
|---|---|---|
| ホール名 | TEXT | |
| グループ種別 | TEXT | `'台番号末尾'` / `'機種'`(恒常窓) / `'機種_直近'`(直近`RECENT_TEST_WINDOW_DAYS`日窓) |
| グループ | TEXT | 末尾版=`グループ末尾_0`〜`9`/`グループゾロ目`/一致ルール行は`一致ルール`固定。機種版=機種名そのまま |
| 日付条件 | TEXT | `calendar_candidates`の49候補名、一致ルール名(`下2桁一致`/`末尾一致`、機種版では出現しない)、または看板機種検定の`恒常`(機種版のみ) |
| 該当日数 | INTEGER | 一致ルール・恒常の行は「有効ペア数」(対応ペアが組めた日数) |
| p値 | REAL | BH補正前の生p値(グリッドはMann-Whitney U片側greater、一致ルール・恒常はWilcoxon符号順位検定片側greater) |
| 効果量 | REAL | rank-biserial相関(-1〜1)。グリッドは`calendar_test`と同じ2群比較版、一致ルール・恒常は対応ペア版 |
| BH有意 | INTEGER | グループ種別ごとに独立の仮説群としてBH補正し、かつ効果量>=`EFFECT_SIZE_THRESHOLD`(0.3)の場合のみ1(末尾版=541仮説、機種版は店舗の検定対象機種数×50で変動) |
| 台数中央値 | REAL | 機種版のみ。当該機種の日次有効台数の中央値(`patterns.group_size_medians`)。表示/予測側の追加ゲート(暫定n≥3等)の判断材料。末尾版の行はNULL |
| 使用データ最終日 | TEXT | |
| 更新日時 | TEXT | |

`teppan_conditions`と同じ「グループ種別単位で全削除→再挿入」方式(PRIMARY KEYなし、
`DELETE WHERE ホール名=? AND グループ種別=?`→INSERT。**2026-07修正**: 以前は「ホール名のみ」で
無条件に全グループ種別を削除していたため機種版の書き込みが末尾版の既存行を消すバグがあった。
`write_group_calendar_conditions`の`group_types`引数で削除対象を明示する方式に変更済み)。
有意でない組み合わせも含め全条件を保存し、重複統合(同一グループの該当条件のうちmax効果量を採用)は
`patterns.predict_tail_group_next_day`/`predict_machine_group_next_day`(翌日予測時)にのみ適用する。
`fase3/upload_analysis.py`の`TABLES`固定6テーブルには含まれないため、`machine_judgment_log`と同様
アップロード対象外(ローカル専用データ。将来アップロード対象へ追加する場合は`TABLES`リストの更新が必要)。

### 外部ファイル

| ファイル | 内容 |
|---|---|
| `machine_setting_specs.json` | 機種別理論値差表(ホールデータ収録153機種を構造化済み。BB/RB確率はTier A機種のみStage1aで使用、機械割(payout)は全機種でStage2差枚チャンネルのμ_sとして使用。`scrape_machine_specs.py`でchonborista.comから取得→`assign_tier.py`でTier判定・正規化して再構築) |
| `weights.json` | サブスコアの重み wᵢ |
| `kaiten_bin_curves.json` | Stage1b(`multi_store.learn_bin_curves`)が学習した機種別デシルカーブ(回転数zスコア→logLR)。未学習の機種は含まれない(=0.0フォールバック) |
| `stage3_channel_weights.json` | Stage5(`multi_store.fit_hierarchical_model`)が学習したチャンネル重み**{w1, w2, w3}**+直交化パラメータ**{orth_a, orth_b}**。既定値は w1=1.0, w2=0.5, **w3=0.0(回転数チャンネル無効)** で、LOSO交差検証合格時のみ正のw3が保存される。※旧実装は{b0..b3}キーで保存しており読み手(`load_channel_weights`)に無視される(=学習結果が反映されない)バグがあった。2026-07にキー名をw系へ統一 |
| `uplimit_config.json` | 店舗×日 高設定台数上限キャリブレーション(Step1)の設定(`score.load_uplimit_config`)。分位点(0.9)・安全マージン(0.05)・絶対上限(0.5)・業界一般値フォールバック(0.4)・短期ウィンドウ日数(30)・最低必要日数(30)・特異日除外リスト(初期値は空)を`weights.json`と同様に外出し |

---

## 重要な設計上の注意点

### Tier C 機種の扱い
RB列がTier Cの機種(ヴァルヴレイヴ2等)は、RB列を切り捨てて他指標(差枚・BB)のみでlogLRを計算する。チャンネル数が減っても LogOdds の重み正規化で対応可能。

### 欠損偏りガードは必ず通過させる
鉄板台・ローテーションの検定では、Stage4-1の欠損偏りガード(判定不能率差チェック)を省略してはならない。低稼働店ほど判定不能が偏りやすく、ガードなしでは偽陽性が増える。

### 短期版サブスコアのサンプル不足
深さ型(鉄板台・ローテ・据え置き)の短期版は、ウィンドウ内サンプルが不足した場合はα=0として長期版にフォールバックする。短期版に虚構の値を作らないこと(Stage4-1の方針と整合)。

### γ_store は複数店舗データが必要
単店データのみの段階では `gamma_store = NULL` 。Stage5の階層モデルは複数店舗データが揃ってから有効化する(2026-07、`multi_store.py`で実装済み。実データ5店舗で学習・`store_profile.gamma_store`へ反映済み)。標本数不足の店舗は`gamma_store=0.0`(補正なし)でフォールバックする。

### 上限キャリブレーション導入時の特異日除外(案Aで暫定運用・識別方法は未解決)
店舗×日の高設定台数上限(`score.compute_uplimit`)は、周年・グランドオープン等の「本物の強い日」を無条件にこの制約に通すと、検出したい信号そのものを機械的に潰してしまう。周年は年1回程度の低頻度だが、頻度の低さは除外の必要性を下げる理由にはならない(その1日が最も価値のある検出対象であるため)。2026-07のStep1実装では`uplimit_config.json`の「特異日除外リスト」に日付文字列を手動追加する案A(初期値は空リスト)で暫定運用する。周年以外の非公式な全台系投入日も同型の問題を抱えるため、日付指定の除外リストだけでは不十分であり、自動識別方法(案B)は引き続き未設計(保留、詳細は[`今後の実装予定.md`](今後の実装予定.md)2.4節)。

### 差枚チャンネルのμ_sに店舗単位の一律補正(κ)を入れてはならない
「ホールの実測機械割は理論値より低めに出る」という観測は事実だが、これは**店舗が低設定を多く運用していることの反映**であって、個々の台が高設定だった場合の理論値そのものがズレているわけではない。実データ(yasuda7.db、回転数合計1億超)で検証したところ、店舗全体の実測平均÷理論値平均の比は0.037まで落ち込み、これを一律にμ_sへ乗じると高設定検出力そのものを潰すことが判明した。理論確率はRNGで法的に規定される既知の値であるため、店舗単位で一律に割り引く根拠はない。AT/ART機の上乗せ抽選で実戦値が理論値を下回る可能性はあるが、根拠あるデータ(実戦値)が無い限り補正は入れないこと。

### 予測ログ系テーブルのリーク禁止・append-only制約(機能B再設計Stage7、2026-07追加)
`prediction_log`・`pattern_history`は`DELETE`しない(既存の`store_profile`・`stage3_scores`・`teppan_conditions`の「全削除→再挿入」方式と挙動が異なる点を混同しないこと)。`predict_next_day`・`prediction_log`書き込みは「使用データ最終日より後の情報」を一切使わず、実測差枚との突合は必ず評価専用スクリプト(`evaluate_predictions.py`)側で行う(予測計算コード`patterns.py`には実測値を渡さない)。`使用データ最終日`は必ずその予測計算に使ったdfの実際の最大日付を取得して記録すること(ハードコードやその場の`today()`は使わない。手動実行が数日遅れることがあるため、実行日時と使用データ最終日は別概念)。

---

## 関連ファイル

- 要件定義 → `要件定義.md`
- 構成図 → `fase2/データ分析_構成図.md`
- 設計メモ(原典) → `設定解析_穴場発掘ツール_設計メモ_v0_6.md`(v0.6)
- 今後の実装予定(機能B再設計Phase6・上限モデルStep2以降) → [`fase2/今後の実装予定.md`](今後の実装予定.md)
