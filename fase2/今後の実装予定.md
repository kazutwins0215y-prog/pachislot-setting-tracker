---
name: fase2-今後の実装予定
description: 機能B再設計・店舗高設定上限モデルのうち未着手の将来項目の集約。実装済み部分はデータ分析_skill.mdへ統合済みのためここには含めない。
metadata:
  type: roadmap
  status: 未着手
  作成日: 2026-07-07
  統合元: 追加検討_機能B再設計.md（2026-07-06時点でPhase1〜5実装済み）/ 追加検討 店舗高設定上限モデル.md（同時点でStep1実装済み）
---

# fase2 今後の実装予定

> 2026-07に検討・一部実装した2件の追加検討（機能B再設計・店舗高設定上限モデル）のうち、
> **実装済みの内容は[`データ分析_skill.md`](データ分析_skill.md)に統合済み**。
> 本ファイルは両検討に残っている**未着手の将来項目のみ**を集約したもの。
> 実装時はこのファイルの該当セクションを参照し、着手後は完了内容を`データ分析_skill.md`・
> [`実装チェックリスト.md`](実装チェックリスト.md)へ転記のうえ、本ファイルから当該項目を削除すること。

---

## 1. 機能B再設計 Phase6（将来項目）

実装済みのPhase1〜5（pattern_history・符号付きスコア・Stage7-0〜7-2・トップページ・店舗プロファイル拡張）は
`データ分析_skill.md`参照。以下はそこから意図的に切り離した将来項目。

### 1.1 Stage7-4: ウォークフォワードα学習への置き換え

`prediction_log`に長期/短期の生コンポーネント値が数十〜数百件溜まった時点で、既存の
`patterns.walk_forward_alpha`（現在未使用）をこのログに対して実行し、`FIXED_ALPHA`(=0.3)を
学習値に置き換える。

- 前提: `prediction_log`が十分な件数（目安30〜数百件）に達していること。2026-07-06時点で388〜727行程度蓄積されているため、着手判断はサンプル数と評価結果（下記1.4のprediction_accuracy）を見て行う
- 同じ評価ハーネスを、w3（回転数チャンネル）再検証（LOSO）・新規パターン採否判定・後述2節（上限モデルStep2以降）の検証にも共通利用する想定

### 1.2 Stage7-5: S_ローテの「非該当日」判定・翌日予測拡張

深さ型のうち鉄板台・据え置き（**2026-07-08 タスク3で実装済み**。`データ分析_skill.md`参照）は
符号付き化・翌日予測に対応済み。ローテ（`score_rotation`）は現状ユニット単位で1つの定数値を
全期間に適用しており、「日単位の該当/非該当」という概念自体が実装されていないため、
以下が未確定のまま残っている。

- ローテ: 「その日、集中先の台がどれであるか」を日次で判定するロジック（現状は窓全体のジニ係数1値のみ）
- 上記が確定した後、`prediction_log`の`予測種別`を`'S_ローテ'`に拡張し、Stage7-1の
  `predict_next_day`と同じ枠組み（長期/短期ブレンド・append-only・リーク禁止）で翌日投影を追加する

#### 2026-07-08 設計検討の結果（ローテ: 実証先行で保留 / 据え置き: 実装済み）

**ドメイン知識（ユーザー提供）**: ローテの集中先は1日基本1台（グループ台数が多い場合は2台）で、
約1週間の間に全台に設定が入る（同じ台に複数回入る場合もあり）。

**発見**: 現行`score_rotation`は「窓全体（60日等）の台別平均high_probのジニ係数が中程度」で
検出しているが、上記の型のローテは長期窓では台間平均が均等化するため**ジニが低く出て検出されない**
（現行検出が拾うのは「特定台群への恒常的な偏り」であり本物のローテと逆のパターン）。
検出自体の再設計（日次断面の集中 × 長期の均等 × 7〜14日で全台勝者のカバレッジ）が必要。

**方針決定**: ローテは判別が難しいため、**実際にローテを運用している店舗（正解発表あり）のデータで
検出・予測ロジックを実証してから着手する**。それまで実装は保留。

**検討済みの設計候補（実証時のたたき台）**:
- 検出: 日次で高評価台が概ねk台以下 ＋ 直近7〜14日で全台（≥80%）が勝者経験 ＋ 並べ替え検定
- 日次勝者数: k = ceil(台数÷7)（7日×k台≥全台数で一巡が成立）。勝者認定はtop-k台かつ閾値超のみ
  （「誰にも入っていない日」を勝者なしとして表現）
- 翌日スコア: 直近7日窓の未当選台に正（残り台数が減るほど大きく）、当選済み台は弱い負
  （複数回入る場合があるため強い負にしない）。周期が7日からずれる店は窓長をカバレッジ実績から推定
- 欠測日（is_invalid）: 勝者判定不能日はスキップ（履歴は保持）

**据え置き（案A「滑走窓自己相関」で実装済み。2026-07-08 タスク3）**: 実装内容は
`データ分析_skill.md`参照。案B（変化点検出/CUSUM）は`prediction_accuracy`で切断検知の遅延が
ボトルネックと判明した場合のみ追加検討する（段階投入の方針は維持）。
残調整項目（2026-07-08ユーザー合意: `prediction_accuracy`の成績を見てから判断）:
負側スコアの式がr̄_t=0で-1に飽和し「負側は控えめ」の思想より強めに効いている
（店舗平均`s_sueki`が大きく負・狙い目度が下方に引っ張られる）。調整候補は負側への
`NEGATIVE_SCALE`(=0.5)乗算による緩和。

### 1.2a トップページ「熱い台予測」7タブの暫定事項（2026-07 UIリニューアルで実装・未確定のまま残した点）

`app_top.py`の`render_hot_predictions()`（個別台/機種/ローテ/新台/増台/移動台/据えの7タブ）実装時に
判明・簡略化した点。

- **新台/増台タブの区別ロジック未実装**: `patterns.detect_events`は「店舗×機種の前日比較での
  台番号セット差分」しか見ておらず、機種が呼び出し時点で固定される(`detect_all_events`が
  店舗×機種の組み合わせごとにループ)ため、「その機種が店舗にとって初めて入った日」自体は
  検出対象に含まれない(ループが2日目以降の前日比較からしか始まらない)。結果として
  `S_新台増台`は「本当に新しい機種の導入」と「既存機種の台数追加」を区別できておらず、
  7タブの「新台」「増台」は現状同じ`S_新台増台`データを表示している(ユーザー承認済みの暫定対応)。
  区別するには`detect_events`側の改修が必要（例: レプリカ`slot_data`から機種の店舗初出日を
  別途取得し、初出日が直近か否かで判定する等）。本節1.2節（ローテ・据え置きの日次判定）とは
  別軸の課題だが、同じ「幅型/深さ型パターンの粒度を細かくする」検討として合わせて着手するのが効率的。
- **据えタブの「周期条件」は不採用**: 元プランは「S_据え置きが強い店で周期条件を満たす場合のみ」
  だったが、`score_sueki_daily`(平滑化lag-1自己相関)には離散的な周期検出が存在しないため、実装では
  スコア閾値のみで判定している(2026-07-08 タスク3でS_据え置きが日次符号付き値になったことに伴い
  `_SUEKI_SCORE_GATE`も0.5→0.0に変更済み。データ分析_skill.md参照)。
- **7タブの暫定閾値**（`app_top.py`の`_RELIABILITY_GATE`=0.4・`_BREADTH_SCORE_GATE`=0.3・
  `_SUEKI_SCORE_GATE`=0.0、店舗単位の信頼度ゲートは`store_profile`の該当パターン行を参照）は
  実データ運用しながら調整する前提。精度検証・閾値調整は本節1.2節と合わせて行う。

### 1.3 Stage7-X（保留・優先度低）: 週次バックテスト再生

過去日付を人工的に区切って再計算しウォークフォワードのサンプルを増やす案。計算コストに加え、
`kaiten_bin_curves.json`等「1回学習・全店舗再利用」ファイルの過去時点バージョンとの整合が
複雑になるため保留。Stage7-4（実運用データの自然蓄積によるα学習）で収束が遅すぎると
判断された場合のみ検討する。

### 1.4 未確定・実装時に詰める項目（機能B再設計から持ち越し）

1. トップページの機種名/台番号レベルの候補出しロジック: **2026-07-08実装済み（個別台タブのみ）**。
   `app_top._reason_text`が`teppan_conditions`の経路/条件/効果量から短文（「カレンダー: 末尾3に該当」
   「周期: 7日周期を検出済み」等）を生成し、`_render_prediction_tabs`が個別台タブのテーブル下に
   `st.caption`で行ごとに表示する（テーブル列には追加しない）。
   据えタブ（r値→「継続強度」表現）等への拡張は今後の課題として残る（ローテタブは1.2節のローテ
   再設計待ちのため引き続き対象外）
2. 深さ型「非該当日判定」の具体的な閾値・ロジック（据え置きは2026-07-08 タスク3で実装済み。
   ローテは1.2節のローテ再設計待ちで個別設計が必要）
3. 符号付きスコア導入に伴う`app_a.py`側の表示レンジ見直し範囲: `app_b.py`は`range_x=[-1,1]`・
   `RdBu`配色へ対応済みだが、`app_a.py`の「統合スコア(0〜6)」はhigh_prob由来の別指標のため
   対応不要と判断済み。ただし将来`app_a.py`側で符号付きサブスコアを直接表示する要望が出た場合は
   再検討が必要
4. S_ローテの「継続する」という前提の具体的な定式化（1.2節のStage7-5着手時に必要。
   据え置きは2026-07-08 タスク3で実装済み）
5. トップページへの遷移予測(予測種別`'遷移予測'`)の表示: `app_top._load_latest_predictions`は
   現在`予測種別='S_鉄板台'`のみ表示(スコアのレンジ・意味が異なる2種を同一ランキングに
   混在させないため)。遷移予測の精度が`prediction_accuracy`で確認できた段階で、別枠表示
   (全台カバレッジの翌日高設定確率ランキング)を検討する
6. 店舗の癖（据え/上げ/下げ）の表示UI: 2026-07-08 タスク3追記(c)で`store_profile`に
   遷移_ベース率/遷移_p_stay/遷移_p_up/遷移_ペア数を記録済み（記録のみ、表示は未実装）。
   店舗横断比較にはλ=p_stay−p_up（πの影響が打ち消される[-1,1]の共通スケール）、
   店舗個別表示にはp_stay/p_up/(1−p_stay)の3値＋πとの差を使う設計（データ分析_skill.md参照）

### 1.5 遷移モデル(Stage7-3)の条件付き拡張（2026-07-07追加・検討事項。前日差枚条件付きは2026-07-08 タスク4で実装済み）

無条件版(v1: 店舗単位の2状態マルコフ遷移、`patterns.estimate_transition_matrix`)は実装済み
（`データ分析_skill.md`「遷移モデルによる全台翌日予測」参照）。実装時の実データ検証で
**全店舗が日次でほぼ無記憶**（p_stay≈p_up、当日high_prob→翌日実測差枚のSpearman≈0）と
判明したため、無条件版の予測力はほぼ期待できない。以下の条件付き拡張が本命となる。

- **前日差枚条件付きの下げ**: **2026-07-08 タスク4で実装済み**
  （`patterns.estimate_transition_matrix_stratified`・並走ログ`予測種別='遷移予測_前日差枚'`。
  詳細は`データ分析_skill.md`「遷移モデルの前日差枚条件付き拡張＋並走ログ基盤」参照）。
  実データ9店舗中3店舗（yasuda7・グランパ中野・マルハン新宿東宝ビル店）で層間差が有意、
  動機となったエスパス日拓新宿歌舞伎町店自体は非有意（p=0.134）という結果だった。
  採否は`prediction_accuracy`の成績蓄積を待って判断する
- **カレンダー条件付きの上げ**: `teppan_conditions`（検出済みの特定日条件）に翌日が該当する
  場合にP(低→高)を引き上げる（S_鉄板台予測との統合になるため、二重計上に注意。回避の
  設計（(a)鉄板台検出台は遷移側で条件適用しない排他方式、(b)鉄板台翌日予測を遷移モデルへ吸収、
  の2方向）が必要。前日差枚版のゲート通過後に着手する想定は維持）
- **ローテ連動**: S_ローテ検出済みグループ内で「昨日入った台のP(高→低)を上げ、
  兄弟台のP(低→高)を上げる」（Stage7-5のローテ日次判定の設計と共通の前提が必要。1.2節の
  ローテ実証先行の決定により後回し）
- 採否判断: 拡張は1つずつ入れ、`prediction_accuracy`の遷移予測(予測種別`'遷移予測'`)の
  成績改善でゲートする（一気に盛るとどれが効いたか分からなくなるため）。前日差枚版で構築した
  並走ログの仕組み（無条件版と条件付き版を同時記録し`evaluate_predictions.py`の
  (ホール名,予測種別)別集計で対比較）は、カレンダー条件・ローテ連動・1.6/1.7節にもそのまま再利用する

### 1.6 チャンネル重みw1/w2・サブスコア重みwᵢのウォークフォワード学習（2026-07-07追加・検討事項）

> 精度改善検討（2026-07-07）の推奨順3番。実装は保留し検討事項として記録。

手置きのままのパラメータを、リークなしの学習で置き換える。

- **w1/w2（Stage3チャンネル重み、台×日単位)**: 現在w1=1.0/w2=0.5は暫定値
  （`stage3_channel_weights.json`）。教師を「同一台の翌観測日の実測差枚率」にした
  ロジスティック回帰/順位回帰で学習する構想だったが、2026-07-07の検証で
  当日high_prob→翌日差枚のSpearmanがほぼゼロと判明しており、**翌日教師では
  信号が薄い可能性が高い**。設計時は(a)同日LOSO（店舗除外のout-of-sample）教師、
  (b)当日判別（機能A用途）への効果、の2軸で再検討する
- **wᵢ（weights.jsonのサブスコア重み、店舗×日単位)**: 現在は均等重み＝完全未調整。
  教師を「翌日の店舗実測差枚率」にしたSpearman最大化で店舗ランキングの精度向上を狙う。
  こちらは店舗集計により台単位ノイズが平均化されるぶん翌日教師でも信号が残りやすい
- 共通の前提: 評価ハーネス（`prediction_log`/`prediction_accuracy`）の毎日運用で
  検証サンプルが蓄積されていること。学習はウォークフォワード/LOSO必須
  （循環学習・当日リークの再発防止。bin_curves旧実装・旧α学習の教訓）

#### 2026-07-08 設計確定（実装は未着手）

**w1/w2: 「正解発表店舗データ待ち」へ分類変更（それまでw1=1.0/w2=0.5の現状維持）**
- 上記の「(a)同日LOSO教師」案は検討の結果**棄却**: 教師の同日実測差枚はチャンネル1（差枚logLR）の
  入力そのものであり、学習が「差枚で差枚を当てる」退化解（w1最大・w2→0）に収束する。
  LOSOは店舗固有クセへの過学習は防ぐが「差枚→差枚」の自明な関係は全店舗で成り立つため循環は解消しない
- 翌日教師も当日high_prob→翌日差枚のSpearman≈0（判明済み）のため信号がほぼない
- 循環も信号薄もない教師は**正解発表店舗の真の設定ラベル**のみ。1.2節ローテ実証用に追加予定の
  店舗（正解発表あり）と同じデータでw1/w2キャリブレーションも検証する（一石二鳥）。
  1店舗の正解から他店舗への一般化可否はその際に確認する

**wᵢ: 設計確定（教師=翌日の店舗実測差枚率、原案どおり）**
- 学習器: **Spearman直接最大化**（座標降下またはグリッド探索）。目的関数を
  `evaluate_predictions.py`の評価指標と一致させ、外れ値（爆発日の差枚）に頑健にする
- 粒度: **共通重み＋店舗別の有効/無効ゲート**（段階投入）。重みの値は全店舗共通で学習し、
  店舗×サブスコアごとに「翌日店舗差枚と有意に相関するか」を検定して有意でない店舗では
  そのサブスコアの重みを0にする（効く店だけで効かせる。w3のLOSOゲート・1.5店舗別有効化と同思想）。
  第2段（条件付き）: 並走ゲート採用後、エスパス型の逆信号ケースが目立つなら符号反転を許可→
  それでも不足なら階層型（共通＋店舗別補正の縮小推定）へ発展
- 学習データの出所: **日次スナップショットのappend-only蓄積**（当日のサブスコア・合成スコアを
  日々記録し、溜まった分だけで学習）。過去日の一括再計算による疑似履歴は`kaiten_bin_curves.json`等の
  当時バージョン問題で当日リークが混入するため不採用（1.3節と同じ理由）
- 採否ゲート: **均等重みとのウォークフォワード並走対比較**（学習重み版と均等重み版の店舗ランキングを
  同一期間で記録しSpearmanで上回った場合のみ採用。1.5の並走ログ基盤を再利用）
- 実務上のポイント: 学習自体はデータが貯まってからだが、**記録側（日次スナップショット蓄積）は
  先行実装する価値がある**（蓄積開始が遅れるほど学習開始も遅れる。据え置き・1.5の実装と合わせて
  記録側だけ先に入れる選択肢あり）

**[2026-07-09 タスク5実装済み] 記録側のみ先行実装完了**
- `fase2/score.py`に`score_snapshot`テーブル（店舗×使用データ最終日で1行。各サブスコアの
  当日断面店舗平均・狙い目度_店舗平均・実効重みJSONを保存）と`write_score_snapshot`
  （(ホール名,使用データ最終日)重複ガード付きappend-only）を追加、`run_store_profile.py`の
  `synthesize`直後から呼ぶようにした
- **学習側（Spearman直接最大化・店舗別ゲート・均等重みとの並走対比較）は未着手のまま**
  （本節に残す。蓄積が十分溜まってから着手）

### 1.7 GBDTによる融合層の置き換え（2026-07-07追加・検討事項）

> 精度改善検討（2026-07-07）の推奨順5番。1.5〜1.6の後にハーネス上でまだ天井が
> 見えた場合のみ着手する条件付き項目。

- 手置き/線形の融合（w1/w2/w3・wᵢ・α）を、勾配ブースティング（LightGBM等）による
  非線形融合に置き換える。入力特徴量は既存資産をそのまま使う:
  各logLR・サブスコアSᵢ・曜日/日付末尾・γ_store・稼働・遷移モデルの翌日事前確率
- データ規模（6店舗×台日数十万行）はGBDT向き。深層学習が活きる規模ではない
- 目的関数はランキング（LambdaRank）またはSpearman/Precision@N評価
  （`evaluate_predictions.py`の既存指標と一致させる）
- 期待値は控えめに見積もる（順位相関で数ポイントの上積み程度）。検証は
  ウォークフォワード分割必須
- 教師が「翌日の差枚」という高分散の代理ラベルである以上、到達精度には天井がある
  （1.5節の無記憶性の発見も参照）

---

### 1.8 機種単位・台番号末尾・店舗イベント傾向の検知拡張（2026-07-08追加・検討事項）

ユーザー提供のドメイン知識（明確な情報・実データでも確認可能とのこと）:
- マルハン新宿東宝ビル店・エスパス日拓新宿歌舞伎町店: **6のつく日は台番号末尾6が高配分**
- マルハン新宿東宝ビル店・BIGディッパー: **毎日必ず1機種以上の全台系がある**
- 移動台・増台・新台は店舗傾向が顕著（**新台が強い店は毎回強い**）

現行実装とのギャップ（2026-07-08確認）:
1. **機種単位のカレンダー/周期検知が未実装**: `score_zentaiki`は当日記述のみ（1日分で計算・
   日付条件なし）。「この機種は特定日に全台系」「この店は毎日どれかの機種が全台系」という
   構造の検出・翌日予測（明日の全台系候補機種）は存在しない。機種レベルのローテとも言える
   「毎日1機種以上」制約は翌日予測の強い事前情報になり得る
   （**日次判定側は2026-07-09にPhase1実装済み。詳細は`データ分析_skill.md`「機種強さ軸・
   全台系/高配分の日次判定」参照。カレンダー/周期構造の学習は下記「次回分（末尾版）」の対象**）
2. **1/2全台系は現行で不利**: S_全台系=平均×(1−std/0.5)は「揃い」を要求するため、機種内
   半分だけ高い日はstdが大きくスコアが逆に下がる。全台系と鉄板台の谷間に落ちる
   （**「高配分」ラベルとして2026-07-09にPhase1実装済み。上記1と同じ節参照**）
3. ~~**台番号末尾検知が未実装**: 機能Aの表示フィルタのみ。鉄板台カレンダー経路が台ごとに
   個別検出することはあるが、末尾グループとして証拠を束ねないため検出力が低く、
   「日付末尾＝台番号末尾」の構造ルールを表現できない。末尾グループ単位の検定
   （曜日/日付末尾×台番号末尾のクロス検定）を追加すれば検出力が大幅に上がる見込み~~
   **【2026-07-10フェーズ1〜4で実装済み。詳細は`データ分析_skill.md`「末尾版: 台番号末尾
   グループのカレンダー構造検出」参照】**
4. **イベント（新台/増台/移動台）の店舗事前傾向priorが未実装**: `S_新台増台`/`S_移動台`は
   イベント後の実績追従（トレンド−基準値）のみ。「この店は新台に毎回入れる」という
   店舗傾向を`store_profile`から事前確率としてイベント初日に付与する仕組みがない
   （信頼度は7タブの表示ゲートにのみ使用中）。1.2a節の新台/増台の区別ロジックと同時に
   設計するのが効率的
- ローテ補足（ユーザー提供）: ローテは周期性より**「当日にローテ期間がかぶっているか」**が
  重要 → 1.2節の再設計たたき台に「ローテ期間の開始/終了（アクティブ期間）の判定」を
  観点として追加（周期検出流用C案を不採用とした判断とも整合）
- 検証の足がかり: マルハン新宿東宝・bigディッパー東中野は**収集済み**のため、「6の日末尾6」
  「毎日全台系1機種以上」は手元のレプリカで即・実データ検証できる（3節の正解発表入力とも
  相互検証可能）。着手時はまずこの検証から入る

#### 2026-07-09 実装済み: 機種強さ軸＋全台系/高配分の日次判定 Phase1

上記ギャップ1・2への対応方針をユーザーと合意し、Phase1として実装済み。発端は「機能Aの統合
スコア機種平均が事前確率π(=0.15)に引き戻されて常に1.4前後で横ばいになり、機種ごとの強さが
読めない」という観察（仕様であることは確認済み。較正された事後確率の群平均はπに収束するため）。

基本設計（zスコア/投入率/S_全台系の3役分担）・判定ラベル（全台系/高配分/普段どおり）の定義・
`machine_judgment_log`テーブル・機能Aの「設定投入率」軸は実装済み。詳細は
`データ分析_skill.md`「機種強さ軸・全台系/高配分の日次判定」「機種×日 全台系/高配分の判定ログ」・
実装チェックリスト.md H-14参照。

**残る項目（Phase2・未着手）**: 閾値（z≥2・S_全台系≥0.5）は固定値のままで、実測との突き合わせに
よる調整は`machine_judgment_log`の蓄積を待つ（`evaluate_predictions.py`の枠組みを流用、
ウォークフォワード限定）。**Phase2の格上げ（2026-07-09合意）: 3節の正解発表ラベルを閾値調整の
直接教師に使う**。`発表タイプ`が判定ラベルと対応する（全456/全6→全台系、半数456→高配分(1/2)、
台数指定→投入率=k/n、通常日→普段どおりの弱い負例）。特に台数指定・全台発表日は
**投入率推定Σp/nの較正**（全台系日に表示値がどこまで届くか＝軸の実効上限の定量化）にも使える。
片側ラベルの制約（発表なし≠負例。測れるのは検出漏れ率が主、誤検出側は理論値＋通常日発表で補完）と
発表店舗→非発表店舗への一般化確認（1.6節w1/w2と同じ注意）は3節の設計思想に従う。
較正用2店舗（マルハン新宿東宝・bigディッパー東中野）は収集済み・即入力可、かつBIGディッパーは
毎日全台系ありの店のためラベル蓄積が速く、受け身のPhase 2より早期に閾値調整へ入れる見込み。

**次回分（末尾版）: 2026-07-10設計確定・同日フェーズ1〜4実装完了（検定はA案=Mann-Whitney U相対検定に変更して確定）。実装済み内容は`データ分析_skill.md`「末尾版: 台番号末尾グループのカレンダー構造検出」に統合済み**

同じ3役分担の機械を、グループ定義を「機種」から「台番号末尾（店舗内全台横断）」に差し替えて
適用する（z・投入率はグループの集合定義に依存しないためそのまま流用可能）。以下すべて
2026-07-10ユーザー合意済みの確定設計。

- **グループ**: 台番号末尾0〜9＋台番号ゾロ目（全桁同一: 11,22,…,99,111,222…）の11グループ。
  ゾロ目該当台が存在しない店舗ではグループ自動スキップ。グループ定義は関数パラメータ化し、
  後述「機種単位の癖分析」で「グループ=機種」に差し替えて検出器を共通利用する
- **レイヤー1（日次検出）**: 末尾グループごとのz・投入率。11グループ同時検定のFDRはBH補正で制御
- **レイヤー2（カレンダー構造の学習・末尾版の本体）**: 「日付条件×台番号末尾」の組み合わせごとに、
  グループの日次投入率系列（グループ×日のΣhigh_prob÷n、is_invalid除外）を
  **「候補日 vs 非候補日」のMann-Whitney U片側検定(greater)** にかけ、全仮説一括のBH補正＋
  効果量ゲート（rank-biserial相関 rbc≥`EFFECT_SIZE_THRESHOLD`(0.3)）で判定する
  （既存`patterns.calendar_test`と同一方式・同一の物差し。鉄板台カレンダー検定とロジックを共通化できる）。
  **【2026-07-10フェーズ1で当初案のStouffer統合z（`z=(Σp−nπ)/√(nπ(1−π))`をΣz÷√kで統合）から
  変更確定】**: 店舗全体の平均high_prob（マルハン0.260/エスパス0.236）がπ=0.15より系統的に高い
  （sigmoid飽和の右裾。中央値は0.12〜0.13でπ付近）ため全198仮説が有意化して閾値として破綻。
  順位ベースの相対比較に変えると店舗オフセットが自動相殺される（詳細は下記「フェーズ1検証結果」）。
  日付条件は既存の`patterns.calendar_candidates`（18候補: 曜日7＋日付末尾10＋日付ゾロ目1。
  旧記述の「17候補」は数え間違い）に**毎月X日(31)を加えた49候補**へ拡張し、
  **鉄板台カレンダー検定にも同じ49候補を適用する**（鉄板台の検出結果が従来と変わり得る点・
  仮説数増によるBH検出力の微減はユーザー許容済み）
- **一致ルール2本を追加**（固定グループ×固定条件では表現できない動的グループを、構造ルール
  1本=仮説数+1で表現する）: (a)「日付＝台番号下2桁の一致」（例: 12日に末尾12番台が高配分）、
  (b)「日付末尾＝台番号末尾の一致」の全末尾統合版。毎日該当日があるため蓄積も速い。
  **【A案採用に伴う補足】** 一致ルールは「毎日が該当日」のため候補日vs非候補日の構図が
  成立しない。日ごとの「一致グループ投入率 vs 同日の非一致グループ平均投入率」の対応ペアに対する
  **Wilcoxon符号順位検定(片側)** を使い、同じBH補正・効果量ゲート（対応あり版rbc）に載せる
  （検定の詳細設計はフェーズ2実装時に確定）
- **最低該当日数**: 候補日5日未満**または対照日5日未満**の条件は検定対象から自動除外
  （`calendar_test`と同じ5/5基準。品質ガード兼・仮説数の自動削減。
  毎月X日条件は月1回のため5ヶ月分の蓄積が必要になる点は許容済み）。「何日で確実性を持つか」は
  現状不明のため**検出は緩く保存・使用側でゲート**の方針: テーブルに該当日数k・生p値・
  効果量rbc・BH有意フラグを全保存し（A案採用により統合z列は廃止）、表示/予測側のゲート
  (kと有意性)は可変の暫定値として持つ。将来は`'S_末尾'`
  並走ログのk帯別的中率（`evaluate_predictions.py`流用）でゲートを学習（7タブ暫定閾値と同じ運用思想）
- **重複条件の統合**: 毎月6日⊂日付末尾6⊂一致ルール、と条件が入れ子になるため、保存は全条件を
  残し、予測時は同一グループへの該当条件のうち**max(効果量)を採用**（加算しない=二重計上回避）
- **検出窓**: 全期間で開始（検証対象の癖は恒常的・毎月X日条件は短窓では貯まらないため）。
  非定常（推し末尾の月替わり）が問題化したら窓/EWMA導入を後日検討（据え置きの前例踏襲）。
  テーブルは窓変更に耐える構造にしておく
- **保存先**: 新テーブル`group_calendar_conditions`（ホール名/グループ種別='台番号末尾'/
  グループキー/日付条件/p値(BH補正前の生p値)/効果量(rank-biserial相関)/該当日数/
  BH有意フラグ/使用データ最終日。A案採用により統合z列は持たない）。グループ種別列により
  機種癖分析のカレンダー癖を後から行追加だけで収容できる（store_profileへのJSON列案は棄却）
- **翌日予測への接続**: 並走記録のみ。予測種別`'S_末尾'`を`prediction_log`へappend-only記録
  （リーク禁止・既存`write_prediction_log`の重複ガード流用）し、**合成スコア（狙い目度）へは
  混ぜない**。`prediction_accuracy`で的中実績を確認してから、合流と鉄板台カレンダー経路との
  排他設計を判断する（遷移予測と同じ運用。1.5節の並走ログ基盤を再利用）
- **検証**: マルハン新宿東宝・エスパス歌舞伎町の「6の日末尾6」（ユーザー確認済み・データ収集済み）で
  レイヤー2の答え合わせから入る（**2026-07-10実施済み・合格。下記「フェーズ1検証結果」参照**）。
  「毎日全台系1機種以上」（マルハン新宿東宝・BIGディッパー）は
  `machine_judgment_log`が貯まれば集計だけで検証可能
- **実装フェーズ（フェーズ1〜4は2026-07-10完了。実装済み内容の一次情報は`データ分析_skill.md`
  「末尾版: 台番号末尾グループのカレンダー構造検出」参照）**:
  1. ~~検証スクリプト（スクラッチパッド）: 上記2店舗の実データでレイヤー2が「6の日×末尾6」を
     有意検出できるか単発で答え合わせ~~ **【完了・合格。検定はA案に変更確定】**
  2. ~~検出側本実装: `calendar_candidates`の49候補拡張＋末尾レイヤー1/2＋一致ルール＋
     新テーブル＋`run_store_profile.py`組み込み＋鉄板台の49候補切替~~ **【完了。ただし
     レイヤー1(日次のz判定)は今回は実装せず、レイヤー2(Mann-Whitney U)が生の投入率のみで
     完結するため見送った(下記「フェーズ2実装時の判断」参照)】**
  3. ~~`'S_末尾'`並走記録~~ **【完了。`patterns.predict_tail_group_next_day`+
     `run_store_profile._run_tail_group_predictions`】**
  4. ~~UI軸置き換え（下記備考）~~ **【完了。`app_a.py`機種名ビューの統合スコア軸を廃止】**
  5. ドキュメント連動更新（データ分析_skill.md・実装チェックリスト.md・本ファイル・CLAUDE.md
     ※本項目のみ現在進行中）

**フェーズ2実装時の判断: レイヤー1(日次のz判定)は実装を見送った**

設計時点では「レイヤー1(グループ×日のz)がレイヤー2の入力になる」想定だったが、レイヤー2の
検定をA案(Mann-Whitney U)に変更した結果、レイヤー2はグループ×日の**生の投入率**だけで完結し
zスコアを経由しなくなった。レイヤー1を独立実装する価値があるのは「今日は末尾X番が熱い」という
単日表示(フェーズ4以降のUI検討事項)に使う場合のみのため、今回は過剰実装を避けて見送った
(実データ確認: レイヤー1にπ=0.15をそのまま使うと1日ごとの判定で40〜62%が「異常」判定される
過剰検出が起きることを確認済み。単日表示が欲しくなった時点でπ→店舗実測平均に差し替える
最小修正(B案相当)で対応する)。

**フェーズ1検証結果（2026-07-10実施・検定A案で確定）**

マルハン新宿東宝ビル店（186日分）・エスパス日拓新宿歌舞伎町店（190日分）の実データ、
11グループ×18日付条件=198仮説で答え合わせを実施した。検証スクリプトはスクラッチパッドの
使い捨て（保存しない）。

- **当初設計のStouffer統合z（π=0.15）は破綻**: 両店舗で198/198仮説が有意化。原因は
  (a)店舗全体の平均high_probがπより系統的に高い定数オフセット（sigmoid飽和により平均0.24〜0.26、
  中央値0.12〜0.13。台単位の分布は全店舗共通で、店舗差は機種平均を取ったときの「揃い」の頻度のみ）、
  (b)Bernoulli分散仮定が実分布（右裾の重い連続値）と合わず分散を過小評価、の複合
- **3案を実データで並走比較**: A案=Mann-Whitney U相対検定（候補日vs非候補日の日次投入率の順位比較）/
  B案=πを店舗実測平均に置換したStouffer / C案=日次の「グループ−店舗全体」差分zのStouffer統合。
  有意数はマルハン5/5/6、エスパス6/4/11。**3案とも本命「末尾6×6の日」を全仮説中1位で検出**
- **A案採用の決め手**: 偽仮説「末尾6×金曜」（6の日が金曜に落ちる月の漏れ込み。B案z=3.45・
  C案z=4.63で有意化）を効果量ゲート（rbc<0.3）で唯一抑制できた。既存`calendar_test`と
  同一方式のため二重実装がなく、順位ベースで分布の歪みに頑健
- **本命の実測値**: マルハン p=1.15e-12・rbc=0.984（候補日中央値0.482 vs 対照日0.254）、
  エスパス p=4.77e-13・rbc=0.999（0.367 vs 0.229）
- **副産物（新発見の癖）**: **ゾロ目台×ゾロ目の日(11日・22日)が両店舗で有意**（rbc 0.76〜0.78、
  末尾6に次ぐ強さ。ユーザー確認済み: 末尾6と同等の鉄板とのこと）。エスパスでは
  末尾9×9の日・末尾9×3の日（rbc≈0.52）も検出
- **フェーズ2への注意事項**: レイヤー1（日次検出のz）は現行の機種判定Phase1と同じπ方式のままだと
  同じ店舗オフセットの影響を受ける（強い日は全グループのzが底上げされる）。フェーズ2実装時に
  「対店舗当日比（C案型の日次差分z）」への変更を検討する（機種判定側のPhase2較正と同じ枠組みで
  合わせて判断）

**備考: 「設定投入率」軸は統合スコアの機種平均と数値的に同一（2026-07-09ユーザー指摘）**

投入率=Σhigh_prob÷n は機種内のhigh_prob平均そのものなので、投入率×6 は機種名ビューの
既存「統合スコア」軸（機種ごとの`mean(high_prob×6)`）と同じ曲線を描く（差はis_invalid除外と
丸めの微差のみ）。新軸の付加価値は曲線の形ではなく、(a)軸の意味づけ（0=なし/3=半分/6=全台＋
普段どおり0.9の基準線）、(b)zゲート（有意でない点のグレー・小マーカー表示）、
(c)ツールチップ（z・期待高設定台数・台数・判定ラベル）にある。
結果として機種名ビューに同一曲線の軸が2本並ぶ重複があった。**UI整理は2026-07-10決定・実施済み:
機種名ビューの「統合スコア」軸を「設定投入率」軸で置き換えた**（投入率軸はzゲート・基準線・
ツールチップ付きで上位互換のため。台番号ビューは個別台の生値のため重複がなく統合スコア軸を維持。
上記実装フェーズ4で完了）。

**次回分（追加）: 機種単位の癖分析（2026-07-09合意・2026-07-10設計確定・看板機種＋機種カレンダー癖は実装済み）**

個別台で判別していた癖（カレンダー・周期・ローテ等）を機種単位に拡張する。末尾版と同じ
「グループ×日付条件のクロス検定」の機械（`patterns.build_group_calendar_conditions`）を、
グループ定義=機種で再利用する（実装の一次情報は`データ分析_skill.md`「機種版: 看板機種＋
機種カレンダー癖の検出」参照）。

対象と検出の型（着手は低リスク順）:
1. **看板機種**（恒常的に強い機種）: **実装済み**。当初案（店舗×機種の長期平均投入率が
   店舗πより有意に高いかの二項検定）は末尾版フェーズ1で判明したStouffer破綻と同型のリスクが
   あるため**案A=Wilcoxon対応ペア検定**（自グループvs同日の他グループ平均、一致ルールと同じ
   思想）に変更して実装（`patterns.group_constant_test`）
2. **機種カレンダー癖**（特定日にこの機種）: **実装済み**。当初案の「末尾版レイヤー2と同じ
   Stouffer統合z＋BH補正」は末尾版フェーズ1でA案（Mann-Whitney U相対検定）に変更確定済みのため、
   機種版もA案の`group_calendar_test`をそのまま流用（`build_group_calendar_conditions`の
   `group_type='機種'`呼び出し）
3. **新台/増台/再導入/移動台の「導入後の扱い」**: 未実装。下記「第2弾の設計メモ」参照
4. **おすすめ機種週間・機種ローテ**: 未着手・保留。投入率系列の「数日連続高→別機種へ交代」＋
   機種間排他性の検出。最難のため正解発表店での実証先行（台ローテ=1.2節と同じ判断）。
   「毎日1機種以上全台系」制約（ギャップ1）と同族
- 対象外: **2〜3台設置機種のローテ**は機種集計の恩恵がなく（n小でノイズ低減が効かない）、
  1.2節の台粒度ローテ再設計に任せる。検定側は日次有効台数中央値2台未満の機種を除外
  （`patterns.MACHINE_GROUP_MIN_UNITS`）

**2026-07-10設計確定事項（実装済み部分）**:
- **検定方式は末尾版のA案（Mann-Whitney U）をそのまま踏襲**（当初案のStouffer統合zは
  末尾版フェーズ1で破綻済みのため機種版でも不採用。看板機種の恒常検定のみ「毎日が該当日」の
  構造のため一致ルールと同じWilcoxon対応ペア方式）
- **2窓検定並走（案A）**: 恒常窓（全期間）＋直近`RECENT_TEST_WINDOW_DAYS`(=90)日窓の2本を
  独立にBH補正し`group_calendar_conditions`へ保存（グループ種別`'機種'`/`'機種_直近'`）。
  個別台側の鉄板台等が採用する「検定は全期間1本＋予測時に長期/短期αブレンド」とは異なる方式を
  機種版に採用した理由: 機種の癖は個別台より非定常（推し機種の月替わり）なリスクが高く、
  αブレンドは「条件が消えたこと」を表現できない（弱い効果量投影が残り続ける）のに対し、
  2窓検定は直近窓で非有意になれば明示的に「今は該当しない」と分かる。個別台側への遡及適用は
  機種版の実証結果（`prediction_accuracy`でS_機種_直近が的中率を上げるか）を見てから判断する
  （台単位は1点あたりのノイズが大きく90日窓でも検定力不足の懸念があるため）
- **n≥2ゲート・使用側ゲート方針**: 検定対象は日次有効台数中央値2台以上。ユーザーからの
  精度懸念（n=2はノイズが粗いのでは）に対し、末尾版のゾロ目グループ（該当台少数でも
  rbc0.76〜0.78で検出成功した前例）を根拠に「検出は緩く保存・使用側でゲート」の既存方針を
  維持することで合意。機種の台数中央値を`group_calendar_conditions.台数中央値`列に保存し、
  表示/予測側の追加ゲート（暫定n≥3等）は蓄積後にこの値を見て調整する
- **実データ検証結果**: `データ分析_skill.md`「機種版」節の「実データ検証結果」参照
  （マルハン・bigディッパーとも効果量0.3〜0.6の妥当な範囲で検出、Stouffer破綻の再現なし）

**第2弾の設計メモ（2026-07ユーザー合意・未実装）: 新台/増台/再導入/移動台の「導入後の扱い」**

ユーザー指摘: 新台・増台・移動台に共通して重要なのは「導入初日に入るか」ではなく
**「導入後の一定期間、どう扱われるか」**（店によって新台は弱いが増台は強い、等の非対称性がある）。

- **判別ロジック（4カテゴリに分離、2026-07ユーザー合意）**:
  - 新台: レプリカ`slot_data`から機種の店舗初出日を取得し、`detect_events`の台数増イベント日と
    初出日が一致する場合
  - 増台: 前日在籍あり・台数が増加（初出日<イベント日）
  - **再導入: 新台とは別カテゴリとして分離（2026-07ユーザー合意）**。理由: 店舗によって
    「新台は弱いが再導入は強い」「再導入は増台と同じ扱い」など扱いが店ごとに異なるため、
    混ぜるとカーブが打ち消し合って平坦化し検出力そのものが落ちる。不在期間（暫定閾値≥7日。
    メンテ・一時撤去による数日欠測を再導入と誤判定しないためのガード）を挟んで再出現した場合に
    再導入と判定する
  - 判別不能: 初出日=店舗の収集開始日（左打ち切り。導入後カーブの学習対象から除外）
  - 分離のメリデメ（ユーザー質問への回答、2026-07）: メリットは検出力の純度（混ぜたカーブは
    平坦化し検出不能になり得る）と将来互換性（細かく保存→後で束ねるのは可能だが逆はできない。
    「検出は緩く保存」の既存方針と同じ非対称性）。デメリットはサンプル減（再導入は店舗×半年で
    数件レベルの可能性があり検定不成立の店舗が多く出る見込みだが、5日/5日ゲートと同じ扱いで
    無害化できる）と判別ロジックの複雑化（不在日数閾値が恣意的・機種名表記ゆれへの感度が
    特に高い）
- **導入後カーブの検出**: イベントタイプ（新台/増台/再導入/移動台）×経過日数ビン（初日/2〜3日/
  4〜7日/8〜14日/15日以降=平常化）ごとに、その機種の日次投入率 vs 同日の店内他機種平均の
  Wilcoxon対応ペア検定（＝看板機種検定`group_constant_test`と同じ機械の「行を経過日数ビンで
  絞った」版）。**店舗単位で学習**（イベント1回では検定にならないため、店舗内の全イベントを
  束ねてサンプルを稼ぐ。「新台が強い店は毎回強い」というドメイン知識の前提と整合）
- **1.2a節（新台/増台の区別ロジック未実装）と同時着手が効率的**: `detect_events`が機種の
  店舗初出日を検出対象に含めていない問題（ループが2日目以降の前日比較からしか始まらない）は
  本節の判別ロジックとも共通の改修が必要
- 着手順の位置づけ: 1（看板機種）・3（機種カレンダー癖）実装済み → 本節（導入後の扱い）が次点
  → 4（機種ローテ）は正解発表待ちで保留

設計上の合意事項（引き続き有効）:
- 機種単位は個別台よりむしろ検出条件が良い（1観測あたりのノイズが√n分の1。
  「サンプル数が落ちる」懸念は観測点数ではなく信号の綺麗さで相殺される）
- 仮説数は台単位より減る（機種30×条件 ≪ 台400×条件）が、合成スコアへの合流時の希釈は
  店舗別有効化ゲート（wᵢ・w3と同思想: 効く店だけ重み>0）で防ぐ。癖がある店/ない店の差も同ゲートで吸収
- リスク: 交絡・二重計上（新台ブースト×看板×カレンダーが同一機種で重なる→予測合流時に
  排他/階層設計が必要）、癖の非定常性（推し機種の月替わり→2窓検定で対応済み。導入後カーブ側は
  未対応）、稼働の交絡（薄稼働日は投入率がπへ回帰→is_invalid・有効台数ゲートを検定側に）、
  機種名寄せ（表記ゆれ・後継機で系列が切れる。特に再導入判定で感度が高い）
- 正解発表（3節）は機種単位が分母のため、機種の癖はground truthで直接検証できる

## 2. 店舗高設定上限モデル Step2以降（将来項目）

Step1（候補C・長期/短期αブレンド流用）は`データ分析_skill.md`「店舗×日 高設定台数上限
キャリブレーション」に実装済みとして記載済み。以下はそこから意図的に切り離した将来項目。

### 2.1 Step2: Kalman filterへの移行（候補D、最終形）

```python
状態_t = 状態_{t-1} + プロセスノイズ   # 月次変化を緩やかなドリフトとしてモデル化
観測_t = 状態_t + 観測ノイズ           # その日の高設定割合
Kalman更新:
  不確実性が高い(データ少) → ゲイン大 → 新しい観測を強く反映
  不確実性が低い(データ多) → ゲイン小 → ノイズに惑わされにくい
上限 = min(0.5, 状態推定値 + 安全マージン)
```

- 前提: `multi_store.py`側で店舗パラメータ用のKalman filter基盤が実際に構築されること
  （現時点で`multi_store.py`にKalman機構はまだ存在しない。γ_storeはLOSO交差検証によるスカラー
  補正のみ）。基盤ができた時点で上限推定ロジックをそちらに統合し、候補C（Step1）を置き換える
- 採用理由（候補比較時の結論）: 「データ増加で精度向上」と「時間変化への追従」を単一パラメータ
  （プロセスノイズ分散）で連続的に両立できる点が候補A/B/Cにない特性のため。候補比較の詳細は
  `データ分析_skill.md`「店舗×日 高設定台数上限キャリブレーション」参照

### 2.2 α自体の学習（候補Cの固定値からの置き換え）

Step1は指示通り`patterns.FIXED_ALPHA`(=0.3)の固定値で暫定実装した。本来の設計では、
1節のStage7評価ハーネス（`prediction_log`/`evaluate_predictions.py`）を店舗×日集計向けに
転用し、「上限推定値ベースの補正が実測差枚の予測力を上げるか」をSpearman相関で検証してから
αを決定する想定だった。

- Stage7は台×日単位（S_鉄板台の翌日予測）の評価だが、本項目は店舗×日単位（E[高設定台数]/N）
  の評価であり、集計粒度が異なる。ただし「長期版・短期版をαブレンドし、ウォークフォワードで
  実測に対する予測力を検証する」という骨格自体は共通のため、`prediction_log`のテーブル構造・
  `evaluate_predictions.py`の評価ロジックを店舗×日集計向けに転用できる（二重実装を避けるため
  新規の評価スクリプトは作らない）
- 着手前提: 1.1節（Stage7-4）と同様、評価ハーネス側の運用実績が十分に溜まってから行う

### 2.3 発動日割合の偏り（要フォローアップ・実データで確認済みの課題）

2026-07-06の実データ検証（5店舗）で、上限キャリブレーションの発動日割合が店舗により
3.8%〜82.7%と大きくばらつき、一部店舗（yasuda7: 82.7%、パーラースーパーセブン新高円寺店: 61.6%）
で非常に高頻度という結果が出た。

- 原因: 長期/短期分位点ブレンド値が絶対上限0.5を上回り、`min(0.5, ...)`により実質的に絶対上限
  0.5固定になっている（店舗ごとの動的推定が機能していない）。Stage3出力(high_prob)の日次平均が
  0.5近辺に集中していることが根本原因で、w3=0時代に見られた狭帯域問題と同型の現象
- **2026-07-07対応: Stage3に事前確率切片β₀(=ln(π/(1-π))、π=`prior_high_ratio`既定0.15)を導入**
  （`データ分析_skill.md`Stage3節参照）。根本原因だった「β₀なし=暗黙にπ=0.5」を是正した。
  残タスク:
  - 導入後の発動日割合・分位点の推移を運用データで確認し、`uplimit_config.json`の
    安全マージン(0.05)・絶対上限(0.5)を必要に応じて再調整する
  - π=0.15自体が暫定値。店舗別学習は2.1節（Kalman）と統合して検討
  - `stage3_channel_weights.json`のw3が引き続き0.0（LOSOゲート未合格）である影響の切り分け
- 判断材料が揃うまでは、`uplimit_config.json`の値は運用しながら調整する暫定値のまま据え置く

### 2.4 特異日（周年・グランドオープン等）の自動識別（案B、保留）

Step1では案A（`uplimit_config.json`の「特異日除外リスト」への手動追加、初期値は空）で暫定運用中。
案B（極端な乖離自体を「特異日候補」として別枠検出し、この制約をスキップする）は実装コスト中・
誤判定リスクありのため、案Aの運用で問題（本物の強い日が機械的に潰される事例）が顕在化してから
検討する。

### 2.5 日タイプ別（イベント日/通常営業日）の上限分割

上限を店舗単位で1本にするか、日タイプ別（イベント日/通常営業日）に分けて学習するかは未決定。
Step1実装時点ではデータ量を見て判断するとしていたが、実データでの判断はまだ行っていない。
2.3節の発動日割合の偏りの原因調査と合わせて検討するのが効率的。

### 2.6 Tier A機種によるマクロ整合性クロスチェック

Tier A機種が十分にある店舗については、マクロ整合性逆算（`multi_store.check_macro_consistency`と
同型の手法、Stage6実装済み）を上限推定のクロスチェックとして併用できないか、Phase F（複数店舗、
実装済み）の枠組みを流用して再検討できる。優先度は低い（未着手）。

---

## 3. 正解発表（ground truth）受け取り基盤（2026-07-08設計・実装は別セッション）

1.2節（ローテ実証）・1.6節（w1/w2キャリブレーション）・**1.8節（全台系/高配分判定の
閾値調整・投入率較正。2026-07-09追加）**の前提となる、店舗の設定発表を手入力で
蓄積する仕組み。発表店舗は限られる（現実的に10店舗程度）。

### ラベルの性質（設計の前提）

- **片側ラベル（positive-only）**: 発表された台=高設定は信頼できるが、発表なし=低設定ではない
  （発表なしで入っている場合もある）。学習・評価は「正例が同日・同機種の未発表台より
  high_prob上位に来るか」のランキング型で設計する
- 発表は**事後告知**（リークの心配がない純粋な検証データ。入力は夜〜翌日まとめてでよい）
- 「1/2で456」の分母は**機種単位**
- ローテ発表は**「今日はこの台に入れた」の台レベル**（日次勝者の正解が直接得られる。
  検出だけでなく翌日予測の的中評価まで正解ベースで可能）
- 媒体はLINEオプチャ画像/X画像/X文字/Webサイト等多岐だが、手入力で統一フォーマットに吸収する

### スキーマ案（1発表=1行）

日付 / ホール名(stores.json正式名に名寄せ) / 機種名(ana-slo表記に名寄せ、NULL=店舗全体) /
発表タイプ('全456'|'全6'|'半数456'|'台数指定'|'ローテ対象'|'台指定'|'通常日') / 数値(台数・割合) /
台番号リスト(台指定・ローテ日次勝者) / 設定値('456'|'56'|'6'等) /
発表タイミング('事前告知'|'当日'|'事後') / 媒体メモ / 入力日時

- 「通常日」タイプは弱い負例として貴重なので捨てずに記録する
- 保存先は**新規の専用ファイル**（例: `ホールデータ/ground_truth.db`）。`analysis.db`は
  再生成可能なローカル成果物という位置づけのため、再生成不可能な手入力データは混ぜない

### 過去ログのさかのぼり方針（決定済み）

- **ローテ実証用店舗**: 連続期間を全日入力（直近1〜2ヶ月）。勝者系列・一巡・カバレッジは
  系列データなので日を飛ばすと壊れる（無作為・一日おきは不可）
- **較正用店舗**: 発表があった日を全部。工数が厳しい場合の間引きは**無作為のみ**
  （一日おき等の規則的間引きはイベントの日付規則（偶数日・ゾロ目等）と共振して系統偏りを生むため禁止。
  入力する日は発表内容を見る前に乱数で決める＝選択バイアス防止）

### 必要量の見積り

| 対象 | 量 | 工数 |
|---|---|---|
| ローテ店舗2〜3店×直近2ヶ月連続 | 約120〜180件 | 1件30秒〜1分、計1.5〜3時間 |
| 較正店舗2〜3店×直近2〜3ヶ月の発表日 | 約100〜300件 | 計1〜3時間 |

過去ログから遡れるため、合計3〜6時間の初期入力で検証開始量が揃う。以後は5〜15分/日。
w1/w2はパラメータ2〜3個のため機種×日100〜300件で足りる。ローテ検出は一巡×4周期以上=
1店舗あたり最低約1ヶ月の連続データが必要。

### 発表店舗の候補（2026-07-08ユーザー提供＋ana-slo掲載確認済み）

- **全台発表**: マルハン新宿東宝ビル店 → **収集済み店舗**（過去データあり・バックフィル不要。較正の最優先）
- **全台・1/2発表**: BIGディッパー系列 → 東中野店は**収集済み**。他にana-slo掲載確認済み:
  門前仲町/新橋1号/戸越銀座/大井町（東京）、京都駅前/大久保（京都）
- **ローテ発表**: UNO系列 → ana-slo掲載確認済み: 三ノ輪UNO/有楽町UNO（東京）、本八幡UNO（千葉）。
  現在未収集のため、対象店舗確定後にstores.json追加＋発表期間に合わせたバックフィル収集が必要
  （`.claude/skills/add-new-store`参照。ana-sloの詳細データ保持期間は約1年1ヶ月）

### 確定事項（2026-07-08確定・実装は別セッション）

- **対象店舗（確定）**:
  - ローテ実証: **三ノ輪UNO・新橋UNO・有楽町UNO・有楽町DUO**（4店ともana-slo掲載確認済み・
    現在未収集 → stores.json追加＋発表ログを遡れる期間に合わせたバックフィル収集が必要）
  - 較正（w1/w2）: マルハン新宿東宝ビル店＋bigディッパー東中野店（どちらも収集済み・即入力可）
- **入力方式（確定）**: ローカルStreamlit入力フォーム。要件:
  - 店舗・機種・発表タイプ・**台番号**（ローテ日次勝者・台指定用）をプルダウン/マルチ選択で入力
  - **プルダウンの選択肢は「選択した日付時点」のレプリカ（slot_data）から生成する**。
    過去データの入力になるため、現在の機種構成・台番号と一致しない場合がある（ユーザー指摘）。
    「日付→ホール→その日付に存在した機種→その日付×機種の台番号」の順に絞り込む
  - その日付のレプリカデータが未収集の場合のフォールバック（近傍日の構成を候補表示 or
    自由入力＋取込後の突合チェック）を用意する
- **実装・運用の順序**（プルダウンが日付時点のレプリカに依存するため収集が先）:
  1. UNO系4店をstores.jsonへ追加しバックフィル収集（発表ログを遡れる期間＋ana-slo保持期間
     約1年1ヶ月の範囲で）
  2. 入力フォーム＋`ground_truth.db`実装
  3. 過去ログの初期入力（ローテ4店=直近1〜2ヶ月連続、較正2店=発表日全部/間引きは無作為）
  4. 検証実行（ローテ検出再設計のたたき台=1.2節、w1/w2較正=1.6節）

## 関連ファイル

- 実装済み内容の一次情報 → [`データ分析_skill.md`](データ分析_skill.md)（patterns.py「店舗×日
  高設定台数上限キャリブレーション」・score.py同名セクション・「翌日予測(Stage7-0〜7-2)」参照）
- 実装状況の一覧 → [`実装チェックリスト.md`](実装チェックリスト.md) Phase G・機能B再設計関連
- 要件定義 → [`../要件定義.md`](../要件定義.md)
