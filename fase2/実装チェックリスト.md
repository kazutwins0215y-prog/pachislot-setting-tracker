# fase2 実装チェックリスト

実装タスクの完了状況一覧（[x]=実装済み）。パイプライン全体の構成・データフローは
[`データ分析_構成図.md`](データ分析_構成図.md)を参照
（旧フローチャートは内容が古くなり構成図と重複するため2026-07-12に削除）。

---

## タスクリスト（実装順）

### Phase A — patterns.py 基盤

- [x] Stage 0〜4-1（preprocess.py）— **実装済み**
- [x] **A-1** `detect_events / detect_all_events / is_new_series` — 移動/増台/撤去検出
- [x] **A-2** `score_zentaiki` — S_全台系（当日横断スコア集計）
- [x] **A-3** `score_shintai` — S_新台増台
- [x] **A-4** `score_idoudai` — S_移動台
- [x] **A-5** `compute_breadth_scores` — 幅型まとめ関数

### Phase B — patterns.py 深さ型

- [x] **B-1** `benjamini_hochberg` — FDR補正（他のすべての深さ型が依存）
- [x] **B-2** `acf_screen` — ACFスクリーニング
- [x] **B-3** `pdm_confirm` — PDM確認
- [x] **B-4** `lomb_scargle_screen` — 欠損率高台向け代替
- [x] **B-5** `calendar_test` — カレンダー17候補検定
- [x] **B-6** `score_teppandai` — S_鉄板台（2経路統合）。2026-07仕様変更: 旧「検出台の全日に定数1.0/0.6」を廃止し、**該当日のみ**効果量ベースでスコア付与(カレンダー=rank-biserial、周期=位相ビン平均差/0.5、noisy-or統合)。検出条件は`teppan_conditions`テーブル(analysis.db)へ保存
- [x] **B-7** `score_rotation` — S_ローテ
- [x] **B-8** `score_sueki_daily` — S_据え置き（2026-07-08 タスク3で日次判定版に差し替え。詳細はH-11参照）
- [x] **B-9** `compute_depth_scores` — 深さ型まとめ関数

### Phase C — αブレンド

- [x] **C-1** `compute_short_term_score` — 短期版算出
- [x] **C-2** `walk_forward_alpha` — ウォークフォワードα学習 → **2026-07停止**(ターゲット=当日平均high_probが特徴量と同日情報を共有するリークで実質自己回帰だった。関数はStage7での再利用に備え残置・現在未使用)
- [x] **C-3** `blend` — α合成
- [x] **C-4** `learn_all_alphas` — **2026-07仕様変更**: 固定α=0.3(`FIXED_ALPHA`)を返す。真のウォークフォワード(ターゲット=t+1実測差枚)はStage7で機能B再設計とあわせて再実装予定

### Phase D — score.py

- [x] **D-1** `score_kadou_hikusha` — S_稼働低さ
- [x] **D-2** `compute_reliability` — 信頼度計算
- [x] **D-3** `synthesize` — 合成スコア Σ(wᵢ×Sᵢ)÷Σ(wᵢ)
- [x] **D-4** `update_store_profile` — DB書き込み

### Phase E — 出力（Streamlit）

- [x] **E-1** `app_a.py` — 機能A: 店内比較・可視化ツール
- [x] **E-2** `app_b.py` — 機能B-詳細: 振り返りダッシュボード
- [x] **E-3** `app_b.py` — 機能B-簡潔: 狙い目メモ
- [x] **E-4** `app.py`（2026-07追加）— 機能A・機能B-詳細をサイドバー選択メニューで切り替える統合エントリポイント。`app_a.render()`/`app_b.render_detail()`として画面本体を分離し、`st.set_page_config`は`app.py`側で1回のみ実行。単体起動(`streamlit run app_a.py`等)も引き続き可能

### Phase F — 複数店舗（multi_store.py）— 2026-07実装完了

- [x] **F-1** Stage 1b — 店舗別行動成分（回転数重み）
      - `learn_bin_curves`: 全店舗データをプールし、機種別に台内zスコア(`compute_kaiten_zscore`)のデシル→logLR_rng(疑似正解)基準の実測曲線を学習。標本200件未満の機種は対象外。結果は`kaiten_bin_curves.json`に保存し`compute_all_logLR`が自動読込
- [x] **F-2** Stage 5 — 階層モデル・γ_store学習 → **2026-07再設計(LOSO交差検証+合格ゲート)**
      - ⚠️ 旧実装は「y1を教師に学習したカーブ(y3)をy1に回帰」する循環学習で、傾きが構成上ほぼ1になり検証になっていなかった(実測β3≈0.985はこの理論値通りで無意味な値。γ_storeもノイズ主体)
      - 現実装: `validate_kaiten_channel`(LOSO交差検証)で「店舗sを除いて学習したカーブの直交化残差y3⊥が、店舗sの**翌観測日**y1を予測するか」を検証。合格(プール相関>0・p<0.05・正の店舗率≥70%)時のみw3=プール回帰傾きを保存、不合格時は**w3=0.0(回転数チャンネル無効)**
      - 直交化: y3から`a+b·y1`成分を除いた残差のみをチャンネル③に使用(RNG証拠の二重計上防止)。パラメータ(orth_a/orth_b)は全店舗プールで学習し`stage3_channel_weights.json`に保存
      - Stage3の既定値もw3=0.0に変更(`preprocess._DEFAULT_CHANNEL_WEIGHTS`)。テスト2店舗でのLOSO検証は不合格(r≈0)を確認済み。本番5店舗での判定は`python multi_store.py`実行時に表示される
- [x] **F-3** Stage 6 — 検証戦略（Tier再現性・マクロ整合性）
      - `check_tier_reproducibility`（複数店舗共通機種のTier判定・回転数↔RNG相関符号の一致確認）・`check_macro_consistency`（実測差枚率と事後high_prob平均のスピアマン相関、実データではρ=0.7で整合）
      - `run()` — 上記を一気通貫で実行するエントリポイント（`python multi_store.py`）
      - 6-3（店舗群分割クロスバリデーション）は未実装。店舗数が増えた段階での追加検討事項

### 運用補助スクリプト（2026-07追加）

- [x] `run_store_profile.py` — 1店舗分のpreprocess→patterns→scoreパイプラインを通しで実行し、分析DB(`ホールデータ/analysis.db`)の`stage3_scores`(Stage3出力の台×日スコア)と`store_profile`を作成・更新する(`--hole <店舗名>`で特定店舗のみ)。機能Bはこれらのテーブルを読むだけの設計のため、新規に取り込んだ店舗は実行するまで機能Bに表示されない。fase4(日次自動実行)が実装されるまでの間の手動運用を想定
- [x] `data_source.py`（2026-07追加）— fase2共通のデータ読み込み層。入力=Tursoレプリカ(`ホールデータ/turso_replica.db`、fase1が維持・読み取り専用)、成果物保存先=分析DB(`ホールデータ/analysis.db`)のパスと接続を集約。旧店舗別DB(`ホールデータ/{店舗名}.db`)はアーカイブ扱いでfase2は参照しない

### Phase G — 店舗上限キャリブレーション（G-1実装済み・2026-07-06、G-2〜G-4未着手）

> 詳細: [`データ分析_skill.md`](データ分析_skill.md)「店舗×日 高設定台数上限キャリブレーション」・今後の項目は[`今後の実装予定.md`](今後の実装予定.md)2節参照

- [x] **G-1** Step1: 候補C（長期/短期αブレンド流用）— `score.compute_uplimit`/`compute_daily_uplimit_ratio`・`patterns.blend_scalar`・`uplimit_config.json`・`store_profile`の`上限キャリブレーション値`/`上限信頼度`カラムを実装。実データ5店舗で動作確認済み。**要フォローアップ**: 一部店舗で発動日割合が非常に高い(最大82.7%)ため、`uplimit_config.json`の安全マージン・絶対上限の見直しが必要な可能性あり(データ分析_skill.md参照)
- [ ] **G-2** Step2: 候補D（Kalman filter、multi_store.pyのKalman基盤構築後に統合）
- [ ] **G-3** 周年・グランドオープン等の特異日除外ロジック（案Aの手動除外リストで暫定運用中。自動識別は未設計・保留）
- [ ] **G-4** 日タイプ別（イベント日/通常営業日）に上限を分けるか店舗単位1本にするかの判断（データ量を見て決定）

### Phase H — 機能B再設計（H-1〜H-5実装済み・2026-07-06、H-6未着手）

> 詳細: [`データ分析_skill.md`](データ分析_skill.md)「出力3系統」機能B再設計・今後の項目は[`今後の実装予定.md`](今後の実装予定.md)1節参照

- [x] **H-1** Phase1: `pattern_history`テーブル（`score.write_pattern_history`、append-only）
- [x] **H-2** Phase2: 符号付きスコア拡張（幅型`np.clip(raw/0.5,-1,1)`・深さ型S_鉄板台の非該当日負スコア・`score.synthesize`の信頼度重み減衰・`app_b.py`表示レンジ`[-1,1]`/`RdBu`配色）
- [x] **H-3** Phase3: Stage7-0〜7-2（`teppan_conditions.周期日数`・`predict_next_day`/`predict_next_day_with_blend`・`prediction_log`・`evaluate_predictions.py`）
- [x] **H-4** Phase4: トップページ（`app_top.py`。店舗ランキング・当日の熱い台・翌日予測ランキング）
- [x] **H-5** Phase5: 店舗プロファイル拡張（`app_b.py`の検知期間履歴・当月カレンダーヒートマップ2層）
- [ ] **H-6** Phase6（将来項目）: Stage7-4（ウォークフォワードα学習への置き換え）・Stage7-5（S_ローテの非該当日判定・翌日予測拡張。S_据え置きはH-11で実装済み）
- [x] **H-7** Stage3事前確率切片β₀（2026-07-07。`preprocess.compute_log_odds`にβ₀=ln(π/(1-π))を導入、π=`stage3_channel_weights.json`の`prior_high_ratio`既定0.15。狭帯域問題・上限キャリブレーション過剰発動を根治。詳細は`データ分析_skill.md`Stage3節）
- [x] **H-8** Stage7-3: 遷移モデル全台翌日予測（2026-07-07。`patterns.estimate_transition_matrix`/`predict_transition_with_blend`・`run_store_profile._run_transition_predictions`・予測種別`'遷移予測'`。実データ検証で全店舗が日次ほぼ無記憶と判明、条件付き拡張は`今後の実装予定.md`1.5節。同1.6〜1.7節に重み学習・GBDTの検討事項を記録）
- [x] **H-9** 古い予測ランキングの除外/明示（2026-07-08。`app_top._load_latest_predictions`にグローバル最新対象日−2日(`_PREDICTION_STALE_GRACE_DAYS`)の鮮度フィルタを追加。個別台/機種タブのみ対象。猶予内の古い行は「鮮度」列+`ui_theme.style_stale_rows`でグレーアウト表示、店舗/全店舗横断が全て猶予落ちした場合は専用メッセージ表示）
- [x] **H-10** 個別台タブの理由文表示（2026-07-08。実装済み未使用だった`app_top._reason_text`を`_render_prediction_tabs`に接続。個別台タブのテーブル下に`st.caption`でカレンダー条件/周期日数の根拠短文を表示。据えタブ等への拡張・ローテタブ対応は`今後の実装予定.md`1.4節に残す）
- [x] **H-11** 据え置きの日次判定＋翌日予測（2026-07-08 タスク3。`patterns.score_sueki_daily`/`sueki_daily_r`が直近14日窓のlag-1自己相関をEWMA平滑して日次符号付きスコア化(旧`score_sueki`の全期間1定数を廃止)。`run_store_profile._run_sueki_predictions`が長期/短期ブレンドで予測種別`'S_据え置き'`を`prediction_log`へ追記。`estimate_transition_matrix`に`pi`(ベース率)を追加し、`store_profile`に遷移_ベース率/p_stay/p_up/ペア数の4列を保存(記録のみ、表示は今後の実装予定)。`app_top._SUEKI_SCORE_GATE`を0.5→0.0に調整。詳細は`データ分析_skill.md`「据え置きの日次判定と翌日予測」参照）
- [x] **H-12** 遷移モデルの前日差枚条件付き拡張＋並走ログ基盤（2026-07-08 タスク4。`patterns.estimate_transition_matrix_stratified`が前日(t-1)の実測差枚が店舗内上位2割か否かで層別した遷移行列を推定(ペア構築は`_build_transition_pairs`で無条件版と共通化)。層間差の有意性は並べ替え検定(`_stratified_permutation_test`、1000回・有意水準0.05・統計量max(|Δp_stay|,|Δp_up|))で判定し、有意な店舗のみ`run_store_profile._run_transition_predictions`が予測種別`'遷移予測_前日差枚'`を無条件版(`'遷移予測'`)と並走記録(DELETE/UPDATEなし)。`evaluate_predictions.py`は無改修で両種別を対比較可能、`app_top.py`は`'S_鉄板台'`のみ表示のため新種別は非表示。実データ9店舗中3店舗(yasuda7・グランパ中野・マルハン新宿東宝ビル店)で有意。詳細は`データ分析_skill.md`「遷移モデルの前日差枚条件付き拡張＋並走ログ基盤」参照）
- [x] **H-13** wᵢ学習用の日次スナップショット記録・記録側のみ先行実装（2026-07-09 タスク5。`score.write_score_snapshot`/`score_snapshot`テーブル(店舗×使用データ最終日で1行、各サブスコアの当日断面店舗平均・狙い目度_店舗平均・実効重みJSONを保存)を新設し、`run_store_profile.run_for_hole`の`synthesize`直後から呼ぶ。重複ガードは`write_prediction_log`と同型((ホール名,使用データ最終日)既存ならスキップ)。学習側(Spearman直接最大化等)は蓄積待ちのため`今後の実装予定.md`1.6節に残す。`fase3/upload_analysis.py`の`TABLES`固定リストのため自動アップロード対象外(確認済み)。詳細は`データ分析_skill.md`「score_snapshotテーブル」参照）
- [x] **H-14** 機種強さ軸・全台系/高配分の日次判定 Phase1（2026-07-09。`patterns.score_zentaikei_judgment`が機種×日×ホールでzスコア(Σhigh_prob−n×π基準の異常検定)・投入率(Σhigh_prob÷n)・S_全台系(揃いの確認)の3指標から判定ラベル(全台系/高配分/普段どおり)を算出、`benjamini_hochberg`で同日×同ホール内のFDR補正。`score.write_machine_judgment_log`/`machine_judgment_log`テーブル(機種×日×ホール粒度、`PRIMARY KEY`への`INSERT OR IGNORE`で全履歴バックフィル+日次追記)を新設し、`run_store_profile._run_zentaikei_judgment`が`write_stage3_scores`直後から呼ぶ。`app_a.py`機種名比較ビューのY軸に「設定投入率」を追加(0〜6軸・0.9基準線・zゲートのグレー表示・ツールチップ)。閾値はPhase1固定値、実測突き合わせによる調整は`今後の実装予定.md`1.8節Phase2。詳細は`データ分析_skill.md`「機種強さ軸・全台系/高配分の日次判定」参照）
- [x] **H-15** 末尾版: 台番号末尾グループのカレンダー構造検出 フェーズ1〜4（2026-07-10。フェーズ1(検証): マルハン新宿東宝・エスパス歌舞伎町の実データで「6の日×末尾6」の答え合わせを実施し、当初設計のStouffer統合z(π=0.15)は店舗オフセット(sigmoid飽和で平均high_prob>>π)により全198仮説が有意化して破綻すると判明。既存`calendar_test`と同じ「候補日vs対照日」のMann-Whitney U相対検定(A案)に変更して確定。フェーズ2(検出側): `patterns.calendar_candidates`を17→49候補(毎月X日31を追加、鉄板台検定`calendar_test`にも自動適用)に拡張、`tail_digit_group`(末尾0〜9+ゾロ目11グループ、関数注入で「グループ=機種」等に差し替え可能)・`group_calendar_test`(グリッド539仮説)・`match_rule_test`(一致ルール2本、Wilcoxon符号順位検定)・`build_group_calendar_conditions`(541仮説を1群としてBH補正+効果量ゲート)を新設。新テーブル`group_calendar_conditions`(`score.write_group_calendar_conditions`、`teppan_conditions`と同じ全削除→再挿入方式)に`run_store_profile._run_group_calendar_conditions`から保存。フェーズ3(並走予測): `patterns.predict_tail_group_next_day`が該当する有意条件のmax(効果量)を採用(重複統合)し、`_run_tail_group_predictions`が予測種別`'S_末尾'`で`prediction_log`へ並走記録(長期/短期ブレンドなし)。フェーズ4(UI): `app_a.py`機種名比較ビューの「統合スコア」軸を廃止し「設定投入率」軸へ完全置き換え(台番号ビューは統合スコアのまま維持)。実データ検証: 本命「末尾6×6の日」が両店舗で541仮説中1位(rbc0.98〜1.0)、副産物で「ゾロ目台×ゾロ目の日」も両店舗で有意(rbc0.76〜0.78、ユーザー確認済みの鉄板)。詳細は`データ分析_skill.md`「末尾版: 台番号末尾グループのカレンダー構造検出」・`今後の実装予定.md`1.8節参照）
- [x] **H-16** 機種単位の癖分析: 看板機種＋機種カレンダー癖（2026-07。末尾版と同じ検出器(`build_group_calendar_conditions`)をグループ定義=機種で再利用。`patterns.machine_group`(機種名そのまま・日次有効台数中央値2台未満はNaN=検定対象外、`MACHINE_GROUP_MIN_UNITS`)・`patterns.group_constant_test`(看板機種検定。「自グループvs同日の他グループ平均」の対応ペアWilcoxon符号順位検定、末尾版のStouffer破綻と同型のリスクを一致ルールと同じ対応ペア化で回避)を新設。`build_group_calendar_conditions`に`include_match_rules`/`include_constant`フラグを追加(機種版は一致ルール除外・看板機種検定を含める)。恒常窓(全期間)・直近`RECENT_TEST_WINDOW_DAYS`(=90)日窓の2窓検定を独立にBH補正して並走保存(グループ種別`'機種'`/`'機種_直近'`。個別台側のαブレンドと異なり2窓検定を選んだ理由は`今後の実装予定.md`1.8節参照)。`run_store_profile._run_machine_group_conditions`/`_run_machine_group_predictions`が`predict_machine_group_next_day`経由で予測種別`'S_機種'`/`'S_機種_直近'`を`prediction_log`へ並走記録(機種→台展開、長期/短期ブレンドなし)。`write_group_calendar_conditions`のDELETEスコープを「ホール名のみ」→「ホール名+グループ種別」に修正(機種版書き込みが末尾版の既存行を消すバグを解消)、`group_calendar_conditions`に`台数中央値`列を追加(機種版のみ、使用側ゲートの判断材料)。実データ検証(マルハン新宿東宝: 検定対象97機種・全期間9件/直近90日3件有意、bigディッパー東中野: 検定対象36機種・1件有意「いざ！番長」)、fase4自動実行(2026-07-10 10:30追い実行)で全10店舗の一括実行も確認済み。新台/増台/再導入/移動台の「導入後の扱い」は未実装で`今後の実装予定.md`1.8節に設計メモを残す。詳細は`データ分析_skill.md`「機種版: 看板機種＋機種カレンダー癖の検出」参照）

- [x] **H-17** 導入後カーブ: 新台/増台/減台/再導入/純移動の判別＋経過日数ビン検定（2026-07-13。`patterns.detect_introduction_events`が店舗観測日ベースのタイムラインで機種レベルイベント5カテゴリを判別(増台側は復帰ガード、減台は7日遅延確定、再導入は不在7日ベース)。`patterns.introduction_curve_test`がカテゴリ5種×経過日数ビン5種(初日/2〜3日/4〜7日/8〜14日/15日以降)=最大25仮説を店舗単位でWilcoxon符号順位検定(両側)+BH補正。新テーブル`introduction_events`(`score.write_introduction_events`、店舗単位で全削除→再挿入)・`group_calendar_conditions`にグループ種別`'導入後'`を追加保存。`run_store_profile._run_introduction_conditions`/`_run_introduction_predictions`が予測種別`'S_導入後'`を`prediction_log`へ並走記録(合成スコアには混ぜない)。`app_top.py`の新台/増台タブは`introduction_events`の機種の最新イベントカテゴリで対象を振り分け(旧`detect_events`は区別不能だった、今後の実装予定.md 1.2a節の残課題を解消)。実データ10店舗検証: BH有意20件(新台11・増台5・減台1・再導入3)、効果量は一貫して負(導入直後は店内平均より投入率が控えめ)。詳細は`データ分析_skill.md`「導入後カーブ」参照）
- [x] **H-18〜H-22** Phase6: 機能B理想形（2026-07-13設計合意・同日実装。`prediction_accuracy`の実測精度検証(遷移予測/S_据え置きは無効確定、S_鉄板台は店舗依存、S_末尾/S_機種は有望)を受けた5項目の再設計。詳細は`データ分析_skill.md`「機能B再設計」Phase6参照）
  - **H-18** おすすめ店舗の定義変更: `app_b.compute_store_recommend_score`（「設定予測の的中が期待できる店舗」ランキングへ変更。`build_habit_matrix`の軸別ステータスを再利用し実績ベース(spearman相関)/有意条件ベース(`min(件数,10)×効果量`、案cハイブリッド)を合算。S_稼働低さはランキング外の添え列。件数キャップは実データで三ノ輪unoが50超に張り付く事故を受けて導入）
  - **H-19** 熱い台予測のスケール統一: `app_top.signed_percentile`/`_apply_signed_percentile`（7タブの生スコアを符号付きパーセンタイル(案b・0固定アンカー)に統一。参照分布は個別台/機種タブが`prediction_log`、他は`stage3_scores`から構築。表示列名を「予測スコア」に統一）
  - **H-20** 店舗特徴の癖の有効性マトリクス: `app_b.build_habit_matrix`（軸(S_鉄板台/遷移予測/S_据え置き/S_末尾/S_機種/S_導入後)ごとに有効/弱い/無効/検証中/データ不足/対象外を判定。旧サブスコア内訳の縦棒グラフを置き換えて削除）
  - **H-21** 検知期間履歴の機種名/台番号つき表示: `app_b._load_zentaikei_periods`/`_load_stage3_pattern_periods`/`_load_introduction_events`（`pattern_history`店舗全体平均の近似表示から、`machine_judgment_log`/`stage3_scores`/`introduction_events`の実データ参照へ切替。ローテはタブを残し「再設計待ち」案内のみ。稼働低さのみ`pattern_history`ベースを維持）
  - **H-22** カレンダーヒートマップの再設計: `app_b._store_relative_scores`/`_load_future_calendar_conditions`/`_project_future_calendar_scores`（表示項目を「設定配分」「差枚」の2指標に整理し店舗内相対評価(-1〜1、店舗中央値アンカーの符号付きパーセンタイル)へ。最終収集日より先はカレンダー型有意条件(`teppan_conditions`経路='カレンダー'・`group_calendar_conditions`)を将来日付に照合して投影）

### Phase I — 正解発表（ground truth）受け取り基盤（I-1〜I-2実装済み・2026-07-12、I-3〜I-4未着手）

> 詳細: [`データ分析_skill.md`](データ分析_skill.md)「`ground_truth`テーブル」・今後の項目は[`今後の実装予定.md`](今後の実装予定.md)3節参照

- [x] **I-1** ステップ1(一部): `fase1/stores.json`へローテ実証用店舗のうち新橋uno・三ノ輪unoを追加（バックフィル収集はfase1の日次実行で進行中）。残り有楽町UNO・有楽町DUOの追加は未着手
- [x] **I-2** ステップ2: `ホールデータ/ground_truth.db`＋`fase2/ground_truth_entry.py`（ローカル専用Streamlit、`streamlit run fase2/ground_truth_entry.py`）を実装。日付→ホール→機種名→台番号の順にレプリカ`slot_data`から候補を絞り込む入力フォーム、レプリカ未収集日の近傍±3日フォールバック、直近1件のみの取消、入力済み一覧のフィルタ・突合チェックを実装。`ground_truth`テーブルへのinsert/delete・レプリカ照会ロジックを実データで単体テスト済み
- [ ] **I-3** ステップ3: 過去ログの初期入力（ユーザー作業。ローテ実証用店舗は直近1〜2ヶ月連続全日、較正用店舗は発表日を無作為抽出）
- [ ] **I-4** ステップ4: 検証実行（ローテ検出再設計=1.2節、w1/w2較正=1.6節、機種判定Phase2較正=1.8.1節のたたき台としてground_truthラベルを使用）
