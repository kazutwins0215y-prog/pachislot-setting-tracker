__pycache__/
*.pyc
*.log
*.lock
.venv/
venv/
.env

# fase4: 機種スペック再取得の最終実行日(実行のたびに変わるためGit管理外)
fase4/specs_refresh_state.json

# SeleniumBase (UC Mode) が実行時に作る作業ディレクトリ
downloaded_files/

# SQLite系ファイルの誤コミット防止（主要DBはホールデータ/配下だが保険として全体で除外）
*.db
*.db-wal
*.db-shm

# ローカルSQLite（Turso移行後は不要。移行前の生データはローカルに残す）
ホールデータ/

# 開発ハーネスの計画書（ローカル履歴として残すがGit管理外）
.claude/plans/
