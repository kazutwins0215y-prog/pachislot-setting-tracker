# このファイルは fase2/.streamlit/config.toml と内容を同期させること
# (Streamlit Community Cloudはcwdがリポジトリルートなためこちらのファイルが効く。ローカル実行はfase2側が効く)

[theme]
base = "dark"
primaryColor = "#D6FE3E"
backgroundColor = "#0D0E11"
secondaryBackgroundColor = "#1A1C21"
textColor = "#F2F3F5"

[client]
toolbarMode = "minimal"
